/dist
.env
.DS_Store
/uploads
/node_modules
yarn-error.log

.idea

coverage

!src/**
# ...but that negation also re-includes the root .gitignore's `**/.impeccable/`
# tool cache under src/, so re-ignore it here.
src/**/.impeccable/

./tsconfig.tsbuildinfo
medusa-db.sql
build
.cache

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.medusa
# runtime uploads served at /static (restore via scripts/restore-backend-static.mjs)
/static

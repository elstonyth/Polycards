# Implementation Plans — Full-Repo Bug Hunt (Pokenic)

Generated by the `improve` skill on 2026-07-02, against commit `4ca2593`.
Scope: every function / feature / button / route across the **storefront**
(`src/`), the **Medusa backend** (`backend/packages/api`), and the **admin
dashboard** (`backend/apps/admin`). Method: seven parallel read-only audits
(money paths, gameplay/fulfillment, admin API + platform, cross-cutting
security, storefront account/money UI, storefront gameplay/marketing UI, admin
dashboard app), then every surviving finding re-opened and vetted against the
live code by the advisor.

**Payment gateway is deliberately excluded** — top-up runs through the mock
gateway (`ALLOW_MOCK_TOPUP` / `unsafe-demo` prod opt-in) and reward payout is
dormant until a real PSP + AMLA compliance land. Those are decided tradeoffs,
not bugs. Implementation bugs _inside_ those guards were still in scope.

Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done. **Do not fix anything not covered by a plan
without checking the "considered and rejected" list first** — several
plausible-looking issues were investigated and found to be by-design.

---

## Headline

The money core is in good shape. Per-customer advisory locks, an append-only
ledger with a DB-level unique index on `pull_id`, idempotent workflow steps,
frozen-account gates, and commission decay all held up under scrutiny. **No
confirmed critical vulnerability and no confirmed money-loss bug were found.**
What remains is one high-value _verification_ (admin FX route auth), one
real UX-severity bug (a charged user can get stuck on the spin screen), and a
tail of low-severity validation/hardening items.

---

## Execution order & status

| Plan | Title                                                          | Priority | Effort | Depends on | Status                                                                                                                           |
| ---- | -------------------------------------------------------------- | -------- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 001  | Verify + harden admin FX-rate route auth                       | P1       | S      | —          | DONE                                                                                                                             |
| 002  | Watchdog for charged-but-stuck pack/slot open                  | P1       | M      | —          | DONE (claw deferred — different state machine; behavioral proof by-inspection + smoke)                                           |
| 003  | AuthProvider: don't log users out on transient network errors  | P2       | S      | —          | DONE                                                                                                                             |
| 004  | Close admin money-input validation gaps                        | P2       | S      | —          | DONE                                                                                                                             |
| 005  | Regression guard: storefront credit-reason enum ⊇ backend enum | P2       | S      | —          | DONE                                                                                                                             |
| 006  | Resolve Zod-JIT vs CSP `unsafe-eval` before enforcing CSP      | P2       | M      | —          | DONE (Option 1 — `z.config({ jitless: true })` already present in schemas.ts; verified via zod source + tests; no code change)   |
| 007  | Triage & upgrade path for dependency CVEs (npm audit)          | P3       | L      | —          | DONE (root 15→5 via in-range fixes + next 16.2.10 patch; backend + rest framework-blocked, see 007-audit-report.md)              |
| 008  | Low-severity backend hardening bundle                          | P3       | M      | —          | DONE (A & D already-safe/verdict, no change; B/C/E/F applied; new pagination-bounds spec + external-funded/delivery specs green) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

## Dependency notes

- All plans are independent and can be executed in any order. The numbering is
  by leverage (impact ÷ effort ÷ risk), not by dependency.
- 001 is first because it is a _verification_ with a potentially critical
  payoff and a tiny fix; run it before anything else so the answer is known.

---

## Round 2 — full-history review (PR #1 → current), 2026-07-12

Generated by the `improve` skill against commit `e9ce6968` (branch
`feat/virtual-shop`). Scope: the delta since round 1's `4ca2593` (131 commits /
~758 files — the sim harness, achievements, avatar frames, media pipeline,
delivery cancel + serialization, VIP settle, admin-dashboard churn), plus a
full re-verification of the month-long simulation's 15 findings against the live
code. Method: four parallel read-only audits (backend money core, backend
feature delta, admin dashboard, storefront), every surviving finding re-opened
and confirmed by the advisor.

**Method caveat — this is a static code-read audit, not a runtime pass.** No
flow was exercised live this session (no fresh buy→open→sell→deliver→cancel run).
"Fixed in code" means the advisor read the current source and confirmed the fix
is present; it rests on that read plus the _prior_ simulation run, not on a new
end-to-end exercise. The headline below is "what the code-read found," not
"verified working in a running system." Before shipping, run the flows (or the
integration suites named in each plan) to confirm behavior.

**Headline.** The commerce core held up under the read. **11 of the 15
simulation findings are confirmed fixed in the current code** (idempotent-topup
visibility, customer cancel route + concurrency serialization, VIP batch-open
grant settlement, FX quote firmness on the sell path, four error-copy fixes),
and the showcase-sell worry is a non-issue (read paths gate on vaulted status).
Every credit-writing path in the delta routes through the advisory-locked
mutation. **No money-loss bug and no critical vulnerability were found by the
code-read.** What remains is a tail of correctness/UX bugs, two
security-hardening items, and one test gap — all S/M effort.

The single item with real money exposure is **009** (unbounded card markup on the
edit path); the one security item with a near-anonymous trigger is **010**
(customer-metadata mass-assignment on account create).

| Plan | Title                                                                                                                                                                  | Priority | Effort | Depends on      | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 009  | Bound card markup multiplier (edit path + backend validator)                                                                                                           | P1       | S      | —               | DONE (worktree `agent-a89f05e9ae07dca65`, commit `eaaf26ba`; ceiling 11 = 1000% under `1+pct/100`; +3 unit cases green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                                      |
| 010  | Reject client metadata on customer **create** (frame/avatar/handle bypass)                                                                                             | P1       | S      | —               | DONE (worktree `agent-aee05db63bd98d23d`, commit `b3e99c34`; matcher exact-match confirmed non-shadowing, live spec 2/2 green, register call traced metadata-free, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                           |
| 011  | Invalidate balance + tx views after a commission reversal                                                                                                              | P2       | S      | —               | DONE (worktree `agent-a498188cd73049dca`, commit `dc80b6d6`; mirrors `useFreezeCustomer` pattern, build + 25 tests green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                                    |
| 012  | Fix blank pack titles in admin pull ledger (join by slug, not id)                                                                                                      | P2       | S      | —               | DONE (worktree `agent-a6381dddc80920f79`, commit `ef638d82`; new `admin-pulls.spec.ts` proven red→green on a live DB, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                                        |
| 013  | Paginate Customer360 commissions + audit tables                                                                                                                        | P2       | M      | —               | DONE (worktree `agent-abe87016382d23af1`, commit `93373b76`; Pager mirrors support page, reset-on-id guard added, `queries.ts` untouched so no 011 conflict, build + 25 tests green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                         |
| 014  | Fix daily-credit header balance desync (re-read, don't set prize)                                                                                                      | P2       | S      | —               | DONE (worktree `agent-ab288a78fefe02350`, commit `398c69f9`; `void refreshBalance()` swap, typecheck/lint/200 tests green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                                   |
| 015  | Close remaining admin money-input gaps (zero-adjust, daily-box qty)                                                                                                    | P2       | S      | —               | DONE (worktree `agent-a95c58cfd69e44d4a`, commit `d9d89dc9`; qty ceiling verified against backend `daily-box.ts` = exactly 1, build + 25 tests green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                        |
| 016  | Put price/balance/shortfall in pack-open "Not enough credits"                                                                                                          | P3       | S      | —               | DONE-with-caveat (worktree `agent-aa109382e7a5ae42d`, commit `c5817c60`; backend both throw sites interpolate price/balance/shortfall, live spec green, reviewer-verified. **CAVEAT:** storefront `packs.ts:104` `friendlyError` rewrites the message to a generic string + shows a top-up CTA (`needsTopUp`), so numbers reach direct API clients but NOT the storefront user. Surfacing numbers on the storefront is a product decision — see follow-up note. **merged into `feat/virtual-shop` 2026-07-12**) |
| 017  | Extract + test the voucher-ladder fold logic                                                                                                                           | P3       | S–M    | —               | DONE (worktree `agent-ae6f4f5820a4e8b46`, commit `96a68df2`; verbatim extraction to `voucher-ranges.ts`, +12 tests (37 total), build green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                  |
| 018  | Backend hardening: delivery batch cap + slab-bake egress allowlist                                                                                                     | P3       | M      | —               | DONE (worktree `agent-a781a18d54d4517b9`, commits `1a3a1263` + `714d122d`; delivery cap HTTP-tested, SSRF denylist 23 unit tests green incl. IPv4-mapped-IPv6 bypass closed in review round 1, reviewer-verified; DNS-rebind residual documented as follow-up; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                  |
| 016b | Storefront: show top-up shortfall in insufficient-credit CTA (resolves 016 caveat, client-side) — _renumbered from 019 to avoid colliding with the round-3 plan files_ | P3       | S      | complements 016 | DONE (worktree `agent-af003f483a9e370a9`, commit `da71528a`; both CTAs prepend `You're {rm(shortfall)} short.` from client-side balance+price, `balance===null` falls back to prior copy, `friendlyError`/`packs.ts` untouched, typecheck/lint/200 tests green, reviewer-verified. Incidental: prettier reformatted the `Phase` type union in SlotMachineClient — cosmetic, matches existing WIP. **merged into `feat/virtual-shop` 2026-07-12**)                                                               |

Round-2 plans are independent and can run in any order; numbering is by leverage.
Recommended first three: **009, 010, 012** (highest impact ÷ effort — a money
bug, a security bypass, and a one-line-logic ledger regression).

### Round-2 findings considered and rejected / verified-fixed

Recorded so nobody re-audits them. Each was opened and confirmed by the advisor.

- **Showcase-sell guard** (sim #7, `buyback-pull.ts`): no guard exists, but it's
  harmless — a sold-back pull keeps `showcased=true`, yet both readers (vault
  list, public profile) gate on `status==='vaulted'`, so a sold card never
  appears publicly. Not a bug.
- **11 simulation findings verified fixed in code** — idempotent-topup `replayed`
  flag (#2, PR #132), daily-draw "nothing" message (#3), delivery cancel route +
  concurrency serialization (#4/#8/#12, PR #131/#133), FX quote-vs-execute
  firmness on the sell path (#5, PR #129), topup keyless-400 copy (#1), address
  missing-fields naming (#10), shared rate-limiter per-path labels (#11, P3-10),
  double-submit delivery copy (#9, PR #130), VIP batch-open level-up settle
  in-saga (#13, PR #134). Re-confirmed present; do not re-file.
- **Admin repair for pre-existing double-ship rows** (sim #2/#15): PR #133's
  per-order advisory-locked transitions prevent _new_ one-pull/two-orders
  corruption. No admin force-void route exists for rows corrupted _before_ the
  fix — but that's an operational data-repair task, not a code defect. Surfaced
  as a Direction option, not a plan.
- **Dependency posture**: `npm audit --omit=dev` (storefront) is down to 2
  moderate advisories (postcss bundled under `next`, no in-range fix — clears on
  the next Next bump). Nothing actionable beyond round-1 plan 007's Medusa-bump
  recommendation for the backend.
- **Sim #6 admin buyback-dispute — half fixed.** The _view_ half is done: PR #129
  (`5c9f6f2e`) added quote-vs-payable data to `/admin/customers/[id]/pulls`
  (`route.ts:19-64`) and the support-desk display. The _honor-a-single-quoted-
  sell-without-moving-global-FX_ half is still absent — no such admin action
  exists (`grep` for honor/settle-buyback under `api/admin` is empty; the only
  admin credit action is the raw `credits` adjust). An operator _can_ resolve a
  dispute today via that manual credit-adjust, so no admin function is broken —
  it's a missing capability with a workaround. Surfaced as a Direction option
  below, not filed as a plan.

### Round-2 coverage — what was NOT audited

Honest scope limits so the "everything works" bar isn't overstated. This was a
delta-focused **static** review; the following were not covered and are the
first places to look if a flow misbehaves:

- **No runtime exercise** — see the method caveat above. Every "fixed" verdict is
  a code-read, not a live buy/open/sell/deliver/cancel run.
- **Backend, unread or skimmed**: `admin/customers/[id]` sub-route _backend_
  contracts (audit/commissions/gacha/pulls/transactions beyond the money-write
  core), the reward-box workflow internals (`draw-reward-box.ts` /
  `settleRewardDraw`), and referral/commission internals (`reverseOpen`, override
  schedule, `matureDueCommissions`). `admin/pricing/fx/history` + `/health`
  routes unread (the main FX POST/GET was read).
- **Storefront, skimmed only**: social/browse surfaces — `profile/[user]`,
  `leaderboard`, `invite/[handle]`, `pack-party`, `repacks`, `daily/JoinPrompt`,
  and `PrizeReveal` overlay a11y. Commerce/account surfaces were swept fully.
- **Admin SPA**: not every route's zod/coercion contract was cross-checked
  against its client payload; `eslint`/`eslint.config.js` DX was out of scope.
- **Security depth**: the two security findings (010 metadata, 018 slab-bake
  SSRF) were confirmed by static reads of route + vendored handler, not by a live
  request. `/security-review` on the eventual diffs is still worth running.
- **Round-1 "considered and rejected" items** were not re-audited (trusted as
  settled).

### Round-2 Direction (options for the maintainer, not bugs)

- **Admin delivery-order repair tooling.** New corruption is prevented (PR #133),
  but no admin force-void/detach route exists to repair rows corrupted before it.
  Worth a small spike _only if_ such rows exist in prod — otherwise skip.
- **Admin "honor a single quoted sell" action** (sim #6, honor-half). The desk can
  now _see_ a disputed buyback quote-vs-payable (PR #129) but can only settle it
  by a raw credit-adjust. A dedicated action that pays out one pull at its shown
  quote without moving global FX would close the dispute loop cleanly. A
  design/spike-sized decision (what FX rate to honor, audit trail, idempotency),
  not a bug — ask for a plan if you want it built.
- **Centralize client↔backend validation contracts.** Findings 009, 011, 014,
  015 are all one shape: a guard added to one caller/modal but missed on a
  sibling, or a client `max` the backend doesn't enforce. A shared validation
  module (or backend-as-source-of-truth codegen) would kill this whole drift
  class — the same recommendation round 1 made for the enum-mirror class (plan
  005). Consider it if a third instance appears.

### Round-2 execution rollup (2026-07-12) — all 10 plans implemented in isolated worktrees, reviewer-verified, merged into `feat/virtual-shop`

Each plan was executed by a dispatched subagent in its own git worktree (branched
from `e9ce6968`), then reviewed by the advisor (scope + diff + re-run criteria).
**All 10 worktree branches were merged into `feat/virtual-shop` on 2026-07-12**
(per-plan rows above carry the merge stamps). Verification ran live where a DB was needed (`pokenic-postgres` +
`.env.test` were reachable from worktrees).

| Plan | Verdict              | Worktree branch                    | Commit(s)               |
| ---- | -------------------- | ---------------------------------- | ----------------------- |
| 009  | APPROVE              | `worktree-agent-a89f05e9ae07dca65` | `eaaf26ba`              |
| 010  | APPROVE              | `worktree-agent-aee05db63bd98d23d` | `b3e99c34`              |
| 011  | APPROVE              | `worktree-agent-a498188cd73049dca` | `dc80b6d6`              |
| 012  | APPROVE              | `worktree-agent-a6381dddc80920f79` | `ef638d82`              |
| 013  | APPROVE              | `worktree-agent-abe87016382d23af1` | `93373b76`              |
| 014  | APPROVE              | `worktree-agent-ab288a78fefe02350` | `398c69f9`              |
| 015  | APPROVE              | `worktree-agent-a95c58cfd69e44d4a` | `d9d89dc9`              |
| 016  | APPROVE (caveat)     | `worktree-agent-aa109382e7a5ae42d` | `c5817c60`              |
| 017  | APPROVE              | `worktree-agent-ae6f4f5820a4e8b46` | `96a68df2`              |
| 018  | APPROVE (1 revision) | `worktree-agent-a781a18d54d4517b9` | `1a3a1263` + `714d122d` |

> Note: the 010/012/013/014/015/016/017/018 worktrees were created **nested**
> under the 009 worktree (`…/agent-a89f05e9ae07dca65/.claude/worktrees/…`) — a
> harness path quirk; the git branches/commits are valid regardless. `git
worktree list` shows the true paths.

**Merge-order guidance** (overlapping files edit different regions → git 3-way
should auto-merge, but merge in this order and re-run the admin build after each):

- `backend/apps/admin/src/lib/queries.ts` — 011 (adds invalidations to
  `useReverseCommission`). 013 left this file untouched, so **no 011↔013
  conflict**.
- `backend/apps/admin/src/routes/customers/[id]/page.tsx` — 013 (pagination
  state + pagers) and 015 (zero-adjust guard) touch different regions. Merge
  **013 before 015**; the guard hunk sits far from the pager hunks.
- `backend/apps/admin/src/routes/daily-rewards/page.tsx` — 017 (extracts
  `foldRangesLocal`/`summarizeLevels`/`LEVELS` into `voucher-ranges.ts`, deletes
  them from the page) and 015 (edits `rowErrors` qty guard). Merge **017 before
  015** so 015's `rowErrors` hunk applies against the post-extraction file; the
  two hunks are ~470 lines apart.
- All backend/storefront plans (009 validate.ts, 010 middlewares, 012 pulls
  route, 014 DailyClient, 016 service.ts, 018 delivery+bake-slab) touch disjoint
  files — no cross-plan conflicts.

**Two follow-ups the maintainer should decide (documented, not auto-built):**

1. **016 storefront copy — RESOLVED by plan 016b** (renumbered from 019). The backend (016) names
   price/balance/shortfall for API clients; the storefront `openPack` action
   deliberately maps `/not enough credits/i` through `friendlyError`
   (`src/lib/actions/packs.ts:104`) to a generic string + a `needsTopUp` CTA, so
   the raw numbers never reach the storefront. Rather than loosen that
   sanitization, **plan 016b** shows the shortfall client-side (computed from the
   `balance` + pack price already in scope) next to the existing top-up CTA —
   delivering the plan-016 intent on the storefront with zero backend coupling.
   Both are DONE and merged into `feat/virtual-shop`. No open decision remains.
2. **018 DNS-rebind residual.** The slab-bake SSRF guard blocks IP literals
   (incl. obfuscated IPv4 and IPv4-mapped IPv6) and known internal hostnames, but
   does not DNS-resolve — a public hostname resolving to a private IP is a
   documented follow-up (add a resolve-then-check guard if this fetch ever takes
   less-trusted input). Low priority: the trigger is admin-only.

**Recommended pre-merge gate:** run `/security-review` on the 010 and 018 diffs
(auth/SSRF surfaces), and run each plan's named integration/unit suite once in
your main tree before merging (the worktree runs used `.env.test`; your prod
config may differ).

---

## Full findings table (vetted)

Ordered by leverage. "Confidence" is the advisor's after re-reading the code.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Category      | Impact                      | Effort | Risk | Confidence                 | Plan |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------------------------- | ------ | ---- | -------------------------- | ---- |
| 1   | Admin FX-rate route (`admin/pricing/fx/route.ts`) is typed `MedusaRequest` (not `AuthenticatedMedusaRequest`) and is absent from `middlewares.ts`. Needs runtime proof that Medusa's default `/admin` auth actually gates it. If it doesn't, an unauthenticated actor could set the global USD→MYR rate and skew every displayed price.                                                                                                                         | security      | HIGH _if_ unprotected       | S      | LOW  | MED (runtime check needed) | 001  |
| 2   | Pack/slot open deducts credit server-side, then relies on the reel's `transitionend` settle callback to release the UI and reveal the win. `handleSpin` has no watchdog/timeout and no try/catch around the post-charge mapping — any missed settle or thrown mapping strands a **charged** user in `phase='spinning'` with no recovery but a page refresh.                                                                                                     | bug           | MED-HIGH                    | M      | MED  | MED                        | 002  |
| 3   | `AuthProvider.refresh()` catches _all_ `/api/me` errors and calls `setCustomer(null)` — a transient network blip or 5xx logs the user out until they refresh.                                                                                                                                                                                                                                                                                                   | bug           | MED                         | S      | LOW  | HIGH                       | 003  |
| 4   | Admin money inputs under-validate: zero-amount credit adjust is accepted (`support/page.tsx`); PriceCharting `canSave` allows `stock === 0`; markup % has no upper bound; customer-search query length is unbounded.                                                                                                                                                                                                                                            | bug           | LOW                         | S      | LOW  | HIGH                       | 004  |
| 5   | `parseList()` silently drops ledger rows whose `reason` isn't in the storefront `CREDIT_REASONS`. Backend and storefront enums are **currently identical** (10 values), so no live data loss — but there is no test locking that invariant, and it has drifted before (PR #36).                                                                                                                                                                                 | tests/dx      | MED (latent)                | S      | LOW  | HIGH                       | 005  |
| 6   | Zod 4's JIT compiles schemas with `new Function()`; the storefront CSP `script-src` has no `unsafe-eval`. Flipping `CSP_ENFORCE=true` in prod (an explicit planned step) would break or spam-report every client-side validation.                                                                                                                                                                                                                               | security      | MED                         | M      | MED  | HIGH                       | 006  |
| 7   | `npm audit` reports high-severity advisories in Hono, Next.js, esbuild/Vite, and fast-uri. Most are transitive under the Medusa/Next framework and need coordinated upstream bumps, not local patches; reachability varies.                                                                                                                                                                                                                                     | security      | HIGH sev / MED reachability | L      | MED  | HIGH (audit)               | 007  |
| 8   | Low-severity backend hardening: no `(0,100)` bound on `teamOverridePct` at settings-save; no sign assert on `pack_open` external-funded cents; silent `take:1000` truncation in eligible-products and delivery-item queries; leaderboard points column type (BIGINT?) unverified; admin audit/commissions accept unbounded `limit`/`offset` at the route boundary (service clamps, so no live DoS); admin cards `GET` 500s the whole page if the FX read fails. | bug/hardening | LOW-MED                     | M      | LOW  | MED                        | 008  |

---

## Findings considered and rejected

Recorded so nobody re-audits them. Each was opened and confirmed by the advisor.

- **Weighted-pick last-row bias** (`modules/packs/pick.ts`): the `roll >= totalWeight` fallback to the last row is standard weighted-selection and documented as an accepted float-drift edge; bias is asymptotically zero. Not a bug.
- **Stock decrement race / negative stock** (`workflows/steps/decrement-card-stock.ts`): explicitly by-design — "STOCK IS A COUNTER, NOT A GATE." The adjust is guarded by `target.stocked > 0`, and the one-unit drift is documented as acceptable. Not a bug.
- **Notification mark-read TOCTOU / duplicate rows** (`store/notifications/[id]/read`): a partial unique index on `(notification_id, customer_id) WHERE deleted_at IS NULL` already exists (`models/notification-read.ts`); the retry-on-conflict re-fetch is correct. Only a rare double-miss re-throw remains (cosmetic).
- **CSRF on state-changing store POSTs** (open, buyback, topup, draw, delivery, referral): customer auth is **bearer-only** — the backend issues no customer session cookie, so there is no ambient-credential CSRF vector. Documented in `middlewares.ts:32-44`. Not a bug.
- **Admin double-submit on money buttons**: `@medusajs/ui` `Button` with `isLoading` renders as `disabled`, and the handlers (`requestAdjust`/`applyAdjust`) early-return while `isPending`. Guarded.
- **Slot `settled` ref "never cleared"** (`SlotReelColumn.tsx`): the main effect sets `settled.current = false` on its first line, and the column remounts per spin (keyed by `spinKey`). Reset is correct.
- **Slot `onSettled` stale closure** (`SlotReelStack.tsx`): `onSettledRef` is refreshed every render via an effect; the callback always reads the current value. Correct pattern.
- **`availableBalance` staleness / SHA-256 idempotency collision / soft-delete replay**: all defended by the per-customer advisory lock in `mutateCreditAtomic` and existing idempotency tests. Not bugs.
- **Media upload path traversal** (`admin/media/route.ts`): `path.basename()` after `\`→`/` normalization is sufficient; multer memory storage + `validateImage` (mime/magic-byte/size/dims) gate the rest.
- **"Verify topup/draw rate limit + CSRF exists"** (raised by the money-path audit): confirmed present in `middlewares.ts` — `createCreditTopupRateLimit()` on `/store/credits/topup`, `deliveryWriteRateLimit` on `/store/rewards/*`, all after `authenticate('customer', ['bearer'])`.

---

## Direction (not bugs — for the maintainer, low volume by request)

The user asked for bugs, so this is deliberately short:

- **Pagination is capped, not paginated** on several read surfaces (recent-pulls
  hard-capped at 12, vault at 500, no `skip`/`limit` params). Fine today; will
  bite the first user with a large vault or an "infinite scroll" feed. Covered
  as a note in plan 008, not built out.
- **A backend↔storefront enum contract check** (plan 005) is the first instance
  of a general class: several shared Zod schemas mirror backend enums by hand.
  A tiny codegen or shared-constants module would kill this whole drift class —
  worth considering if it recurs.

---

## Round 3 — deep full-repo audit (the areas rounds 1–2 didn't cover), 2026-07-12

Generated by the `improve` skill (deep tier) against commit `e9ce6968`
(branch `feat/virtual-shop`; working tree dirty at planning time — small
uncommitted changes to the from-pricecharting flow, `SlotMachineClient.tsx`,
sim scripts, `package.json`). Method: eight parallel read-only audits —
backend money internals (the round-2-unread commission/VIP/reward-box/jobs
code), backend admin-API contracts + middleware coverage, storefront
social/browse + unlisted routes, performance (first dedicated pass), tech
debt & architecture (first pass), test coverage (first pass), deps/DX/docs,
and direction. Every table-bound finding was re-opened and confirmed by the
advisor against the live code. Same caveat as round 2: **static code-read, no
runtime exercise.**

Run constraint honored: several agent worktrees were active during this run
(plans 009–018 executors, `perf/p1-frames-listcache`, `pixelslot-rebrand`,
sim branches). Round-3 plans avoid every file those touch; where a plan edits
`service.ts` (shared with TODO plan 011) the plan carries an explicit
rebase/drift instruction. Frames-list caching was excluded from the perf
audit entirely (in progress in a worktree).

**Headline.** The core held up again — and so did the layers above it. The
admin-API audit came back _clean_ (middleware coverage map verified against
Medusa framework source and Express 4 path-to-regexp wildcard semantics; every
admin/store write validates; public feeds mask PII). The money internals
audit's only finding is an availability issue, not a correctness one. The
real round-3 story is operational and trust debt: **CI never type-checks the
largest backend package; the flagship E2E money-loop spec has been disabled
since 2026-07-07; the hourly commission job holds every beneficiary's credit
lock in one transaction; the public profile route loads 20k rows per view;
and several publicly reachable pages present fabricated data — including
"verified" badges on real companies — as live product.**

### Round-3 plans (written per the skill's non-interactive default: top items by leverage)

| Plan | Title                                                                 | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---- | --------------------------------------------------------------------- | -------- | ------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 019  | Make CI type-check `backend/packages/api`                             | P1       | S      | —          | DONE (advisor-executed 2026-07-12; script pins `node node_modules/typescript/bin/tsc` because the api-local `.bin/tsc` shim mis-resolves under `yarn run` (3,585 phantom errors); direct binary + turbo `@acme/api:check-types` verified green)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 020  | Onboarding truthfulness: `.env.example`, README DB step, compose name | P2       | S      | —          | DONE — steps 2–4 done by advisor 2026-07-12; step 1 resolved by **plan 028** (2026-07-13): guard-secrets now exempts `*.env.example`/`*.env.template`, and `.env.example` carries the Medusa vars. Round-4 note: 020's README pointer at `scripts/launch-stack.ps1` turned out to be git-excluded (local-only) — corrected by 028's inline docker commands                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 021  | Commission-maturity job: one transaction per beneficiary              | P2       | M      | —          | DONE (worktree `agent-ab85c6936518ad34d`, commit `9304b98d`; enumerator `@InjectManager` + private `@InjectTransactionManager` per-beneficiary flip, notify after commit best-effort; new `mature-commissions.spec.ts` 4/4 green + neighbor suites green, advisor-reviewed; merged into `feat/virtual-shop` 2026-07-12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 022  | Public profile route: SQL aggregates + 30s cache                      | P2       | M      | —          | DONE (worktree `agent-a312d2d53d7da0cd9`, commit `f4f5a40a`; `profileStatsForCustomer` SQL aggregate (capped CTE + DISTINCT ON odds, per-card rounding), paged recent feed, showcased-only collection query, 30s cache + `clearProfileCache()`; `public-profile.spec` 7/7 incl. parity pin, leaderboard 3/3, advisor-reviewed; merged into `feat/virtual-shop` 2026-07-12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 023  | Revive the disabled E2E money-loop specs                              | P2       | M      | —          | DONE (worktree `agent-abc7f29cc93900db6`, commits `fad1bb26`..HEAD; all six `test.fixme` specs re-authored against the /slots reel + vault select-mode UI — customer flagship, anonymous demo spin (`?demo=1`), bulk-sell ("Select"/"Sell N" bar), delivery-request ("Deliver N"), both odds-reflection headlines (published-odds invariant is now a before/after snapshot of the /slots panel) — each verified passing twice; helpers: logout → /me, topup sends the now-mandatory Idempotency-Key, dead PUBLISHED_ODDS constant removed; baseline repair outside the plan's file list: card-management FMV assertions re-selectored for the MYR admin money fields (RM entered, USD stored); full suite green (18 passed, 1 pre-existing rewards env-skip); NOTE: the shared dev DB had lost the 6 seeded packs incl. `pokemon-rookie`/`pokemon-elite` — restored via the idempotent `yarn seed`) |
| 024  | Storefront honesty cleanup (fake trust signals, dead flows/code)      | P2       | M      | —          | DONE (worktree `agent-aa3af0f141451da14`, commits `0adf97af`..`511bdb0d`; merchants de-fabricated + disclosed, social/repacks disclosed + dead controls removed, /series un-sitemapped, UserMenu deleted, shadcn → devDeps; `npm run check` + 200 tests green, advisor-reviewed; merged into `feat/virtual-shop` 2026-07-12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 025  | Local integration-test runbook + smoke subset                         | P3       | S–M    | —          | DONE (worktree `agent-aa302ad78b2a6a49f`, commit `26f019e6`; api README runbook + `test:integration:smoke` (5 money suites incl. plan 021's), smoke 20/20 green twice incl. a clean-shell docs-only run, advisor-reviewed; merged into `feat/virtual-shop` 2026-07-12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

Dependency notes: all independent; run **019 first** (cheapest, gates
everything after it in CI). **025 before 021/022** if the executor wants
local verification (CI's integration job covers them regardless).
021 and 022 both edit `modules/packs/service.ts` (different methods) and both
neighbor TODO plan 011 — rebase + drift-check on conflict; do not run 021 and
022 concurrently in separate worktrees if you can avoid it.

### Round-3 findings table (vetted)

Ordered by leverage. Confidence is the advisor's after re-reading the code.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                               | Category  | Impact   | Effort | Risk | Confidence | Plan |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | -------- | ------ | ---- | ---------- | ---- |
| 1   | CI's `yarn check-types` (turbo) silently skips `backend/packages/api` — no `check-types` script; `medusa build` is SWC transpile-only. Only a local Claude Stop hook type-checks it ("a red build … it did once" — the hook's own comment).                                                                                                                                                           | dx        | HIGH     | S      | LOW  | HIGH       | 019  |
| 2   | `.env.example` omits every `NEXT_PUBLIC_MEDUSA_*` var (the publishable key has no code fallback → all /store calls 401); README backend setup never creates `pokenic-postgres`/`pokenic-redis`; compose still ships `ai-website-cloner` identity.                                                                                                                                                     | dx/docs   | MED      | S      | LOW  | HIGH       | 020  |
| 3   | `matureDueCommissions` (`service.ts:3423`) runs the whole hourly job in ONE transaction, accumulating a `pg_advisory_xact_lock('credit:<id>')` per beneficiary and awaiting a notification write per row inside it — money paths for those customers block behind the batch; a notify throw rolls back all flips.                                                                                     | bug       | MED-HIGH | M      | LOW  | HIGH       | 021  |
| 4   | Public `/store/profiles/:handle` loads ≤20k pull rows + cards + a ~100k-ceiling odds set into Node per view, folds stats in JS, and has no cache (its siblings all do). `leaderboardTop` already computes the same aggregates in SQL.                                                                                                                                                                 | perf      | MED-HIGH | M      | MED  | HIGH       | 022  |
| 5   | The 6 storefront↔backend E2E money-loop specs (`signup→topup→open→vault→sell-back`, bulk-sell, delivery, odds headline) are `test.fixme` since 2026-07-07 — the only deterministic cross-surface money coverage is off.                                                                                                                                                                               | tests     | HIGH     | M      | LOW  | HIGH       | 023  |
| 6   | Fabricated content presents as production: `/merchants` decorates real companies (Cardmarket, TCGPlayer…) with "verified" badges + invented ratings; `/social`/`/repacks` mock data + dead controls, no disclosure; `/series` (self-declared demo) is in the sitemap; `/repacks` CTA funnels into `/clawmaker`'s unconditional "Please login" wall; + dead `UserMenu.tsx`, `shadcn` CLI in prod deps. | bug/trust | MED      | M      | LOW  | HIGH       | 024  |
| 7   | Backend integration suites (66, OOM-sharded, live PG+Redis) have no documented local path — money-path verification is effectively CI-only for contributors/executors.                                                                                                                                                                                                                                | dx/tests  | MED      | S–M    | LOW  | HIGH       | 025  |

### Round-3 findings vetted but NOT planned (ask for a plan if wanted)

- **`service.ts` god object (4,237 lines, 53 methods, 8 domains, #1 churn, no
  co-located unit spec).** The highest-blast-radius refactor candidate in the
  repo. Correct sequence: characterization tests on the money seams first,
  then extract domain helpers behind the service facade (several already
  exist: `buyback-rate.ts`, `pricing.ts`, `daily-box.ts`). L effort,
  MED risk — schedule deliberately, not as a drive-by.
- **Rarity-tier duplication + half-executed decision #10.** `RARITY_ORDER`
  (`src/lib/rarity.ts:19`) and `RARITIES` (`src/lib/packs-format.ts:13`) both
  claim to be the single source; `RARITY_RGB` vs `TIER_COLOR`
  (`price-tier.ts:21`) have drifted on 3 of 6 shades, and
  `slot-machine-redesign.md:181`'s "rarity colors everywhere in the reveal"
  decision is unfinished (`PokemonToken.tsx` still imports price-tier). S+M.
  Deferred: the mobile-first redesign will rework the reveal — fold it in then.
- **Money/date formatters re-implemented per surface** (`rm`/`relativeTime` in
  `src/lib/format.ts` vs `backend/apps/admin/src/lib/format.ts`; admin
  `usdToMyr` hand-mirrors backend `displayMarketPrice` and says so in a
  comment). Third instance of the mirror-drift class (after plan 005 and the
  round-2 note). A tiny shared package kills the class; M effort.
- **FX resolver + CDN redirect micro-caching.** `resolveFxRateInfo`
  (`pricing.ts:81`) hits the DB per call on ~10 uncached routes (S; keep the
  strict money-write resolver uncached); `/cdn/cards/[file]` 302s with no
  `Cache-Control` so every admin thumbnail re-round-trips (S). Both easy;
  bundle into any nearby backend PR.
- **Real-timer sleeps in integration specs** (`pack-open-rate-limit.spec.ts:140`
  sleeps 15.5s; `password-reset.spec.ts` fixed waits). S–M; make the
  rate-limit window test-configurable.
- **Admin SPA `src/lib` transform helpers untested** (`admin-rest.ts`,
  `queries.ts`, `image-validation.ts` — churn without unit specs; routes are
  legitimately Playwright-covered). S.
- **Vault grid re-renders all ≤500 tiles per selection toggle**
  (`VaultClient.tsx:435`) — measure first; likely fine (LOW confidence).
- **`backend/yarn.lock` pins `lodash@4.18.1`/`4.17.23`** — versions that don't
  exist on the public registry (manifest says `^4.18.1`). Investigate
  provenance before any lockfile regeneration (LOW confidence, S).
- **Operator-only items:** verify `.env.local` is gitignored (the
  guard-secrets hook correctly blocked the audit from checking; rotate if it
  was ever committed). `src/subscribers/password-reset.ts` logs reset tokens
  by documented dev-mode design — revisit when a real mail provider lands.
  Diff `backend/packages/api/.env.template` against the operational vars api
  code reads (`PRICECHARTING_API_TOKEN`, `FX_USD_MYR_URL`,
  `REWARDS_REDEMPTION_ENABLED`, `COMMISSION_COOLDOWN_DAYS`, `STOREFRONT_URL`,
  `MERCUR_STOREFRONT_URL`) — hook-blocked for agents.

### Round-3 verified clean / considered and rejected

Recorded so nobody re-audits. Each was opened (or its verification basis
stated) by the auditing agent and spot-confirmed by the advisor.

- **Admin-API/middleware coverage: clean.** Framework source confirms all
  `/admin/*` routes get mandatory admin auth; every store write maps to an
  `authenticate('customer',['bearer'])` matcher; Express 4 + path-to-regexp
  0.1.x `*`-spans-`/` semantics verified against the installed dependency, so
  the multi-segment matchers really cover their sub-routes. Every admin/store
  write validates its body. Customer sub-route pagination is double-bounded;
  FX fallbacks prevent 500s; public feeds mask PII.
- **Referral/commission/VIP/reward-box internals: no defect found** — sorted
  `credit:` locks, paged reads, idempotent reversal keys, integer-sen math,
  monotonic VIP upserts, cycle-guarded sponsor linking.
- **`drawDailyBox` product-prize stock read-then-mint**: same
  "stock is a counter, not a gate" posture already settled in round 1;
  economy dormant; not re-filed.
- **Pull-table indexes**: `IDX_pull_customer_id_rolled_at`, `IDX_pull_rolled_at`
  (partial), and the partial credit-tx index exist — the hot scans are covered.
- **`vault/buyback-batch` N serial workflows**: documented `ponytail:`
  tradeoff with measured cost and named upgrade path. Settled.
- **Storefront API proxies, PrizeReveal a11y, reset-password token handling,
  no HTML sinks (`dangerouslySetInnerHTML` count: 0)**: reviewed clean.
- **Admin fetch layering** (`client` → `packs-api` → `admin-rest` → `queries`):
  documented and intentional, not competing patterns.
- **scripts/sim** shares no logic with `src/` (thin HTTP wrappers only) — no
  drift risk. `public/cdn/cards` (4.6MB) is live (seed + mock + cdn route),
  not dead.
- **Cross-surface version splits** (React 18/19, zod 3/4, tailwind 3/4):
  framework-pinned across three separate runtimes; no shared bundle; not
  actionable.
- **`/bank-withdrawal`**: honest two-state placeholder, no fabricated data —
  explicitly cleared despite the audit hint.

### Round-3 Direction (options for the maintainer — not bugs, coarse estimates)

1. **P2P selling/trading — the missing verb.** `PRODUCT.md:18` promises
   "hold, trade, redeem, or sell back"; `how-it-works/page.tsx:142` promises
   "list it on the marketplace at your own price … trade cards with each
   other directly". No listing/trade/offer/transfer route or entity exists;
   the marketplace grid reads a single house seller (Mercur multi-vendor
   installed but unused); `/social` is the mock UI for it. L, HIGH risk
   (escrow/fraud/disputes) — scope as a listing-sale-first design spike, and
   note it only pays off after real cash-out exists.
2. **PSP integration + real cash-out readiness spike.** The code anticipates
   it by name: `topup.ts` "Batch B" seams, `topup-credits.ts` "the real
   gateway slots in here" with the idempotency contract already threaded;
   `/bank-withdrawal` says payouts go live with the gateway. The compliance
   half (AMLA/KYC) is the long pole — enumerate blockers before building.
3. **Launch the dormant rewards economy.** `REWARDS_REDEMPTION_ENABLED`
   default-off ("dormant until Phase P") gates already-built claim/draw/
   withdraw routes, admin editors, and storefront surfaces. A launch spike =
   enumerate remaining Phase-P work (economy parameters, abuse limits,
   runbook), then staged rollout. M-ish, MED risk.
4. **Promote the sim harness to a nightly economy-regression gate.** The
   2-day pilot caught a P1 money bug + 10 others with 0 harness errors;
   `ledger.mjs` invariants exist; nothing in CI references sim. M, LOW risk —
   and it's the safety net options 1–3 all want.
5. **Mock-route triage (DIR-05).** Keep-as-static vs delete per route:
   `roulette` (second gacha mechanic — delete unless wanted), `merchants` /
   `pokemon` (static reference or delete), `social`/`activity`/`repacks`
   (fold into the trading track — don't delete), honest placeholders
   (`30th`, `airdrop`, `free`, `clawmaker`, `bank-withdrawal`) keep. Plan 024
   makes everything honest in the interim, so this decision loses urgency.

### Round-3 coverage — what was NOT audited

- **No runtime exercise** — same as round 2: every verdict is a code-read.
- Vendored/framework code (`node_modules/@medusajs/*`, Mercur internals) was
  read only where a finding required it (router auth, path-to-regexp).
- Admin SPA behavior was audited statically; no browser walk of every screen.
- `scripts/sim` internals beyond debt/duplication (its correctness has its own
  findings pipeline in `SIM-FINDINGS.md`).
- `backend/apps/vendor` (Mercur vendor app) — out of active use, skimmed only.
- Localization/i18n depth, Windows-specific tooling paths, and the
  `.claude/` hook scripts themselves (read for evidence, not audited).

---

## Round 4 — deep full-repo audit (post-merge master + the #135–#142 delta), 2026-07-13

Generated by the `improve` skill (deep tier) against commit `dbce0561`
(branch `claude/deep-improvement-b6191c`, in sync with master). Method:
eight parallel read-only audits — the PR #140 playthrough-withdrawal money
delta, the rest of the backend delta (#135/#139 + merge coherence of all
round-2/3 plan work), storefront sweep, tests+CI, performance, tech
debt+deps, DX/docs, and direction — with every table-bound finding re-opened
and confirmed by the advisor against the live code. Same caveat as rounds
2–3: **static code-read, no runtime exercise.**

Non-interactive run: per the skill default, plans were written for the top
findings by leverage without waiting for a selection. Seven plans (the 3–5
default was expanded because this operator has executed all 25 prior plans).

**Headline.** The merged state of rounds 1–3 is coherent — no plan work was
mangled in the merges, middlewares changes are purely additive, and the
delta's supporting casts (address guard, delivery batch, admin pagination)
audit clean. The two stories of round 4: **(1) the new playthrough
withdrawal gate measures spend on the wrong basis** — commission/buyback-
funded play banks playthrough, so a later real-money deposit can unlock
without ever being played (display-only today; becomes a money hole the day
the mandated cashout writer lands) — and **(2) CI enforcement theater**: the
backend's ~90 unit/module specs (including both new wallet-gate specs) and
the admin SPA suite are defined, green locally, and executed by nothing.
Supporting cast: the README's onboarding path dead-ends at a git-excluded
script, `backend/package.json` pins a lodash version that doesn't exist on
the public registry, and the hottest authenticated read amplifies ~6× per
call.

### Round-4 plans

| Plan | Title                                                                   | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | ----------------------------------------------------------------------- | -------- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 026  | Playthrough gate: compute "used" on the external-funded (deposit) basis | P1       | M      | —          | DONE (APPROVE; branch `advisor/026-playthrough-external-basis`, commit `711d9cea`. `used_cents` now sums `−external_funded_cents` over pack_open rows (deposit-funded spend only); withdrawable.ts docs updated; existing fixtures made external-basis-honest; new discriminating case "promo-funded play does not unlock a later deposit" proves the fix; qa-withdraw-gate.mjs seeds now write the basis. Executor proved red→green via Step 2; reviewer re-ran: greps, backend typecheck, wallet-summary 4/4, smoke 20/20 (first smoke re-run had 5 failures — adjudicated as Postgres contention from concurrent module-tier runs; clean re-run green). Executor caveats on record: QA script syntax-checked not executed; the new case was only ever run green (its red proof is Step 2's mechanism). Reversal coherence independently verified at service.ts:819/822 + 926/929)                                                                                             |
| 027  | CI: run backend unit + module suites, admin vitest; restore turbo cache | P1       | M      | —          | DONE (APPROVE after 1 approved amendment; branch `advisor/027-ci-test-tiers`, commits `92d8577e` + `487c7860`. New CI jobs `backend-unit` (no-DB: jest unit tier + turbo `test` = admin vitest + odds-math/pokemon jest) and `integration-modules` (58 suites, service containers), turbo cache restore in `backend-quality`. Amendment: `reward-draw.unit.spec.ts` was a mis-suffixed module-runner test that made the no-DB unit tier red — renamed to `reward-draw.spec.ts` (module tier owns it; zero coverage loss). Reviewer re-ran: YAML parse ok, post-rename unit tier 634/634 without DB, turbo test 5/5, executor baselines unit 93s / modules ~174s. NOTE: full CI proof still needs the first PR run — watch the two new jobs there)                                                                                                                                                                                                                                |
| 028  | Onboarding truth round 2: hook exemption, .env.example, README scrub    | P2       | S–M    | —          | DONE (APPROVE; branch `advisor/028-onboarding-truth-2`, commit `86d96eef`, 8 in-scope files; reviewer re-ran all greps + typecheck + lint + `playwright test --list` green; guard-secrets exemption applied to both machine-local hook copies (uncommittable — replicate on other machines); accepted deviations: launch-stack mention dropped in favor of the checkable gate, `.env` block-probe substituted for nonexistent `.env.local`, `NEXT_PUBLIC_MEDIA_HOST` added per the plan's own cross-check. **Plan 020 step 1 now resolved → mark 020 fully DONE.** Operator follow-ups: rotate the seeded admin password on any persistent stack (literal remains in git history); stale `/claw` comment at `tests/e2e/slot-vault-room.spec.ts:7` noted, untouched)                                                                                                                                                                                                              |
| 029  | from-PC route: cap money fields + resolve pixel id at add-time          | P2       | S      | —          | DONE (APPROVE; branch `advisor/029-from-pc-bounds`, commit `a7254229`. `capAtMost` helper caps market_value/price at `MAX_MARKET_VALUE_USD` and stock at 10,000; `resolvePixelPokemonPatch` at add-time maps NOT_FOUND→400. 4 new rejection cases; reviewer re-ran: greps, typecheck, product-from-pc 6/6, card-inherits-pc 2/2. Accepted judgment call: shared helper instead of three inline guards — same message shape as validate.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 030  | Dependency hygiene: phantom lodash pin + unused @base-ui/react          | P2       | S      | —          | DONE-partial (APPROVE, amended: **lodash half REJECTED — finding invalidated**, lodash 4.17.23/4.18.0/4.18.1 are real 2026 releases verified against registry.npmjs.org publish dates + lodash GitHub tags, pin/lock legitimate as-is; @base-ui half done: branch `advisor/030-dep-hygiene`, commit `a477a613`, reviewer re-ran `npm test` 200/200 + `npm run check` green; backend/ byte-identical to dbce0561)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 031  | Cross-flow ledger-conservation spec in the money smoke subset           | P2       | S–M    | —          | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 032  | /store/credits read diet + FX display cache + CDN redirect caching      | P3       | M      | —          | DONE (APPROVE; branch `advisor/032-credits-read-diet`, commits `c521b96e`+`cb71e1bb`+`312a2961`. Lean `GET /store/credits/balance` (auth-matched, 401-tested) now serves the header chip + vault page; 30s FX display cache with `clearFxDisplayCache()` seam, strict resolver provably uncached; CDN 302 carries `Cache-Control: public, max-age=86400`. Reviewer re-ran: pricing units 24/24, store-credits 5/5, typecheck, `npm test` + `npm run check` green. **Plan errata (executor caught, reviewer confirms):** the "0 matches" vault.ts grep criterion was wrong — `getTransactions` (vault.ts:227) legitimately needs the full route (transactions + totals), correct end state is 1 remaining match; and the cache exemplar lives in `store/profiles/[handle]/route.ts`, not service.ts. Documented tradeoff: a transient FX-read failure pins the fallback rate ≤30s (display only); executor also ran the FULL unit tier 640/640 to prove no cross-spec cache leak) |

Ordering notes: all independent, but run **027 early** — once merged it
auto-enforces 026's and 031's new specs in CI. **026 must land before any
cashout writer is built** (see DIR-01). 028 completes plan 020's blocked
step 1 (mark 020 fully DONE when 028 lands). 028 and 030 both edit
`README.md` (different lines: onboarding/structure vs. the tech-stack
`@base-ui` row) — merge in either order, re-check both greps after the
second. 026 is the only round-4 plan touching `service.ts`; no cross-plan
file conflicts otherwise. 031 is green before and after 026 by design.

### Round-4 findings table (vetted)

Ordered by leverage. Confidence is the advisor's after re-reading the code.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Category      | Impact                                           | Effort | Risk    | Confidence                     | Plan |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- | ------------------------------------------------ | ------ | ------- | ------------------------------ | ---- |
| 1   | Playthrough gate `used` = Σ raw `amount` over `pack_open` rows (`service.ts:2230`) instead of the deposit-funded `external_funded_cents` basis the rest of the money core uses (`creditSummary`, VIP). Promo/commission/buyback-funded play banks playthrough → a later deposit unlocks unplayed. Contradicts the gate's own header; cashout writer must route through it.                                                                                                                                                                                                                                                                                                                 | bug/security  | HIGH once cashout exists; MED now (display-only) | M      | MED     | HIGH mechanics / MED intent    | 026  |
| 2   | CI never executes `test:unit` / `test:integration:modules` (jest routes by `TEST_TYPE`; ci.yml only runs the HTTP tier) — ~90 specs incl. every money unit/module spec and both new wallet-gate specs are green-but-unenforced. Admin SPA `vitest` also never runs (no turbo `test` task). Turbo cache never restored (cold rebuild every backend PR).                                                                                                                                                                                                                                                                                                                                     | tests/dx      | HIGH                                             | M      | MED     | HIGH                           | 027  |
| 3   | Committed onboarding is broken on a fresh clone: README's provisioning step invokes `scripts/launch-stack.ps1` (git-excluded, `.git/info/exclude:46`); root `AGENTS.md`/`CLAUDE.md`/`docs/HANDOFF.md`/`docs/research/` referenced but gitignored; README flagship is the deleted `/claw`; `.env.example` still missing `NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY` (no code fallback → all /store 401). Root cause of the env-example blockage: `guard-secrets.js:30` regex blocks committed `.env.example`/`.env.template`. Plus: committed admin-password fallback in `tests/e2e/helpers/constants.ts:16` (env-required + rotation), Vite-boilerplate admin README, e2e README lists 3/9 specs. | dx/docs       | MED-HIGH                                         | S–M    | LOW     | HIGH                           | 028  |
| 4   | `from-pricecharting` route: `market_value`/`price`/`stock` have no upper bound (sibling card path caps at `MAX_MARKET_VALUE_USD`); `pixel_pokemon_id` guard checks non-emptiness, not resolvability — a bogus id degrades at card-registration to the exact spriteless card PR #135 targeted.                                                                                                                                                                                                                                                                                                                                                                                              | security/bug  | MED (admin-only trigger)                         | S      | LOW     | HIGH / MED                     | 029  |
| 5   | `backend/package.json:21` pins `lodash: ^4.18.1` — a version lodash never published; yarn.lock resolves 4.18.1 + 4.17.23 with no registry override. Reproducibility break + dependency-confusion surface. Also: `@base-ui/react` is a runtime dep with zero import sites; README still claims it as the UI foundation.                                                                                                                                                                                                                                                                                                                                                                     | deps/security | MED                                              | S      | LOW-MED | HIGH facts / MED non-existence | 030  |
| 6   | PR #138's sim deletion removed the only cross-flow money-conservation check (`ledger.mjs`); nothing now asserts `Σ(ledger) == balance` across a live topup→open→buyback flow. One integration spec in the plan-025 smoke subset restores the invariant — the safety net the future cashout writer needs.                                                                                                                                                                                                                                                                                                                                                                                   | tests         | MED (HIGH once cashout lands)                    | S–M    | LOW     | HIGH                           | 031  |
| 7   | `/store/credits` amplifies a one-number balance read into ~6 DB round-trips (two near-identical full-ledger scans + a discarded 50-row fetch), re-triggered after every money action by the header chip; `resolveFxRateInfo` hits the DB per call on ~14 uncached display routes; `/cdn/cards/[file]` 302 has no `Cache-Control`. Indexes all verified present.                                                                                                                                                                                                                                                                                                                            | perf          | MED                                              | M      | LOW-MED | HIGH                           | 032  |

### Round-4 findings vetted but NOT planned (ask for a plan if wanted)

- **`WalletSchema` hard-requires the two new wallet fields**
  (`schemas.ts:248-253`): an older-backend response would fail the whole
  wallet page instead of degrading (the module's own `OddsEntrySchema`
  documents the graceful convention). Cannot fire in the current
  lockstep deployment — do opportunistically when next touching schemas,
  together with adding `WalletSchema` to `schemas.test.ts` (it's the one
  schema the contract test doesn't exercise). S.
- **Per-PR E2E smoke** — the money-loop E2E gates nightly only
  (`e2e.yml`: schedule + dispatch, a documented ~15-25-min-boot tradeoff).
  A 1-spec per-PR smoke job is possible but flake-prone; maintainer call.
- **Real-timer sleeps** (`pack-open-rate-limit.spec.ts:140` sleeps 15.5s in
  CI; `password-reset.spec.ts` fixed waits) — round-3 backlog, still open.
  S–M (make the rate-limit window env-configurable in test mode).
- **E2E seed-presence guard** — 8 specs hardcode `pokemon-rookie`/
  `pokemon-elite`; CI reseeds fresh, but local runs fail opaquely if the
  shared dev DB drifts (it happened once). S nicety: fail-fast preflight
  in `tests/e2e/helpers/`.
- **Admin SPA `src/lib` churn helpers still untested** (`admin-rest.ts`,
  `queries.ts`, `image-validation.ts`) — round-3 backlog; `format`/
  `odds-rows`/`query-keys` gained specs, these didn't. Worth more once 027
  makes admin tests run in CI. S.
- **`walletSummary`/`creditSummary` overlapping-scan merge + serial-await
  parallelization** — deferred inside plan 032 (needs MikroORM
  shared-manager concurrency verification first; the lean endpoint removes
  the hot-path pressure).
- **e2e.yml's own ephemeral admin-password literal** (lines ~82-84) — kept
  as the documented CI-only tradeoff (DB dies with the job); wiring a
  `PW_ADMIN_PASSWORD` repo secret would make the literal path dead.
  Operator action.
- **Operator reminders**: rotate the PriceCharting API token
  (`docs/superpowers/PRICECHARTING-DEPLOY-NOTES.md` self-documents it as
  compromised); rotate the seeded admin password on any persistent stack
  once plan 028 removes the committed fallback.

### Round-4 verified clean / considered and rejected

Recorded so nobody re-audits. Spot-confirmed by the advisor where table-bound.

- **Lodash "phantom versions" — REFUTED during execution (2026-07-13).** The
  round-4 table's finding 5 (lodash half) claimed 4.18.1/4.17.23 don't exist
  on public npm. Execution-time network verification proved otherwise:
  registry.npmjs.org reports genuine publish dates (4.17.23 → 2026-01-21,
  4.18.0 → 2026-03-31, 4.18.1 → 2026-04-01) and the lodash GitHub repo
  carries matching release tags. The audit premise was stale
  training-data knowledge (it was honestly hedged as "MED on non-existence,
  not network-verified"). The `^4.18.1` pin and lockfile are legitimate —
  do not re-file, do not "fix" the pin.
- **Playthrough gate mechanics** (aside from the basis finding):
  `withdrawable ≤ available ≤ balance` holds in all branches; SQL cent math
  matches the `creditSummary` pattern; `/store/credits` is bearer-auth,
  actor-scoped; reason enum consistent across model/service/storefront/DB
  CHECK; reversal mirror rows restore the external basis
  (`external_funded_cents: -originalExt`). The `deposited` filter's
  no-negative-topup asymmetry is latent-only (no topup-reversal path
  exists) — documented in plan 026's maintenance notes.
- **Insufficient-credit copy (#136)**: integer-cent-correct; the live
  charge site (`settleOpen`) reports locked-aware `available`. Clean.
- **Address guard (#139)**: all bypass classes traced (empty/whitespace/
  null/missing/non-string; update-mode blanking rejected); unit spec locks
  them. Zero-width-char postal code is a data-quality edge, not a bypass.
- **Delivery-orders route**: batch cap before per-element scan; duplicate
  pull_ids handled downstream; ownership/status/frozen gates precede writes.
- **middlewares.ts delta**: purely additive since #127 (the plan-010
  metadata guard); no matcher loosened; from-PC route consistent with
  sibling CRUD routes in carrying no rate limit (only money-mutation routes
  do).
- **Merge coherence of plans 009–025 in master**: every caller resolves to
  live symbols (pulls pagination, profile aggregates + cache seam,
  per-beneficiary `matureDueCommissions`, bake-slab SSRF denylist). No
  half-applied merges.
- **Storefront honesty-cleanup regressions**: none — no dead imports,
  no dangling links (`/clawmaker` fully gone), no missed fabricated claims;
  demo surfaces all disclosed; wallet page loading/error/staleness paths
  clean (`router.refresh()` after top-up).
- **Perf negatives**: no hot-path N+1 (delivery/vault/leaderboard batched);
  service.ts per-row loops are cold-path bounded; admin TanStack has no
  refetch storm (Mercur's 90s default staleTime); storefront bundle lean,
  `next/image` consistent; the abandoned `perf/p1-frames-listcache`
  worktree was right to be abandoned (single-row read).
- **CI workflow hygiene**: SHA-pinned actions, least-privilege permissions,
  no `continue-on-error`, gitleaks full-history scan, correct path filters.
- **Sim removal (#138)**: clean — zero dangling references in tracked files
  (`seed-sim-daily-box.ts` was deleted too); only the historical `plans/`
  record still names sim files, which is expected.
- **Dead code**: none found beyond `@base-ui/react` (plan 030);
  `npm audit --omit=dev` at root: 0 critical/high (2 known moderates that
  clear on the next Next bump).
- **`service.ts` growth** (4,237 → 4,424 lines): proportional feature work,
  not new architectural decay; the god-object refactor stays a deliberately
  scheduled deferred item from round 3.
- **QA script `qa-withdraw-gate.mjs`**: logic correct, guarded to
  `wdtest-*` customers on local docker only. (Plan 026 may touch its seeds
  for the basis change.)

### Round-4 Direction (updates to round-3's options + one new)

1. **DIR-01 — Close the cash-out loop (round-3 option 2, now concrete and
   urgent-adjacent).** PR #140 shipped the read-model 20%: gate, `cashout`
   ledger reason (written by nothing), `walletSummary.withdrawable`, and a
   user-facing "eligible for withdrawal" number on the wallet page — with
   no payout action anywhere. The spike now has a concrete contract: a
   cashout writer that recomputes the gate **under the per-customer
   `credit:` lock**, caps at `available`, writes the `cashout` row, plus an
   admin approval surface and the PSP/KYC/AMLA long pole. Cheapest interim
   move if the spike slips: soften the wallet-page copy so "eligible for
   withdrawal" doesn't promise an imminent payout. L, HIGH risk. **Do plan
   026 first regardless** — shipping cashout on the current basis ships the
   hole.
2. **DIR-03 — Disclose the 100%-playthrough requirement at deposit time.**
   Enforced backend-side, surfaced only post-hoc on the wallet page; the
   top-up flow says nothing. A material term for a money product and a
   prerequisite disclosure for DIR-01's compliance story. S (copy + the
   `playthrough` fields already returned); product/jurisdiction call, so
   left as direction.
3. **Round-3 option statuses**: P2P trading (opt 1) — grounding corrected:
   `PRODUCT.md` never existed (round-3 cite was phantom); the marketplace
   promises are now feature-flag-gated off by plan 024, so P2P is a clean
   deferred bet sequenced after cash-out. Rewards launch (opt 3) —
   unchanged. Sim-as-nightly-gate (opt 4) — **dead**: the maintainer
   deleted the harness (#138); the salvageable 20% became plan 031. Mock-
   route triage (opt 5) — urgency resolved by plan 024's disclosures;
   drop from the roadmap or fold into the trading track.

### Round-4 coverage — what was NOT audited

- **No runtime exercise** — every verdict is a static code-read (same
  caveat as rounds 2–3); plan verification commands are the executors'
  runtime proof.
- `backend/packages/api/.env.template` content — unverifiable until plan
  028's hook exemption lands (guard-secrets blocked all agents, correctly
  per its current regex).
- `backend/apps/vendor` — dormant, skimmed only (its lack of a `test`
  script is asserted in plan 027's STOP conditions, not deeply audited).
- Admin SPA behavior — static only, no browser walk.
- Vendored/framework internals — read only where a finding required it.
- i18n depth, Windows-specific tooling paths, `.claude/` hook scripts
  beyond `guard-secrets.js` (read as evidence for finding 3).

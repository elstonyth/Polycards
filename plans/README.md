# Implementation Plans — Full-Repo Bug Hunt (Polycards)

Generated by the `improve` skill on 2026-07-02, against commit `4ca2593`.
Scope: every function / feature / button / route across the **storefront**
(`src/`), the **Medusa backend** (`backend/packages/api`), and the **admin
dashboard** (`backend/apps/admin`). Method: seven parallel read-only audits
(money paths, gameplay/fulfillment, admin API + platform, cross-cutting
security, storefront account/money UI, storefront gameplay/marketing UI, admin
dashboard app), then every surviving finding re-opened and vetted against the
live code by the advisor.

**Payment gateway is deliberately excluded** — top-up runs through the mock
gateway (`ALLOW_MOCK_TOPUP` / `unsafe-demo` prod opt-in) and reward payout is
dormant until a real PSP + AMLA compliance land. Those are decided tradeoffs,
not bugs. Implementation bugs _inside_ those guards were still in scope.

> **⚠️ THE EXCLUSION ABOVE IS VOID AS OF ROUND 10 (2026-08-07).** It applied to
> rounds 1–9 only. PR #252 landed the real GlobePay365 gateway, #376 armed
> production withdrawals, and #328 added SMS phone verification — all of it
> live, none of it covered by rounds 1–9. Round 10 audited that surface for
> security; see the Round 10 section at the end of this file. Treat **every**
> pre-round-10 rejection as scoped to the mock gateway, and re-read it with the
> real PSP in mind before relying on it.

Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done. **Do not fix anything not covered by a plan
without checking the "considered and rejected" list first** — several
plausible-looking issues were investigated and found to be by-design.

---

## Headline

The money core is in good shape. Per-customer advisory locks, an append-only
ledger with a DB-level unique index on `pull_id`, idempotent workflow steps,
frozen-account gates, and commission decay all held up under scrutiny. **No
confirmed critical vulnerability and no confirmed money-loss bug were found.**
What remains is one high-value _verification_ (admin FX route auth), one
real UX-severity bug (a charged user can get stuck on the spin screen), and a
tail of low-severity validation/hardening items.

---

## Execution order & status

| Plan | Title                                                          | Priority | Effort | Depends on | Status                                                                                                                           |
| ---- | -------------------------------------------------------------- | -------- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 001  | Verify + harden admin FX-rate route auth                       | P1       | S      | —          | DONE                                                                                                                             |
| 002  | Watchdog for charged-but-stuck pack/slot open                  | P1       | M      | —          | DONE (claw deferred — different state machine; behavioral proof by-inspection + smoke)                                           |
| 003  | AuthProvider: don't log users out on transient network errors  | P2       | S      | —          | DONE                                                                                                                             |
| 004  | Close admin money-input validation gaps                        | P2       | S      | —          | DONE                                                                                                                             |
| 005  | Regression guard: storefront credit-reason enum ⊇ backend enum | P2       | S      | —          | DONE                                                                                                                             |
| 006  | Resolve Zod-JIT vs CSP `unsafe-eval` before enforcing CSP      | P2       | M      | —          | DONE (Option 1 — `z.config({ jitless: true })` already present in schemas.ts; verified via zod source + tests; no code change)   |
| 007  | Triage & upgrade path for dependency CVEs (npm audit)          | P3       | L      | —          | DONE (root 15→5 via in-range fixes + next 16.2.10 patch; backend + rest framework-blocked, see 007-audit-report.md)              |
| 008  | Low-severity backend hardening bundle                          | P3       | M      | —          | DONE (A & D already-safe/verdict, no change; B/C/E/F applied; new pagination-bounds spec + external-funded/delivery specs green) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

## Dependency notes

- All plans are independent and can be executed in any order. The numbering is
  by leverage (impact ÷ effort ÷ risk), not by dependency.
- 001 is first because it is a _verification_ with a potentially critical
  payoff and a tiny fix; run it before anything else so the answer is known.

---

## Round 2 — full-history review (PR #1 → current), 2026-07-12

Generated by the `improve` skill against commit `e9ce6968` (branch
`feat/virtual-shop`). Scope: the delta since round 1's `4ca2593` (131 commits /
~758 files — the sim harness, achievements, avatar frames, media pipeline,
delivery cancel + serialization, VIP settle, admin-dashboard churn), plus a
full re-verification of the month-long simulation's 15 findings against the live
code. Method: four parallel read-only audits (backend money core, backend
feature delta, admin dashboard, storefront), every surviving finding re-opened
and confirmed by the advisor.

**Method caveat — this is a static code-read audit, not a runtime pass.** No
flow was exercised live this session (no fresh buy→open→sell→deliver→cancel run).
"Fixed in code" means the advisor read the current source and confirmed the fix
is present; it rests on that read plus the _prior_ simulation run, not on a new
end-to-end exercise. The headline below is "what the code-read found," not
"verified working in a running system." Before shipping, run the flows (or the
integration suites named in each plan) to confirm behavior.

**Headline.** The commerce core held up under the read. **11 of the 15
simulation findings are confirmed fixed in the current code** (idempotent-topup
visibility, customer cancel route + concurrency serialization, VIP batch-open
grant settlement, FX quote firmness on the sell path, four error-copy fixes),
and the showcase-sell worry is a non-issue (read paths gate on vaulted status).
Every credit-writing path in the delta routes through the advisory-locked
mutation. **No money-loss bug and no critical vulnerability were found by the
code-read.** What remains is a tail of correctness/UX bugs, two
security-hardening items, and one test gap — all S/M effort.

The single item with real money exposure is **009** (unbounded card markup on the
edit path); the one security item with a near-anonymous trigger is **010**
(customer-metadata mass-assignment on account create).

| Plan | Title                                                                                                                                                                  | Priority | Effort | Depends on      | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 009  | Bound card markup multiplier (edit path + backend validator)                                                                                                           | P1       | S      | —               | DONE (worktree `agent-a89f05e9ae07dca65`, commit `eaaf26ba`; ceiling 11 = 1000% under `1+pct/100`; +3 unit cases green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                                      |
| 010  | Reject client metadata on customer **create** (frame/avatar/handle bypass)                                                                                             | P1       | S      | —               | DONE (worktree `agent-aee05db63bd98d23d`, commit `b3e99c34`; matcher exact-match confirmed non-shadowing, live spec 2/2 green, register call traced metadata-free, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                           |
| 011  | Invalidate balance + tx views after a commission reversal                                                                                                              | P2       | S      | —               | DONE (worktree `agent-a498188cd73049dca`, commit `dc80b6d6`; mirrors `useFreezeCustomer` pattern, build + 25 tests green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                                    |
| 012  | Fix blank pack titles in admin pull ledger (join by slug, not id)                                                                                                      | P2       | S      | —               | DONE (worktree `agent-a6381dddc80920f79`, commit `ef638d82`; new `admin-pulls.spec.ts` proven red→green on a live DB, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                                        |
| 013  | Paginate Customer360 commissions + audit tables                                                                                                                        | P2       | M      | —               | DONE (worktree `agent-abe87016382d23af1`, commit `93373b76`; Pager mirrors support page, reset-on-id guard added, `queries.ts` untouched so no 011 conflict, build + 25 tests green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                         |
| 014  | Fix daily-credit header balance desync (re-read, don't set prize)                                                                                                      | P2       | S      | —               | DONE (worktree `agent-ab288a78fefe02350`, commit `398c69f9`; `void refreshBalance()` swap, typecheck/lint/200 tests green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                                   |
| 015  | Close remaining admin money-input gaps (zero-adjust, daily-box qty)                                                                                                    | P2       | S      | —               | DONE (worktree `agent-a95c58cfd69e44d4a`, commit `d9d89dc9`; qty ceiling verified against backend `daily-box.ts` = exactly 1, build + 25 tests green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                        |
| 016  | Put price/balance/shortfall in pack-open "Not enough credits"                                                                                                          | P3       | S      | —               | DONE-with-caveat (worktree `agent-aa109382e7a5ae42d`, commit `c5817c60`; backend both throw sites interpolate price/balance/shortfall, live spec green, reviewer-verified. **CAVEAT:** storefront `packs.ts:104` `friendlyError` rewrites the message to a generic string + shows a top-up CTA (`needsTopUp`), so numbers reach direct API clients but NOT the storefront user. Surfacing numbers on the storefront is a product decision — see follow-up note. **merged into `feat/virtual-shop` 2026-07-12**) |
| 017  | Extract + test the voucher-ladder fold logic                                                                                                                           | P3       | S–M    | —               | DONE (worktree `agent-ae6f4f5820a4e8b46`, commit `96a68df2`; verbatim extraction to `voucher-ranges.ts`, +12 tests (37 total), build green, reviewer-verified; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                                                                                                                  |
| 018  | Backend hardening: delivery batch cap + slab-bake egress allowlist                                                                                                     | P3       | M      | —               | DONE (worktree `agent-a781a18d54d4517b9`, commits `1a3a1263` + `714d122d`; delivery cap HTTP-tested, SSRF denylist 23 unit tests green incl. IPv4-mapped-IPv6 bypass closed in review round 1, reviewer-verified; DNS-rebind residual documented as follow-up; **merged into `feat/virtual-shop` 2026-07-12**)                                                                                                                                                                                                  |
| 016b | Storefront: show top-up shortfall in insufficient-credit CTA (resolves 016 caveat, client-side) — _renumbered from 019 to avoid colliding with the round-3 plan files_ | P3       | S      | complements 016 | DONE (worktree `agent-af003f483a9e370a9`, commit `da71528a`; both CTAs prepend `You're {rm(shortfall)} short.` from client-side balance+price, `balance===null` falls back to prior copy, `friendlyError`/`packs.ts` untouched, typecheck/lint/200 tests green, reviewer-verified. Incidental: prettier reformatted the `Phase` type union in SlotMachineClient — cosmetic, matches existing WIP. **merged into `feat/virtual-shop` 2026-07-12**)                                                               |

Round-2 plans are independent and can run in any order; numbering is by leverage.
Recommended first three: **009, 010, 012** (highest impact ÷ effort — a money
bug, a security bypass, and a one-line-logic ledger regression).

### Round-2 findings considered and rejected / verified-fixed

Recorded so nobody re-audits them. Each was opened and confirmed by the advisor.

- **Showcase-sell guard** (sim #7, `buyback-pull.ts`): no guard exists, but it's
  harmless — a sold-back pull keeps `showcased=true`, yet both readers (vault
  list, public profile) gate on `status==='vaulted'`, so a sold card never
  appears publicly. Not a bug.
- **11 simulation findings verified fixed in code** — idempotent-topup `replayed`
  flag (#2, PR #132), daily-draw "nothing" message (#3), delivery cancel route +
  concurrency serialization (#4/#8/#12, PR #131/#133), FX quote-vs-execute
  firmness on the sell path (#5, PR #129), topup keyless-400 copy (#1), address
  missing-fields naming (#10), shared rate-limiter per-path labels (#11, P3-10),
  double-submit delivery copy (#9, PR #130), VIP batch-open level-up settle
  in-saga (#13, PR #134). Re-confirmed present; do not re-file.
- **Admin repair for pre-existing double-ship rows** (sim #2/#15): PR #133's
  per-order advisory-locked transitions prevent _new_ one-pull/two-orders
  corruption. No admin force-void route exists for rows corrupted _before_ the
  fix — but that's an operational data-repair task, not a code defect. Surfaced
  as a Direction option, not a plan.
- **Dependency posture**: `npm audit --omit=dev` (storefront) is down to 2
  moderate advisories (postcss bundled under `next`, no in-range fix — clears on
  the next Next bump). Nothing actionable beyond round-1 plan 007's Medusa-bump
  recommendation for the backend.
- **Sim #6 admin buyback-dispute — half fixed.** The _view_ half is done: PR #129
  (`5c9f6f2e`) added quote-vs-payable data to `/admin/customers/[id]/pulls`
  (`route.ts:19-64`) and the support-desk display. The _honor-a-single-quoted-
  sell-without-moving-global-FX_ half is still absent — no such admin action
  exists (`grep` for honor/settle-buyback under `api/admin` is empty; the only
  admin credit action is the raw `credits` adjust). An operator _can_ resolve a
  dispute today via that manual credit-adjust, so no admin function is broken —
  it's a missing capability with a workaround. Surfaced as a Direction option
  below, not filed as a plan.

### Round-2 coverage — what was NOT audited

Honest scope limits so the "everything works" bar isn't overstated. This was a
delta-focused **static** review; the following were not covered and are the
first places to look if a flow misbehaves:

- **No runtime exercise** — see the method caveat above. Every "fixed" verdict is
  a code-read, not a live buy/open/sell/deliver/cancel run.
- **Backend, unread or skimmed**: `admin/customers/[id]` sub-route _backend_
  contracts (audit/commissions/gacha/pulls/transactions beyond the money-write
  core), the reward-box workflow internals (`draw-reward-box.ts` /
  `settleRewardDraw`), and referral/commission internals (`reverseOpen`, override
  schedule, `matureDueCommissions`). `admin/pricing/fx/history` + `/health`
  routes unread (the main FX POST/GET was read).
- **Storefront, skimmed only**: social/browse surfaces — `profile/[user]`,
  `leaderboard`, `invite/[handle]`, `pack-party`, `repacks`, `daily/JoinPrompt`,
  and `PrizeReveal` overlay a11y. Commerce/account surfaces were swept fully.
- **Admin SPA**: not every route's zod/coercion contract was cross-checked
  against its client payload; `eslint`/`eslint.config.js` DX was out of scope.
- **Security depth**: the two security findings (010 metadata, 018 slab-bake
  SSRF) were confirmed by static reads of route + vendored handler, not by a live
  request. `/security-review` on the eventual diffs is still worth running.
- **Round-1 "considered and rejected" items** were not re-audited (trusted as
  settled).

### Round-2 Direction (options for the maintainer, not bugs)

- **Admin delivery-order repair tooling.** New corruption is prevented (PR #133),
  but no admin force-void/detach route exists to repair rows corrupted before it.
  Worth a small spike _only if_ such rows exist in prod — otherwise skip.
- **Admin "honor a single quoted sell" action** (sim #6, honor-half). The desk can
  now _see_ a disputed buyback quote-vs-payable (PR #129) but can only settle it
  by a raw credit-adjust. A dedicated action that pays out one pull at its shown
  quote without moving global FX would close the dispute loop cleanly. A
  design/spike-sized decision (what FX rate to honor, audit trail, idempotency),
  not a bug — ask for a plan if you want it built.
- **Centralize client↔backend validation contracts.** Findings 009, 011, 014,
  015 are all one shape: a guard added to one caller/modal but missed on a
  sibling, or a client `max` the backend doesn't enforce. A shared validation
  module (or backend-as-source-of-truth codegen) would kill this whole drift
  class — the same recommendation round 1 made for the enum-mirror class (plan
  005). Consider it if a third instance appears.

### Round-2 execution rollup (2026-07-12) — all 10 plans implemented in isolated worktrees, reviewer-verified, merged into `feat/virtual-shop`

Each plan was executed by a dispatched subagent in its own git worktree (branched
from `e9ce6968`), then reviewed by the advisor (scope + diff + re-run criteria).
**All 10 worktree branches were merged into `feat/virtual-shop` on 2026-07-12**
(per-plan rows above carry the merge stamps). Verification ran live where a DB was needed (`pokenic-postgres` +
`.env.test` were reachable from worktrees).

| Plan | Verdict              | Worktree branch                    | Commit(s)               |
| ---- | -------------------- | ---------------------------------- | ----------------------- |
| 009  | APPROVE              | `worktree-agent-a89f05e9ae07dca65` | `eaaf26ba`              |
| 010  | APPROVE              | `worktree-agent-aee05db63bd98d23d` | `b3e99c34`              |
| 011  | APPROVE              | `worktree-agent-a498188cd73049dca` | `dc80b6d6`              |
| 012  | APPROVE              | `worktree-agent-a6381dddc80920f79` | `ef638d82`              |
| 013  | APPROVE              | `worktree-agent-abe87016382d23af1` | `93373b76`              |
| 014  | APPROVE              | `worktree-agent-ab288a78fefe02350` | `398c69f9`              |
| 015  | APPROVE              | `worktree-agent-a95c58cfd69e44d4a` | `d9d89dc9`              |
| 016  | APPROVE (caveat)     | `worktree-agent-aa109382e7a5ae42d` | `c5817c60`              |
| 017  | APPROVE              | `worktree-agent-ae6f4f5820a4e8b46` | `96a68df2`              |
| 018  | APPROVE (1 revision) | `worktree-agent-a781a18d54d4517b9` | `1a3a1263` + `714d122d` |

> Note: the 010/012/013/014/015/016/017/018 worktrees were created **nested**
> under the 009 worktree (`…/agent-a89f05e9ae07dca65/.claude/worktrees/…`) — a
> harness path quirk; the git branches/commits are valid regardless. `git
worktree list` shows the true paths.

**Merge-order guidance** (overlapping files edit different regions → git 3-way
should auto-merge, but merge in this order and re-run the admin build after each):

- `backend/apps/admin/src/lib/queries.ts` — 011 (adds invalidations to
  `useReverseCommission`). 013 left this file untouched, so **no 011↔013
  conflict**.
- `backend/apps/admin/src/routes/customers/[id]/page.tsx` — 013 (pagination
  state + pagers) and 015 (zero-adjust guard) touch different regions. Merge
  **013 before 015**; the guard hunk sits far from the pager hunks.
- `backend/apps/admin/src/routes/daily-rewards/page.tsx` — 017 (extracts
  `foldRangesLocal`/`summarizeLevels`/`LEVELS` into `voucher-ranges.ts`, deletes
  them from the page) and 015 (edits `rowErrors` qty guard). Merge **017 before
  015** so 015's `rowErrors` hunk applies against the post-extraction file; the
  two hunks are ~470 lines apart.
- All backend/storefront plans (009 validate.ts, 010 middlewares, 012 pulls
  route, 014 DailyClient, 016 service.ts, 018 delivery+bake-slab) touch disjoint
  files — no cross-plan conflicts.

**Two follow-ups the maintainer should decide (documented, not auto-built):**

1. **016 storefront copy — RESOLVED by plan 016b** (renumbered from 019). The backend (016) names
   price/balance/shortfall for API clients; the storefront `openPack` action
   deliberately maps `/not enough credits/i` through `friendlyError`
   (`src/lib/actions/packs.ts:104`) to a generic string + a `needsTopUp` CTA, so
   the raw numbers never reach the storefront. Rather than loosen that
   sanitization, **plan 016b** shows the shortfall client-side (computed from the
   `balance` + pack price already in scope) next to the existing top-up CTA —
   delivering the plan-016 intent on the storefront with zero backend coupling.
   Both are DONE and merged into `feat/virtual-shop`. No open decision remains.
2. **018 DNS-rebind residual.** The slab-bake SSRF guard blocks IP literals
   (incl. obfuscated IPv4 and IPv4-mapped IPv6) and known internal hostnames, but
   does not DNS-resolve — a public hostname resolving to a private IP is a
   documented follow-up (add a resolve-then-check guard if this fetch ever takes
   less-trusted input). Low priority: the trigger is admin-only.

**Recommended pre-merge gate:** run `/security-review` on the 010 and 018 diffs
(auth/SSRF surfaces), and run each plan's named integration/unit suite once in
your main tree before merging (the worktree runs used `.env.test`; your prod
config may differ).

---

## Full findings table (vetted)

Ordered by leverage. "Confidence" is the advisor's after re-reading the code.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Category      | Impact                      | Effort | Risk | Confidence                 | Plan |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------------------------- | ------ | ---- | -------------------------- | ---- |
| 1   | Admin FX-rate route (`admin/pricing/fx/route.ts`) is typed `MedusaRequest` (not `AuthenticatedMedusaRequest`) and is absent from `middlewares.ts`. Needs runtime proof that Medusa's default `/admin` auth actually gates it. If it doesn't, an unauthenticated actor could set the global USD→MYR rate and skew every displayed price.                                                                                                                         | security      | HIGH _if_ unprotected       | S      | LOW  | MED (runtime check needed) | 001  |
| 2   | Pack/slot open deducts credit server-side, then relies on the reel's `transitionend` settle callback to release the UI and reveal the win. `handleSpin` has no watchdog/timeout and no try/catch around the post-charge mapping — any missed settle or thrown mapping strands a **charged** user in `phase='spinning'` with no recovery but a page refresh.                                                                                                     | bug           | MED-HIGH                    | M      | MED  | MED                        | 002  |
| 3   | `AuthProvider.refresh()` catches _all_ `/api/me` errors and calls `setCustomer(null)` — a transient network blip or 5xx logs the user out until they refresh.                                                                                                                                                                                                                                                                                                   | bug           | MED                         | S      | LOW  | HIGH                       | 003  |
| 4   | Admin money inputs under-validate: zero-amount credit adjust is accepted (`support/page.tsx`); PriceCharting `canSave` allows `stock === 0`; markup % has no upper bound; customer-search query length is unbounded.                                                                                                                                                                                                                                            | bug           | LOW                         | S      | LOW  | HIGH                       | 004  |
| 5   | `parseList()` silently drops ledger rows whose `reason` isn't in the storefront `CREDIT_REASONS`. Backend and storefront enums are **currently identical** (10 values), so no live data loss — but there is no test locking that invariant, and it has drifted before (PR #36).                                                                                                                                                                                 | tests/dx      | MED (latent)                | S      | LOW  | HIGH                       | 005  |
| 6   | Zod 4's JIT compiles schemas with `new Function()`; the storefront CSP `script-src` has no `unsafe-eval`. Flipping `CSP_ENFORCE=true` in prod (an explicit planned step) would break or spam-report every client-side validation.                                                                                                                                                                                                                               | security      | MED                         | M      | MED  | HIGH                       | 006  |
| 7   | `npm audit` reports high-severity advisories in Hono, Next.js, esbuild/Vite, and fast-uri. Most are transitive under the Medusa/Next framework and need coordinated upstream bumps, not local patches; reachability varies.                                                                                                                                                                                                                                     | security      | HIGH sev / MED reachability | L      | MED  | HIGH (audit)               | 007  |
| 8   | Low-severity backend hardening: no `(0,100)` bound on `teamOverridePct` at settings-save; no sign assert on `pack_open` external-funded cents; silent `take:1000` truncation in eligible-products and delivery-item queries; leaderboard points column type (BIGINT?) unverified; admin audit/commissions accept unbounded `limit`/`offset` at the route boundary (service clamps, so no live DoS); admin cards `GET` 500s the whole page if the FX read fails. | bug/hardening | LOW-MED                     | M      | LOW  | MED                        | 008  |

---

## Findings considered and rejected

Recorded so nobody re-audits them. Each was opened and confirmed by the advisor.

- **Weighted-pick last-row bias** (`modules/packs/pick.ts`): the `roll >= totalWeight` fallback to the last row is standard weighted-selection and documented as an accepted float-drift edge; bias is asymptotically zero. Not a bug.
- **Stock decrement race / negative stock** (`workflows/steps/decrement-card-stock.ts`): explicitly by-design — "STOCK IS A COUNTER, NOT A GATE." The adjust is guarded by `target.stocked > 0`, and the one-unit drift is documented as acceptable. Not a bug.
- **Notification mark-read TOCTOU / duplicate rows** (`store/notifications/[id]/read`): a partial unique index on `(notification_id, customer_id) WHERE deleted_at IS NULL` already exists (`models/notification-read.ts`); the retry-on-conflict re-fetch is correct. Only a rare double-miss re-throw remains (cosmetic).
- **CSRF on state-changing store POSTs** (open, buyback, topup, draw, delivery, referral): customer auth is **bearer-only** — the backend issues no customer session cookie, so there is no ambient-credential CSRF vector. Documented in `middlewares.ts:32-44`. Not a bug.
- **Admin double-submit on money buttons**: `@medusajs/ui` `Button` with `isLoading` renders as `disabled`, and the handlers (`requestAdjust`/`applyAdjust`) early-return while `isPending`. Guarded.
- **Slot `settled` ref "never cleared"** (`SlotReelColumn.tsx`): the main effect sets `settled.current = false` on its first line, and the column remounts per spin (keyed by `spinKey`). Reset is correct.
- **Slot `onSettled` stale closure** (`SlotReelStack.tsx`): `onSettledRef` is refreshed every render via an effect; the callback always reads the current value. Correct pattern.
- **`availableBalance` staleness / SHA-256 idempotency collision / soft-delete replay**: all defended by the per-customer advisory lock in `mutateCreditAtomic` and existing idempotency tests. Not bugs.
- **Media upload path traversal** (`admin/media/route.ts`): `path.basename()` after `\`→`/` normalization is sufficient; multer memory storage + `validateImage` (mime/magic-byte/size/dims) gate the rest.
- **"Verify topup/draw rate limit + CSRF exists"** (raised by the money-path audit): confirmed present in `middlewares.ts` — `createCreditTopupRateLimit()` on `/store/credits/topup`, `deliveryWriteRateLimit` on `/store/rewards/*`, all after `authenticate('customer', ['bearer'])`.

---

## Direction (not bugs — for the maintainer, low volume by request)

The user asked for bugs, so this is deliberately short:

- **Pagination is capped, not paginated** on several read surfaces (recent-pulls
  hard-capped at 12, vault at 500, no `skip`/`limit` params). Fine today; will
  bite the first user with a large vault or an "infinite scroll" feed. Covered
  as a note in plan 008, not built out.
- **A backend↔storefront enum contract check** (plan 005) is the first instance
  of a general class: several shared Zod schemas mirror backend enums by hand.
  A tiny codegen or shared-constants module would kill this whole drift class —
  worth considering if it recurs.

---

## Round 3 — deep full-repo audit (the areas rounds 1–2 didn't cover), 2026-07-12

Generated by the `improve` skill (deep tier) against commit `e9ce6968`
(branch `feat/virtual-shop`; working tree dirty at planning time — small
uncommitted changes to the from-pricecharting flow, `SlotMachineClient.tsx`,
sim scripts, `package.json`). Method: eight parallel read-only audits —
backend money internals (the round-2-unread commission/VIP/reward-box/jobs
code), backend admin-API contracts + middleware coverage, storefront
social/browse + unlisted routes, performance (first dedicated pass), tech
debt & architecture (first pass), test coverage (first pass), deps/DX/docs,
and direction. Every table-bound finding was re-opened and confirmed by the
advisor against the live code. Same caveat as round 2: **static code-read, no
runtime exercise.**

Run constraint honored: several agent worktrees were active during this run
(plans 009–018 executors, `perf/p1-frames-listcache`, `polycards-rebrand`,
sim branches). Round-3 plans avoid every file those touch; where a plan edits
`service.ts` (shared with TODO plan 011) the plan carries an explicit
rebase/drift instruction. Frames-list caching was excluded from the perf
audit entirely (in progress in a worktree).

**Headline.** The core held up again — and so did the layers above it. The
admin-API audit came back _clean_ (middleware coverage map verified against
Medusa framework source and Express 4 path-to-regexp wildcard semantics; every
admin/store write validates; public feeds mask PII). The money internals
audit's only finding is an availability issue, not a correctness one. The
real round-3 story is operational and trust debt: **CI never type-checks the
largest backend package; the flagship E2E money-loop spec has been disabled
since 2026-07-07; the hourly commission job holds every beneficiary's credit
lock in one transaction; the public profile route loads 20k rows per view;
and several publicly reachable pages present fabricated data — including
"verified" badges on real companies — as live product.**

### Round-3 plans (written per the skill's non-interactive default: top items by leverage)

| Plan | Title                                                                 | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---- | --------------------------------------------------------------------- | -------- | ------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 019  | Make CI type-check `backend/packages/api`                             | P1       | S      | —          | DONE (advisor-executed 2026-07-12; script pins `node node_modules/typescript/bin/tsc` because the api-local `.bin/tsc` shim mis-resolves under `yarn run` (3,585 phantom errors); direct binary + turbo `@acme/api:check-types` verified green)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 020  | Onboarding truthfulness: `.env.example`, README DB step, compose name | P2       | S      | —          | DONE — steps 2–4 done by advisor 2026-07-12; step 1 resolved by **plan 028** (2026-07-13): guard-secrets now exempts `*.env.example`/`*.env.template`, and `.env.example` carries the Medusa vars. Round-4 note: 020's README pointer at `scripts/launch-stack.ps1` turned out to be git-excluded (local-only) — corrected by 028's inline docker commands                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 021  | Commission-maturity job: one transaction per beneficiary              | P2       | M      | —          | DONE (worktree `agent-ab85c6936518ad34d`, commit `9304b98d`; enumerator `@InjectManager` + private `@InjectTransactionManager` per-beneficiary flip, notify after commit best-effort; new `mature-commissions.spec.ts` 4/4 green + neighbor suites green, advisor-reviewed; merged into `feat/virtual-shop` 2026-07-12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 022  | Public profile route: SQL aggregates + 30s cache                      | P2       | M      | —          | DONE (worktree `agent-a312d2d53d7da0cd9`, commit `f4f5a40a`; `profileStatsForCustomer` SQL aggregate (capped CTE + DISTINCT ON odds, per-card rounding), paged recent feed, showcased-only collection query, 30s cache + `clearProfileCache()`; `public-profile.spec` 7/7 incl. parity pin, leaderboard 3/3, advisor-reviewed; merged into `feat/virtual-shop` 2026-07-12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 023  | Revive the disabled E2E money-loop specs                              | P2       | M      | —          | DONE (worktree `agent-abc7f29cc93900db6`, commits `fad1bb26`..HEAD; all six `test.fixme` specs re-authored against the /slots reel + vault select-mode UI — customer flagship, anonymous demo spin (`?demo=1`), bulk-sell ("Select"/"Sell N" bar), delivery-request ("Deliver N"), both odds-reflection headlines (published-odds invariant is now a before/after snapshot of the /slots panel) — each verified passing twice; helpers: logout → /me, topup sends the now-mandatory Idempotency-Key, dead PUBLISHED_ODDS constant removed; baseline repair outside the plan's file list: card-management FMV assertions re-selectored for the MYR admin money fields (RM entered, USD stored); full suite green (18 passed, 1 pre-existing rewards env-skip); NOTE: the shared dev DB had lost the 6 seeded packs incl. `pokemon-rookie`/`pokemon-elite` — restored via the idempotent `yarn seed`) |
| 024  | Storefront honesty cleanup (fake trust signals, dead flows/code)      | P2       | M      | —          | DONE (worktree `agent-aa3af0f141451da14`, commits `0adf97af`..`511bdb0d`; merchants de-fabricated + disclosed, social/repacks disclosed + dead controls removed, /series un-sitemapped, UserMenu deleted, shadcn → devDeps; `npm run check` + 200 tests green, advisor-reviewed; merged into `feat/virtual-shop` 2026-07-12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 025  | Local integration-test runbook + smoke subset                         | P3       | S–M    | —          | DONE (worktree `agent-aa302ad78b2a6a49f`, commit `26f019e6`; api README runbook + `test:integration:smoke` (5 money suites incl. plan 021's), smoke 20/20 green twice incl. a clean-shell docs-only run, advisor-reviewed; merged into `feat/virtual-shop` 2026-07-12)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

Dependency notes: all independent; run **019 first** (cheapest, gates
everything after it in CI). **025 before 021/022** if the executor wants
local verification (CI's integration job covers them regardless).
021 and 022 both edit `modules/packs/service.ts` (different methods) and both
neighbor TODO plan 011 — rebase + drift-check on conflict; do not run 021 and
022 concurrently in separate worktrees if you can avoid it.

### Round-3 findings table (vetted)

Ordered by leverage. Confidence is the advisor's after re-reading the code.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                               | Category  | Impact   | Effort | Risk | Confidence | Plan |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | -------- | ------ | ---- | ---------- | ---- |
| 1   | CI's `yarn check-types` (turbo) silently skips `backend/packages/api` — no `check-types` script; `medusa build` is SWC transpile-only. Only a local Claude Stop hook type-checks it ("a red build … it did once" — the hook's own comment).                                                                                                                                                           | dx        | HIGH     | S      | LOW  | HIGH       | 019  |
| 2   | `.env.example` omits every `NEXT_PUBLIC_MEDUSA_*` var (the publishable key has no code fallback → all /store calls 401); README backend setup never creates `pokenic-postgres`/`pokenic-redis`; compose still ships `ai-website-cloner` identity.                                                                                                                                                     | dx/docs   | MED      | S      | LOW  | HIGH       | 020  |
| 3   | `matureDueCommissions` (`service.ts:3423`) runs the whole hourly job in ONE transaction, accumulating a `pg_advisory_xact_lock('credit:<id>')` per beneficiary and awaiting a notification write per row inside it — money paths for those customers block behind the batch; a notify throw rolls back all flips.                                                                                     | bug       | MED-HIGH | M      | LOW  | HIGH       | 021  |
| 4   | Public `/store/profiles/:handle` loads ≤20k pull rows + cards + a ~100k-ceiling odds set into Node per view, folds stats in JS, and has no cache (its siblings all do). `leaderboardTop` already computes the same aggregates in SQL.                                                                                                                                                                 | perf      | MED-HIGH | M      | MED  | HIGH       | 022  |
| 5   | The 6 storefront↔backend E2E money-loop specs (`signup→topup→open→vault→sell-back`, bulk-sell, delivery, odds headline) are `test.fixme` since 2026-07-07 — the only deterministic cross-surface money coverage is off.                                                                                                                                                                               | tests     | HIGH     | M      | LOW  | HIGH       | 023  |
| 6   | Fabricated content presents as production: `/merchants` decorates real companies (Cardmarket, TCGPlayer…) with "verified" badges + invented ratings; `/social`/`/repacks` mock data + dead controls, no disclosure; `/series` (self-declared demo) is in the sitemap; `/repacks` CTA funnels into `/clawmaker`'s unconditional "Please login" wall; + dead `UserMenu.tsx`, `shadcn` CLI in prod deps. | bug/trust | MED      | M      | LOW  | HIGH       | 024  |
| 7   | Backend integration suites (66, OOM-sharded, live PG+Redis) have no documented local path — money-path verification is effectively CI-only for contributors/executors.                                                                                                                                                                                                                                | dx/tests  | MED      | S–M    | LOW  | HIGH       | 025  |

### Round-3 findings vetted but NOT planned (ask for a plan if wanted)

- **`service.ts` god object (4,237 lines, 53 methods, 8 domains, #1 churn, no
  co-located unit spec).** The highest-blast-radius refactor candidate in the
  repo. Correct sequence: characterization tests on the money seams first,
  then extract domain helpers behind the service facade (several already
  exist: `buyback-rate.ts`, `pricing.ts`, `daily-box.ts`). L effort,
  MED risk — schedule deliberately, not as a drive-by.
- **Rarity-tier duplication + half-executed decision #10.** `RARITY_ORDER`
  (`src/lib/rarity.ts:19`) and `RARITIES` (`src/lib/packs-format.ts:13`) both
  claim to be the single source; `RARITY_RGB` vs `TIER_COLOR`
  (`price-tier.ts:21`) have drifted on 3 of 6 shades, and
  `slot-machine-redesign.md:181`'s "rarity colors everywhere in the reveal"
  decision is unfinished (`PokemonToken.tsx` still imports price-tier). S+M.
  Deferred: the mobile-first redesign will rework the reveal — fold it in then.
- **Money/date formatters re-implemented per surface** (`rm`/`relativeTime` in
  `src/lib/format.ts` vs `backend/apps/admin/src/lib/format.ts`; admin
  `usdToMyr` hand-mirrors backend `displayMarketPrice` and says so in a
  comment). Third instance of the mirror-drift class (after plan 005 and the
  round-2 note). A tiny shared package kills the class; M effort.
- **FX resolver + CDN redirect micro-caching.** `resolveFxRateInfo`
  (`pricing.ts:81`) hits the DB per call on ~10 uncached routes (S; keep the
  strict money-write resolver uncached); `/cdn/cards/[file]` 302s with no
  `Cache-Control` so every admin thumbnail re-round-trips (S). Both easy;
  bundle into any nearby backend PR.
- **Real-timer sleeps in integration specs** (`pack-open-rate-limit.spec.ts:140`
  sleeps 15.5s; `password-reset.spec.ts` fixed waits). S–M; make the
  rate-limit window test-configurable.
- **Admin SPA `src/lib` transform helpers untested** (`admin-rest.ts`,
  `queries.ts`, `image-validation.ts` — churn without unit specs; routes are
  legitimately Playwright-covered). S.
- **Vault grid re-renders all ≤500 tiles per selection toggle**
  (`VaultClient.tsx:435`) — measure first; likely fine (LOW confidence).
- **`backend/yarn.lock` pins `lodash@4.18.1`/`4.17.23`** — versions that don't
  exist on the public registry (manifest says `^4.18.1`). Investigate
  provenance before any lockfile regeneration (LOW confidence, S).
- **Operator-only items:** verify `.env.local` is gitignored (the
  guard-secrets hook correctly blocked the audit from checking; rotate if it
  was ever committed). `src/subscribers/password-reset.ts` logs reset tokens
  by documented dev-mode design — revisit when a real mail provider lands.
  Diff `backend/packages/api/.env.template` against the operational vars api
  code reads (`PRICECHARTING_API_TOKEN`, `FX_USD_MYR_URL`,
  `REWARDS_REDEMPTION_ENABLED`, `COMMISSION_COOLDOWN_DAYS`, `STOREFRONT_URL`,
  `MERCUR_STOREFRONT_URL`) — hook-blocked for agents.

### Round-3 verified clean / considered and rejected

Recorded so nobody re-audits. Each was opened (or its verification basis
stated) by the auditing agent and spot-confirmed by the advisor.

- **Admin-API/middleware coverage: clean.** Framework source confirms all
  `/admin/*` routes get mandatory admin auth; every store write maps to an
  `authenticate('customer',['bearer'])` matcher; Express 4 + path-to-regexp
  0.1.x `*`-spans-`/` semantics verified against the installed dependency, so
  the multi-segment matchers really cover their sub-routes. Every admin/store
  write validates its body. Customer sub-route pagination is double-bounded;
  FX fallbacks prevent 500s; public feeds mask PII.
- **Referral/commission/VIP/reward-box internals: no defect found** — sorted
  `credit:` locks, paged reads, idempotent reversal keys, integer-sen math,
  monotonic VIP upserts, cycle-guarded sponsor linking.
- **`drawDailyBox` product-prize stock read-then-mint**: same
  "stock is a counter, not a gate" posture already settled in round 1;
  economy dormant; not re-filed.
- **Pull-table indexes**: `IDX_pull_customer_id_rolled_at`, `IDX_pull_rolled_at`
  (partial), and the partial credit-tx index exist — the hot scans are covered.
- **`vault/buyback-batch` N serial workflows**: documented `ponytail:`
  tradeoff with measured cost and named upgrade path. Settled.
- **Storefront API proxies, PrizeReveal a11y, reset-password token handling,
  no HTML sinks (`dangerouslySetInnerHTML` count: 0)**: reviewed clean.
- **Admin fetch layering** (`client` → `packs-api` → `admin-rest` → `queries`):
  documented and intentional, not competing patterns.
- **scripts/sim** shares no logic with `src/` (thin HTTP wrappers only) — no
  drift risk. `public/cdn/cards` (4.6MB) is live (seed + mock + cdn route),
  not dead.
- **Cross-surface version splits** (React 18/19, zod 3/4, tailwind 3/4):
  framework-pinned across three separate runtimes; no shared bundle; not
  actionable.
- **`/bank-withdrawal`**: honest two-state placeholder, no fabricated data —
  explicitly cleared despite the audit hint.

### Round-3 Direction (options for the maintainer — not bugs, coarse estimates)

1. **P2P selling/trading — the missing verb.** `PRODUCT.md:18` promises
   "hold, trade, redeem, or sell back"; `how-it-works/page.tsx:142` promises
   "list it on the marketplace at your own price … trade cards with each
   other directly". No listing/trade/offer/transfer route or entity exists;
   the marketplace grid reads a single house seller (Mercur multi-vendor
   installed but unused); `/social` is the mock UI for it. L, HIGH risk
   (escrow/fraud/disputes) — scope as a listing-sale-first design spike, and
   note it only pays off after real cash-out exists.
2. **PSP integration + real cash-out readiness spike.** The code anticipates
   it by name: `topup.ts` "Batch B" seams, `topup-credits.ts` "the real
   gateway slots in here" with the idempotency contract already threaded;
   `/bank-withdrawal` says payouts go live with the gateway. The compliance
   half (AMLA/KYC) is the long pole — enumerate blockers before building.
3. **Launch the dormant rewards economy.** `REWARDS_REDEMPTION_ENABLED`
   default-off ("dormant until Phase P") gates already-built claim/draw/
   withdraw routes, admin editors, and storefront surfaces. A launch spike =
   enumerate remaining Phase-P work (economy parameters, abuse limits,
   runbook), then staged rollout. M-ish, MED risk.
4. **Promote the sim harness to a nightly economy-regression gate.** The
   2-day pilot caught a P1 money bug + 10 others with 0 harness errors;
   `ledger.mjs` invariants exist; nothing in CI references sim. M, LOW risk —
   and it's the safety net options 1–3 all want.
5. **Mock-route triage (DIR-05).** Keep-as-static vs delete per route:
   `roulette` (second gacha mechanic — delete unless wanted), `merchants` /
   `pokemon` (static reference or delete), `social`/`activity`/`repacks`
   (fold into the trading track — don't delete), honest placeholders
   (`30th`, `airdrop`, `free`, `clawmaker`, `bank-withdrawal`) keep. Plan 024
   makes everything honest in the interim, so this decision loses urgency.

### Round-3 coverage — what was NOT audited

- **No runtime exercise** — same as round 2: every verdict is a code-read.
- Vendored/framework code (`node_modules/@medusajs/*`, Mercur internals) was
  read only where a finding required it (router auth, path-to-regexp).
- Admin SPA behavior was audited statically; no browser walk of every screen.
- `scripts/sim` internals beyond debt/duplication (its correctness has its own
  findings pipeline in `SIM-FINDINGS.md`).
- `backend/apps/vendor` (Mercur vendor app) — out of active use, skimmed only.
- Localization/i18n depth, Windows-specific tooling paths, and the
  `.claude/` hook scripts themselves (read for evidence, not audited).

---

## Round 4 — deep full-repo audit (post-merge master + the #135–#142 delta), 2026-07-13

Generated by the `improve` skill (deep tier) against commit `dbce0561`
(branch `claude/deep-improvement-b6191c`, in sync with master). Method:
eight parallel read-only audits — the PR #140 playthrough-withdrawal money
delta, the rest of the backend delta (#135/#139 + merge coherence of all
round-2/3 plan work), storefront sweep, tests+CI, performance, tech
debt+deps, DX/docs, and direction — with every table-bound finding re-opened
and confirmed by the advisor against the live code. Same caveat as rounds
2–3: **static code-read, no runtime exercise.**

Non-interactive run: per the skill default, plans were written for the top
findings by leverage without waiting for a selection. Seven plans (the 3–5
default was expanded because this operator has executed all 25 prior plans).

**Headline.** The merged state of rounds 1–3 is coherent — no plan work was
mangled in the merges, middlewares changes are purely additive, and the
delta's supporting casts (address guard, delivery batch, admin pagination)
audit clean. The two stories of round 4: **(1) the new playthrough
withdrawal gate measures spend on the wrong basis** — commission/buyback-
funded play banks playthrough, so a later real-money deposit can unlock
without ever being played (display-only today; becomes a money hole the day
the mandated cashout writer lands) — and **(2) CI enforcement theater**: the
backend's ~90 unit/module specs (including both new wallet-gate specs) and
the admin SPA suite are defined, green locally, and executed by nothing.
Supporting cast: the README's onboarding path dead-ends at a git-excluded
script, `backend/package.json` pins a lodash version that doesn't exist on
the public registry, and the hottest authenticated read amplifies ~6× per
call.

### Round-4 plans

| Plan | Title                                                                   | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | ----------------------------------------------------------------------- | -------- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 026  | Playthrough gate: compute "used" on the external-funded (deposit) basis | P1       | M      | —          | DONE (APPROVE; branch `advisor/026-playthrough-external-basis`, commit `711d9cea`. `used_cents` now sums `−external_funded_cents` over pack_open rows (deposit-funded spend only); withdrawable.ts docs updated; existing fixtures made external-basis-honest; new discriminating case "promo-funded play does not unlock a later deposit" proves the fix; qa-withdraw-gate.mjs seeds now write the basis. Executor proved red→green via Step 2; reviewer re-ran: greps, backend typecheck, wallet-summary 4/4, smoke 20/20 (first smoke re-run had 5 failures — adjudicated as Postgres contention from concurrent module-tier runs; clean re-run green). Executor caveats on record: QA script syntax-checked not executed; the new case was only ever run green (its red proof is Step 2's mechanism). Reversal coherence independently verified at service.ts:819/822 + 926/929)                                                                                             |
| 027  | CI: run backend unit + module suites, admin vitest; restore turbo cache | P1       | M      | —          | DONE (APPROVE after 1 approved amendment; branch `advisor/027-ci-test-tiers`, commits `92d8577e` + `487c7860`. New CI jobs `backend-unit` (no-DB: jest unit tier + turbo `test` = admin vitest + odds-math/pokemon jest) and `integration-modules` (58 suites, service containers), turbo cache restore in `backend-quality`. Amendment: `reward-draw.unit.spec.ts` was a mis-suffixed module-runner test that made the no-DB unit tier red — renamed to `reward-draw.spec.ts` (module tier owns it; zero coverage loss). Reviewer re-ran: YAML parse ok, post-rename unit tier 634/634 without DB, turbo test 5/5, executor baselines unit 93s / modules ~174s. NOTE: full CI proof still needs the first PR run — watch the two new jobs there)                                                                                                                                                                                                                                |
| 028  | Onboarding truth round 2: hook exemption, .env.example, README scrub    | P2       | S–M    | —          | DONE (APPROVE; branch `advisor/028-onboarding-truth-2`, commit `86d96eef`, 8 in-scope files; reviewer re-ran all greps + typecheck + lint + `playwright test --list` green; guard-secrets exemption applied to both machine-local hook copies (uncommittable — replicate on other machines); accepted deviations: launch-stack mention dropped in favor of the checkable gate, `.env` block-probe substituted for nonexistent `.env.local`, `NEXT_PUBLIC_MEDIA_HOST` added per the plan's own cross-check. **Plan 020 step 1 now resolved → mark 020 fully DONE.** Operator follow-ups: rotate the seeded admin password on any persistent stack (literal remains in git history); stale `/claw` comment at `tests/e2e/slot-vault-room.spec.ts:7` noted, untouched)                                                                                                                                                                                                              |
| 029  | from-PC route: cap money fields + resolve pixel id at add-time          | P2       | S      | —          | DONE (APPROVE; branch `advisor/029-from-pc-bounds`, commit `a7254229`. `capAtMost` helper caps market_value/price at `MAX_MARKET_VALUE_USD` and stock at 10,000; `resolvePixelPokemonPatch` at add-time maps NOT_FOUND→400. 4 new rejection cases; reviewer re-ran: greps, typecheck, product-from-pc 6/6, card-inherits-pc 2/2. Accepted judgment call: shared helper instead of three inline guards — same message shape as validate.ts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 030  | Dependency hygiene: phantom lodash pin + unused @base-ui/react          | P2       | S      | —          | DONE-partial (APPROVE, amended: **lodash half REJECTED — finding invalidated**, lodash 4.17.23/4.18.0/4.18.1 are real 2026 releases verified against registry.npmjs.org publish dates + lodash GitHub tags, pin/lock legitimate as-is; @base-ui half done: branch `advisor/030-dep-hygiene`, commit `a477a613`, reviewer re-ran `npm test` 200/200 + `npm run check` green; backend/ byte-identical to dbce0561)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 031  | Cross-flow ledger-conservation spec in the money smoke subset           | P2       | S–M    | —          | DONE (APPROVE; branch `advisor/031-ledger-conservation`, commit `7e975b89`. New `ledger-conservation.spec.ts` asserts Σ(ledger)==creditSummary==walletSummary==HTTP view + external-basis conservation at 4 hops of a live topup→open→buyback flow; smoke subset now 6 suites; deliberately never pins playthrough.used (026-compatible by design). Reviewer verified the combined gate on the MERGED tree (with 026 live): smoke 6/6, 21/21 incl. the new spec. Executor deviations accepted: dropped unneeded inventory scaffolding (stock is a counter, not observed by conservation); one early misstep (install/build ran once in the main repo — no file edits there) disclosed and re-done in-worktree)                                                                                                                                                                                                                                                                   |
| 032  | /store/credits read diet + FX display cache + CDN redirect caching      | P3       | M      | —          | DONE (APPROVE; branch `advisor/032-credits-read-diet`, commits `c521b96e`+`cb71e1bb`+`312a2961`. Lean `GET /store/credits/balance` (auth-matched, 401-tested) now serves the header chip + vault page; 30s FX display cache with `clearFxDisplayCache()` seam, strict resolver provably uncached; CDN 302 carries `Cache-Control: public, max-age=86400`. Reviewer re-ran: pricing units 24/24, store-credits 5/5, typecheck, `npm test` + `npm run check` green. **Plan errata (executor caught, reviewer confirms):** the "0 matches" vault.ts grep criterion was wrong — `getTransactions` (vault.ts:227) legitimately needs the full route (transactions + totals), correct end state is 1 remaining match; and the cache exemplar lives in `store/profiles/[handle]/route.ts`, not service.ts. Documented tradeoff: a transient FX-read failure pins the fallback rate ≤30s (display only); executor also ran the FULL unit tier 640/640 to prove no cross-spec cache leak) |

**Execution PR:** all approved round-4 branches are integrated on
`advisor/round4-execution` and published as
[PR #143](https://github.com/elstonyth/Polycards/pull/143) (2026-07-13;
zero-conflict merge, merged-tree gates re-run green). Plan 031 appends to the
same branch once its verification gate clears. Merging the PR is the
operator's decision.

Ordering notes: all independent, but run **027 early** — once merged it
auto-enforces 026's and 031's new specs in CI. **026 must land before any
cashout writer is built** (see DIR-01). 028 completes plan 020's blocked
step 1 (mark 020 fully DONE when 028 lands). 028 and 030 both edit
`README.md` (different lines: onboarding/structure vs. the tech-stack
`@base-ui` row) — merge in either order, re-check both greps after the
second. 026 is the only round-4 plan touching `service.ts`; no cross-plan
file conflicts otherwise. 031 is green before and after 026 by design.

### Round-4 findings table (vetted)

Ordered by leverage. Confidence is the advisor's after re-reading the code.

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Category      | Impact                                           | Effort | Risk    | Confidence                     | Plan |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- | ------------------------------------------------ | ------ | ------- | ------------------------------ | ---- |
| 1   | Playthrough gate `used` = Σ raw `amount` over `pack_open` rows (`service.ts:2230`) instead of the deposit-funded `external_funded_cents` basis the rest of the money core uses (`creditSummary`, VIP). Promo/commission/buyback-funded play banks playthrough → a later deposit unlocks unplayed. Contradicts the gate's own header; cashout writer must route through it.                                                                                                                                                                                                                                                                                                                 | bug/security  | HIGH once cashout exists; MED now (display-only) | M      | MED     | HIGH mechanics / MED intent    | 026  |
| 2   | CI never executes `test:unit` / `test:integration:modules` (jest routes by `TEST_TYPE`; ci.yml only runs the HTTP tier) — ~90 specs incl. every money unit/module spec and both new wallet-gate specs are green-but-unenforced. Admin SPA `vitest` also never runs (no turbo `test` task). Turbo cache never restored (cold rebuild every backend PR).                                                                                                                                                                                                                                                                                                                                     | tests/dx      | HIGH                                             | M      | MED     | HIGH                           | 027  |
| 3   | Committed onboarding is broken on a fresh clone: README's provisioning step invokes `scripts/launch-stack.ps1` (git-excluded, `.git/info/exclude:46`); root `AGENTS.md`/`CLAUDE.md`/`docs/HANDOFF.md`/`docs/research/` referenced but gitignored; README flagship is the deleted `/claw`; `.env.example` still missing `NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY` (no code fallback → all /store 401). Root cause of the env-example blockage: `guard-secrets.js:30` regex blocks committed `.env.example`/`.env.template`. Plus: committed admin-password fallback in `tests/e2e/helpers/constants.ts:16` (env-required + rotation), Vite-boilerplate admin README, e2e README lists 3/9 specs. | dx/docs       | MED-HIGH                                         | S–M    | LOW     | HIGH                           | 028  |
| 4   | `from-pricecharting` route: `market_value`/`price`/`stock` have no upper bound (sibling card path caps at `MAX_MARKET_VALUE_USD`); `pixel_pokemon_id` guard checks non-emptiness, not resolvability — a bogus id degrades at card-registration to the exact spriteless card PR #135 targeted.                                                                                                                                                                                                                                                                                                                                                                                              | security/bug  | MED (admin-only trigger)                         | S      | LOW     | HIGH / MED                     | 029  |
| 5   | ~~`backend/package.json:21` pins `lodash: ^4.18.1` — a version lodash never published~~ **[REFUTED during execution — 4.17.23/4.18.0/4.18.1 are real 2026 releases; see "Lodash phantom versions — REFUTED" below. Lodash half rejected.]** Still valid: `@base-ui/react` is a runtime dep with zero import sites; README still claims it as the UI foundation.                                                                                                                                                                                                                                                                                                                            | deps/security | MED                                              | S      | LOW-MED | HIGH facts / MED non-existence | 030  |
| 6   | PR #138's sim deletion removed the only cross-flow money-conservation check (`ledger.mjs`); nothing now asserts `Σ(ledger) == balance` across a live topup→open→buyback flow. One integration spec in the plan-025 smoke subset restores the invariant — the safety net the future cashout writer needs.                                                                                                                                                                                                                                                                                                                                                                                   | tests         | MED (HIGH once cashout lands)                    | S–M    | LOW     | HIGH                           | 031  |
| 7   | `/store/credits` amplifies a one-number balance read into ~6 DB round-trips (two near-identical full-ledger scans + a discarded 50-row fetch), re-triggered after every money action by the header chip; `resolveFxRateInfo` hits the DB per call on ~14 uncached display routes; `/cdn/cards/[file]` 302 has no `Cache-Control`. Indexes all verified present.                                                                                                                                                                                                                                                                                                                            | perf          | MED                                              | M      | LOW-MED | HIGH                           | 032  |

### Round-4 findings vetted but NOT planned (ask for a plan if wanted)

- **`WalletSchema` hard-requires the two new wallet fields**
  (`schemas.ts:248-253`): an older-backend response would fail the whole
  wallet page instead of degrading (the module's own `OddsEntrySchema`
  documents the graceful convention). Cannot fire in the current
  lockstep deployment — do opportunistically when next touching schemas,
  together with adding `WalletSchema` to `schemas.test.ts` (it's the one
  schema the contract test doesn't exercise). S.
- **Per-PR E2E smoke** — the money-loop E2E gates nightly only
  (`e2e.yml`: schedule + dispatch, a documented ~15-25-min-boot tradeoff).
  A 1-spec per-PR smoke job is possible but flake-prone; maintainer call.
- **Real-timer sleeps** (`pack-open-rate-limit.spec.ts:140` sleeps 15.5s in
  CI; `password-reset.spec.ts` fixed waits) — round-3 backlog, still open.
  S–M (make the rate-limit window env-configurable in test mode).
- **E2E seed-presence guard** — 8 specs hardcode `pokemon-rookie`/
  `pokemon-elite`; CI reseeds fresh, but local runs fail opaquely if the
  shared dev DB drifts (it happened once). S nicety: fail-fast preflight
  in `tests/e2e/helpers/`.
- **`reward-draw.spec.ts` product-link case uses an orphan string id**
  (`pull_abc123`) instead of a real created Pull — flagged by CodeRabbit on
  PR #143; valid, but pre-existing content (the PR only renamed the file).
  S: create a reward Pull and assert the association with its generated id.
- **Admin SPA `src/lib` churn helpers still untested** (`admin-rest.ts`,
  `queries.ts`, `image-validation.ts`) — round-3 backlog; `format`/
  `odds-rows`/`query-keys` gained specs, these didn't. Worth more once 027
  makes admin tests run in CI. S.
- **`walletSummary`/`creditSummary` overlapping-scan merge + serial-await
  parallelization** — deferred inside plan 032 (needs MikroORM
  shared-manager concurrency verification first; the lean endpoint removes
  the hot-path pressure).
- **e2e.yml's own ephemeral admin-password literal** (lines ~82-84) — kept
  as the documented CI-only tradeoff (DB dies with the job); wiring a
  `PW_ADMIN_PASSWORD` repo secret would make the literal path dead.
  Operator action.
- **Operator reminders**: rotate the PriceCharting API token
  (`docs/superpowers/PRICECHARTING-DEPLOY-NOTES.md` self-documents it as
  compromised); rotate the seeded admin password on any persistent stack
  once plan 028 removes the committed fallback.

### Round-4 verified clean / considered and rejected

Recorded so nobody re-audits. Spot-confirmed by the advisor where table-bound.

- **Lodash "phantom versions" — REFUTED during execution (2026-07-13).** The
  round-4 table's finding 5 (lodash half) claimed 4.18.1/4.17.23 don't exist
  on public npm. Execution-time network verification proved otherwise:
  registry.npmjs.org reports genuine publish dates (4.17.23 → 2026-01-21,
  4.18.0 → 2026-03-31, 4.18.1 → 2026-04-01) and the lodash GitHub repo
  carries matching release tags. The audit premise was stale
  training-data knowledge (it was honestly hedged as "MED on non-existence,
  not network-verified"). The `^4.18.1` pin and lockfile are legitimate —
  do not re-file, do not "fix" the pin.
- **Playthrough gate mechanics** (aside from the basis finding):
  `withdrawable ≤ available ≤ balance` holds in all branches; SQL cent math
  matches the `creditSummary` pattern; `/store/credits` is bearer-auth,
  actor-scoped; reason enum consistent across model/service/storefront/DB
  CHECK; reversal mirror rows restore the external basis
  (`external_funded_cents: -originalExt`). The `deposited` filter's
  no-negative-topup asymmetry is latent-only (no topup-reversal path
  exists) — documented in plan 026's maintenance notes.
- **Insufficient-credit copy (#136)**: integer-cent-correct; the live
  charge site (`settleOpen`) reports locked-aware `available`. Clean.
- **Address guard (#139)**: all bypass classes traced (empty/whitespace/
  null/missing/non-string; update-mode blanking rejected); unit spec locks
  them. Zero-width-char postal code is a data-quality edge, not a bypass.
- **Delivery-orders route**: batch cap before per-element scan; duplicate
  pull_ids handled downstream; ownership/status/frozen gates precede writes.
- **middlewares.ts delta**: purely additive since #127 (the plan-010
  metadata guard); no matcher loosened; from-PC route consistent with
  sibling CRUD routes in carrying no rate limit (only money-mutation routes
  do).
- **Merge coherence of plans 009–025 in master**: every caller resolves to
  live symbols (pulls pagination, profile aggregates + cache seam,
  per-beneficiary `matureDueCommissions`, bake-slab SSRF denylist). No
  half-applied merges.
- **Storefront honesty-cleanup regressions**: none — no dead imports,
  no dangling links (`/clawmaker` fully gone), no missed fabricated claims;
  demo surfaces all disclosed; wallet page loading/error/staleness paths
  clean (`router.refresh()` after top-up).
- **Perf negatives**: no hot-path N+1 (delivery/vault/leaderboard batched);
  service.ts per-row loops are cold-path bounded; admin TanStack has no
  refetch storm (Mercur's 90s default staleTime); storefront bundle lean,
  `next/image` consistent; the abandoned `perf/p1-frames-listcache`
  worktree was right to be abandoned (single-row read).
- **CI workflow hygiene**: SHA-pinned actions, least-privilege permissions,
  no `continue-on-error`, gitleaks full-history scan, correct path filters.
- **Sim removal (#138)**: clean — zero dangling references in tracked files
  (`seed-sim-daily-box.ts` was deleted too); only the historical `plans/`
  record still names sim files, which is expected.
- **Dead code**: none found beyond `@base-ui/react` (plan 030);
  `npm audit --omit=dev` at root: 0 critical/high (2 known moderates that
  clear on the next Next bump).
- **`service.ts` growth** (4,237 → 4,424 lines): proportional feature work,
  not new architectural decay; the god-object refactor stays a deliberately
  scheduled deferred item from round 3.
- **QA script `qa-withdraw-gate.mjs`**: logic correct, guarded to
  `wdtest-*` customers on local docker only. (Plan 026 may touch its seeds
  for the basis change.)

### Round-4 Direction (updates to round-3's options + one new)

1. **DIR-01 — Close the cash-out loop (round-3 option 2, now concrete and
   urgent-adjacent).** PR #140 shipped the read-model 20%: gate, `cashout`
   ledger reason (written by nothing), `walletSummary.withdrawable`, and a
   user-facing "eligible for withdrawal" number on the wallet page — with
   no payout action anywhere. The spike now has a concrete contract: a
   cashout writer that recomputes the gate **under the per-customer
   `credit:` lock**, caps at `available`, writes the `cashout` row, plus an
   admin approval surface and the PSP/KYC/AMLA long pole. Cheapest interim
   move if the spike slips: soften the wallet-page copy so "eligible for
   withdrawal" doesn't promise an imminent payout. L, HIGH risk. **Do plan
   026 first regardless** — shipping cashout on the current basis ships the
   hole.
2. **DIR-03 — Disclose the 100%-playthrough requirement at deposit time.**
   Enforced backend-side, surfaced only post-hoc on the wallet page; the
   top-up flow says nothing. A material term for a money product and a
   prerequisite disclosure for DIR-01's compliance story. S (copy + the
   `playthrough` fields already returned); product/jurisdiction call, so
   left as direction.
3. **Round-3 option statuses**: P2P trading (opt 1) — grounding corrected:
   `PRODUCT.md` never existed (round-3 cite was phantom); the marketplace
   promises are now feature-flag-gated off by plan 024, so P2P is a clean
   deferred bet sequenced after cash-out. Rewards launch (opt 3) —
   unchanged. Sim-as-nightly-gate (opt 4) — **dead**: the maintainer
   deleted the harness (#138); the salvageable 20% became plan 031. Mock-
   route triage (opt 5) — urgency resolved by plan 024's disclosures;
   drop from the roadmap or fold into the trading track.

### Round-4 coverage — what was NOT audited

- **No runtime exercise** — every verdict is a static code-read (same
  caveat as rounds 2–3); plan verification commands are the executors'
  runtime proof.
- `backend/packages/api/.env.template` content — unverifiable until plan
  028's hook exemption lands (guard-secrets blocked all agents, correctly
  per its current regex).
- `backend/apps/vendor` — dormant, skimmed only (its lack of a `test`
  script is asserted in plan 027's STOP conditions, not deeply audited).
- Admin SPA behavior — static only, no browser walk.
- Vendored/framework internals — read only where a finding required it.
- i18n depth, Windows-specific tooling paths, `.claude/` hook scripts
  beyond `guard-secrets.js` (read as evidence for finding 3).

---

## Round 5 — deep full-repo audit (the layers rounds 1–4 left thin), 2026-07-13

Generated by the `improve` skill (deep tier) against commit `38f7dbdd`
(master, post-#143). Method: eight parallel read-only audits — money-core
correctness (post-merge coherence of 026–032), backend corners never covered
(vendor app, env template, jobs/subscribers/scripts), security full surface,
storefront sweep (previously-skimmed pages), tests+CI (027 verification +
backlog), performance (admin SPA + non-dieted routes), tech debt/deps/DX/docs,
and direction — every table-bound finding re-opened and confirmed by the
advisor against live code. Same caveat as rounds 2–4: **static code-read, no
runtime exercise.**

Non-interactive planning was overridden: the user asked for **all** findings
to become plans and to execute them. Eleven plans (033–043) written.

**Headline.** The money core survived its fifth read — 026–032 merged
coherently, the FX cache strict/lenient split is correct, admin auth and
secrets hygiene are clean, and CI now enforces what it defines. Round 5's two
stories: **(1) the last mile before cash-out has two landmines** — pre-migration
ledger rows make grandfathered deposits permanently non-withdrawable (033),
and a live, never-audited Mercur **seller-registration surface** ships to prod
(034); **(2) the honesty cleanup missed the most-reachable fabricated
surface** — the leaderboard (a main nav tab) promises weekly prizes no backend
pays, and any backend outage renders a fabricated persona on a real user's
profile URL (035).

### Round-5 plans

| Plan | Title                                                                    | Priority | Effort | Depends on                       | Status                                                                                                                                                                                                  |
| ---- | ------------------------------------------------------------------------ | -------- | ------ | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 033  | Grandfather pre-1b deposits in the playthrough withdrawal gate           | P1       | S–M    | —                                | DONE (advisor/round5-execution; PR #144)                                                                                                                                                                |
| 034  | Verify + fence the Mercur seller-registration / vendor surface           | P1       | S+S    | —                                | DONE — investigation downgraded severity: self-registration only yields a PENDING_APPROVAL stub (store visibility needs admin-gated OPEN); fence removes unaudited surface + restores framework default |
| 035  | Storefront honesty — leaderboard prizes, profile error state, pack-party | P2       | S–M    | —                                | DONE (prize rail removed; profile error state; pack-party disclosed)                                                                                                                                    |
| 036  | Bound the daily-reward voucher amount server-side                        | P2       | S      | —                                | DONE (MAX_VOUCHER_MYR=10_000)                                                                                                                                                                           |
| 037  | Docs & env truth round 3 — DESIGN brand, CLAUDE.md drift, env template   | P2       | S      | (coord. 034 on env vendor lines) | DONE for DESIGN.md + env template; **CLAUDE.md edits are an operator follow-up** (gitignored — not committable from a worktree)                                                                         |
| 038  | Fold the redundant second ledger scan out of /store/credits              | P2       | S      | 033                              | DONE (single scan; +revision to fix the credit-balance.unit shape assertions it regressed)                                                                                                              |
| 039  | Test-suite hygiene — real-timer sleep, seed guard, orphan-id, a11y gate  | P2       | S–M    | —                                | DONE (sleep 103s→46s; seed guard; real-Pull reward-draw; a11y gate wired nightly)                                                                                                                       |
| 040  | WalletSchema graceful degrade + contract-test coverage                   | P2       | S      | —                                | DONE                                                                                                                                                                                                    |
| 041  | Dedupe rarity arrays + lock the money-display mirror with a parity test  | P2       | S–M    | —                                | DONE (+revision: usdToMyr now mirrors displayMarketPrice's negative-usd guard)                                                                                                                          |
| 042  | Lazy-load admin card images + fix stop-hook mojibake banner              | P3       | S      | —                                | DONE for the lazy-load images; **stop-verify.js mojibake is an operator follow-up** (gitignored)                                                                                                        |
| 043  | Make reset-admin-password.ts recoverable (operator-applied; untracked)   | P3       | S      | —                                | OPERATOR-APPLIED — untracked file, not worktree-dispatchable; fix handed to the operator                                                                                                                |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

### Round-5 dependency & sequencing notes

- **038 depends on 033**: 033 adds the `external_funded_cents IS NOT NULL`
  filter to `walletSummary.deposited`; 038 threads that filtered value from a
  single shared `creditSummary` scan. Land 033 first; 038 is written against
  the post-033 SQL shape.
- **037 coordinates with 034** on the env template: 034 owns the
  `MERCUR_VENDOR_URL`/`VENDOR_CORS` vendor lines (may remove them when
  fencing); 037 owns every other env var and explicitly leaves the vendor
  lines alone to avoid a conflict.
- **034 is investigate-first**: Step 1 (probe the seller surface) gates Step 2
  (fence). It STOPs to the reviewer if anonymous registration yields a usable
  seller or the house seller was provisioned via that flow.
- **043 is operator-applied, not worktree-dispatched**: its target file is
  untracked (`??` in git), so a worktree branched from HEAD doesn't contain
  it. The operator applies the fix locally and decides whether to commit the
  tooling.
- All other plans are independent and touch disjoint files.

### Round-5 findings table (vetted)

Ordered by leverage. Confidence is the advisor's after re-reading the code.

| #   | Finding                                                                                                                                                                                                                                                                                                                                  | Category     | Impact                                          | Effort | Risk    | Confidence                | Plan |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ----------------------------------------------- | ------ | ------- | ------------------------- | ---- |
| 1   | Playthrough gate: `deposited` sums `amount` (all history) but `used` sums the nullable `external_funded_cents` (no backfill pre-2026-06-21). Deposit+play before 1b → `withdrawable=false` forever. Display-only now; permanent cash-out lockout once the DIR-01 writer ships. Plan 026 didn't cover it.                                 | bug (money)  | MED now / HIGH pre-cashout                      | S–M    | LOW     | HIGH                      | 033  |
| 2   | `seller_registration: true` + `/seller` dashboard + `@mercurjs/core` `/vendor/*` routes ship in the prod image; never auth-audited (prior middleware audits scoped `/admin`+`/store` only). Likely a stock starter default nobody turned off on a single-house-seller app.                                                               | security     | MED-HIGH if registration yields a usable seller | S+S    | LOW-MED | HIGH config / MED exploit | 034  |
| 3   | Leaderboard prize rail (a primary nav tab) promises weekly payouts no backend pays, no disclosure; profile-fetch failure renders a fabricated persona under a real handle; pack-party fabricated "live" parties undisclosed (mitigated: flag-off, unlinked).                                                                             | bug/trust    | MED                                             | S–M    | LOW     | HIGH                      | 035  |
| 4   | Daily-reward `voucher_amount` is the one admin money input with no server-side ceiling — a credit-minting lever once the rewards economy launches. Every sibling is capped.                                                                                                                                                              | security     | LOW-MED (latent, admin-only)                    | S      | LOW     | HIGH                      | 036  |
| 5   | Agent-facing docs contradict reality: tracked DESIGN doc ships as "Polycards" while CLAUDE.md says it doesn't exist; CLAUDE.md still cites removed `@base-ui/react` + a mojibake wrong-repo HANDOFF as source-of-truth; env template omits 6 code-read vars + the `ALLOW_MOCK_TOPUP` prod-checklist note, has 1 stale + 1 duplicate var. | docs/dx      | MED                                             | S      | LOW     | HIGH                      | 037  |
| 6   | `/store/credits` scans the full customer ledger twice — `walletSummary`'s columns are a strict subset of `creditSummary`'s. The round-4 "MikroORM concurrency" deferral blocker is moot (threads scalars, no shared manager).                                                                                                            | perf         | MED (scales w/ ledger size)                     | S      | LOW     | HIGH                      | 038  |
| 7   | Test hygiene: the 15.5s real sleep now runs in CI every backend PR (post-027); 9 e2e specs hardcode seed slugs w/ no preflight; `reward-draw.spec.ts` asserts an orphan string id; `test:a11y` runs in no workflow.                                                                                                                      | tests/dx     | MED                                             | S–M    | LOW     | HIGH                      | 039  |
| 8   | `WalletSchema` hard-requires the new wallet fields (fails the whole page instead of degrading) and is the one schema the contract test skips.                                                                                                                                                                                            | bug (latent) | LOW-MED                                         | S      | LOW     | HIGH                      | 040  |
| 9   | Mirror-drift class trigger met a cycle ago, module unbuilt: `usdToMyr` hand-mirrors backend `displayMarketPrice` untested; two "canonical" rarity arrays; `RARITY_RGB`/`TIER_COLOR` drift (deferred to redesign).                                                                                                                        | tech-debt    | MED (recurring sync tax)                        | S–M    | LOW     | HIGH                      | 041  |
| 10  | Admin card catalog renders ≤1000 eager images unpaginated (real as catalog grows); stop-hook banner prints mojibake.                                                                                                                                                                                                                     | perf/dx      | LOW-MED                                         | S      | LOW     | HIGH                      | 042  |
| 11  | `reset-admin-password.ts` (untracked) deletes the only emailpass identity before registering the replacement — a failed register locks the admin out until re-run.                                                                                                                                                                       | bug          | LOW                                             | S      | LOW     | HIGH                      | 043  |

### Round-5 verified clean / considered and rejected

Recorded so nobody re-audits. Confirmed by the advisor.

- **Plans 026–032 merge coherence**: no half-applied merges; all 25 FX
  resolver call-sites split strict (money-write, uncached) vs lenient
  (display, 30s cache) correctly; open/batch saga compensation ordering sound;
  the lean `/store/credits/balance` route is auth-matched and parity-specced.
- **Security surface (aside from 034/036)**: `/store/credits/balance` inside a
  bearer matcher + actor-scoped (IDOR-safe); every money-mutation route
  carries auth + write-tier limiter; from-PC route (post-029) fully capped;
  CSP enforced in prod (`.do/storefront.app.yaml`); JWT cookie
  httpOnly+sameSite+secure; 0 HTML sinks; secrets hygiene post-028 confirmed
  (constants.ts fallback removed); root audit 2 moderate / 0 high, backend
  audit 0 high reachable.
- **Plan 027 CI wiring**: no turbo↔CI gap; `backend-unit` runs jest unit tier
  - turbo `test` (admin vitest + odds-math/pokemon); `integration-modules`
    runs the module tier; turbo cache restored in all three backend jobs. Money
    critical paths are densely covered AND now enforced.
- **Storefront**: server/client split issues no client-side refetch of
  render-time data (zero `useEffect` in vault/pack-detail/marketplace
  clients); wallet copy matches plan 026's basis; new-since-#127 React code
  (`use-recent-pulls`, `useSellWindow`, `InviteClient`) has correct
  cleanup/guards; client money math reads backend-authoritative values.
- **Backend jobs/subscribers**: `sync-market-prices` paginates + rate-limits +
  is per-card-error-tolerant; `vip-spend-settled` is a thin post-commit
  forwarder; `matureDueCommissions` per-beneficiary txns (plan 021) confirmed;
  `links/` has no custom defineLinks.
- **`next.config.ts` uncommitted local change** (pokemoncenter remotePattern +
  default DO Spaces media host): well-formed, `NEXT_PUBLIC_MEDIA_HOST` still
  overrides, `next/image` optimizer SSRF guard still applies, no CSP coupling
  — the user's WIP, not a finding; would not break if committed.
- **Lodash pin** (round-4 REFUTED): still legitimate — do not re-file.
- **service.ts god object** (4,425 lines / ~65 methods / 91 commits): refreshed
  but disposition unchanged — deliberately-scheduled refactor (characterization
  tests first), not a drive-by. Same for the full cross-runtime shared package
  (plan 041 takes only the two safe slices).

### Round-5 Direction (options for the maintainer — not bugs)

Refreshed from rounds 3–4; the cash-out cluster is now the clear next build.

1. **DIR-01 — Cash-out writer spike (fully unblocked).** Every seam is named
   in code (`withdrawable.ts:14` documents the contract; the `cashout` ledger
   reason exists end-to-end, written by nothing). **Plan 033 is a hard
   prerequisite** — shipping the writer on the current basis ships a permanent
   lockout for grandfathered deposits. Pairs with the still-missing **admin
   money-action surface** (operators settle buyback disputes via the raw
   credit-adjust lever today — no `settle`/`honor`/`payout` route exists). L
   overall (PSP+KYC/AMLA long pole); internal writer + admin surface is M.
2. **DIR-02 — Finish the mobile-first redesign on the brand surfaces.** The app
   shell already shipped (#61: 5-tab bar, TopUpSheet, pill, px-fluid, DESIGN
   tokens); PRODUCT.md's remaining target is home/how-it-works/leaderboard/
   about — per-page recomposition, each independent + low-risk. M–L coarse.
3. **DIR-03 — Disclose the 100%-playthrough requirement at deposit time.**
   Enforced backend-side, invisible in `TopUpSheet.tsx`; a material money-
   product term and a DIR-01 compliance prerequisite. S.
4. **DIR-04 — Rewards-economy launch spike.** `REWARDS_REDEMPTION_ENABLED`
   default-off gates built routes; the CI-enforced module specs + ledger
   conservation net now exist. Plan 036 (voucher cap) is a cheap prerequisite.
   M, MED risk.

### Round-5 coverage — what was NOT audited

- **No runtime exercise** — every verdict is a static code-read (plan
  verification commands are the executors' runtime proof).
- `/vendor/*` live behavior — the discriminating check for finding 2 (plan
  034 Step 1 runs it).
- Migration↔model coherence (needs `migrations:check`); admin SPA browser
  walk; vendored/framework internals beyond what a finding required; i18n
  depth; Windows tooling paths.

---

## Round 6 — deep full-repo audit (the #144–#227 delta), 2026-07-20

Generated by the `improve` skill (deep tier) against commit `b5944e26`
(branch `elstonyth/isopod`). Delta since round 5's `38f7dbdd`: ~83 commits /
634 files / +47k lines — notifications module (#220–#226), Weekly Pulled
Value Challenge + recorded pull value (#212/#213/#219), wallet
withdrawal-gate redesign + open_id provenance (#227), debit-at-press (#200) +
enrichment-failure serving (#201), slab label/compose (#198–#206),
liquid-glass UI (#197), Google OAuth (#174), Resend (#199), VIP redesign +
admin reward surfaces (#207/#208), rebrand/infra rename, CI reshard (#210).
Method: eight parallel read-only audits (backend correctness, frontend
correctness, security, performance, tests+CI, tech debt, deps/DX/docs,
direction), every table-bound finding re-opened and confirmed by the advisor
against live code. Same caveat as rounds 2–5: **static code-read, no runtime
exercise.** User selected: all 12 finding plans + the DIR-A design spike.

Round-6 DONE rows mean **executed on the named branch/worktree, not yet
merged to master** — unlike round 2, whose DONE rows are merged.

> **2026-08-01 (round 8) reconciliation:** only 044+047 (via PR #247), 053, and
> the a11y/CSP-gate work (PR #251) ever reached master. Plans 045, 046, 048,
> 049, 050, 051, 052 were executed and approved but **never merged** — see the
> round-8 section's merge-state table. Plan 059 recovers the code content;
> plan 068 supersedes 050's doc content; 055/056 were independently re-done by
> later PRs.

**Headline.** The money core survived its sixth read — no ledger,
idempotency, or basis break anywhere in the delta, and the round-4/5 ledger
diets held under the #227 redesign. Round 6's stories: **(1) the new admin
reward surfaces (#208) dropped the money-input discipline** every prior
round enforced (uncapped `voucher_amount`/`reward_credits`, no
`adminActionRateLimit`) on exactly the three new routes; **(2) the
notification unread badge fires an unindexable COUNT on every navigation**
(composite-unique-only index geometry); **(3) two E2E specs silently skip
every nightly** (card lifecycle, rewards claim) while reading as coverage;
and **(4) ops docs disagree with reality** — infra names three ways, a fixed
vendor fence still listed as a BLOCKING gate, a CHANGELOG describing the
repo's template ancestor. Forward: the operator's own go-live runbook marks
the Weekly-Challenge settlement engine BLOCKING — board and prize copy
shipped, nothing snapshots or pays at week close (plan 056 spike).

### Round-6 plans

| Plan | Title                                                                   | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ---- | ----------------------------------------------------------------------- | -------- | ------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 044  | Cap + rate-limit the new admin reward money routes (#208)               | P1       | S      | —          | DONE (APPROVE 2026-07-21; branch `advisor/044-admin-reward-caps`, commit `09f30bb9`, worktree `agent-ab336d4580667b895`. Reviewer re-ran: `adminActionRateLimit` count 13→16, caps live in both validators (voucher/reward/payout ≤ MAX_VOUCHER_MYR, thresholds ≤ 100M), 3 POST matchers verified against real route paths. Full unit tier 828 passed. Documented in-scope deviation accepted: 2 pre-existing negative-value assertions re-aligned to the new message strings)                                                                                                                                                                                                                                  |
| 045  | Index the notification unread-count path (+ verify core table)          | P1       | S      | —          | DONE (APPROVE 2026-07-21; worktree `agent-a034b72d35edb9465`, commit `4ef61aa1`. Model index + `Migration20260720150000` applied; reviewer re-read the full diff (exactly 2 files, +29) and confirmed the index live in pokenic-postgres `\d notification_read`. Part B verdict: core `notification` table ALREADY has `IDX_notification_receiver_id` — pre-existing, nothing added. EXPLAIN uses both indexes per executor; migration already applied to the shared dev DB (idempotent IF NOT EXISTS, harmless pre-merge))                                                                                                                                                                                     |
| 046  | update-card: roll back Card mutation when the product mirror fails      | P1       | S–M    | —          | DONE (APPROVE 2026-07-21 after 1 code-review revision; branch `advisor/046-update-card-rollback`, commits `20265acf` + `7589302f`, worktree `agent-046-update-card` (under Desktop\...\.claude\worktrees). Round 0: catch restores all 19 forward-write fields, `newSlabKey` at both return sites, 4 failure cases red→green. Code review caught the one unguarded delete: unchanged-key re-bake + mirror failure would delete the card's ONLY slab — fixed with the sibling equality guard (`:336`, reviewer-verified) + a 5th case proven red (1 failed/9 passed unguarded) → 10/10 green. Full unit tier 827)                                                                                                |
| 047  | Admin challenge/VIP tabs: preserve edits on switch; retire payout patch | P2       | S–M    | 044        | TODO                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 048  | Unskip the two dark E2E specs (pw-test-card seed + rewards flag)        | P2       | S–M    | —          | DONE (APPROVE-with-caveat 2026-07-21 after 1 code-review revision; worktree `agent-a47d3ce4ab7ce76bb`, commits `625ec383` + `8812eec3` (comment-truth fixes: card-management "CI skip is expected" reworded to skip-is-an-alarm; e2e.yml Claim-button comment corrected). Seed idempotency ×3, fixtures DB-verified, SEED_DEMO confirmed ON in the nightly. Executor finding: no seed had ever minted a grant — rewards.spec's header was aspirational; now real. CAVEAT: **first nightly run is the final proof — both specs must show passed, not skipped**. Follow-up candidate: delete duplicate `create-test-product.ts`)                                                                                  |
| 049  | CI: dedupe unit specs out of the modules tier; turbo cache for http job | P2       | S      | —          | DONE (APPROVE 2026-07-21; worktree `agent-a0997078dfc46ca83`, commit `4092ca3d`. Reviewer read both diffs — verbatim per plan, pure additions (+19), 4 `Cache turbo` steps. Proven: modules tier 64→21 suites (−43, all `*.unit.spec.ts`, DB run 21/21 green), unit tier unchanged 823 passed, all 43 runner-free. WATCH ON FIRST PR: http-shard log should show a turbo restore + faster workspace-deps build)                                                                                                                                                                                                                                                                                                 |
| 050  | Docs truth round 4: infra names, runbook vendor bullet, CHANGELOG       | P2       | S      | —          | DONE (APPROVE 2026-07-21; worktree `agent-a01b0d2da17e90983`, commit `7888b138`. Step-1 branch 1 PROVEN: `doctl databases list` → only polycards-pg/valkey (reviewer re-ran independently); pokenic-media 404s → infra-rename runbook marked EXECUTED 2026-07-15; go-live runbook + CHANGELOG + redesign header fixed. Reviewer re-ran all done-criteria greps: 0/0/0; scope exact 4 files)                                                                                                                                                                                                                                                                                                                     |
| 051  | Storefront: true unread total on Mark-all-read; halt SFX on unmount     | P2       | S      | —          | DONE (APPROVE 2026-07-21 after 2 revisions — limit reached; branch `advisor/051-storefront-small-fixes`, commits `4140ac51`+`f6427fb5`+`42a060f1`+`fa5a3e7a`, worktree `agent-aca981fdd703ea0e8`. Rev 1 (executor-caught plan gap): page-scoped early-return dead-buttoned the total-scoped label — dropped. Rev 2 (code-review-caught plan gap): frozen prop couldn't observe the button's own successful clear ("(15)" after clearing 35) — `serverTotal` lifted to state, zeroed on success (reviewer-verified `:30/:40/:102`), 5th helper case. typecheck/test(290)/check green. Accepted: back-nav skew (clamped); no DOM teardown test (no jsdom harness))                                                |
| 052  | notifyFeedNonfatal wrapper (kills the 7-site try/catch copy-paste)      | P3       | S      | —          | DONE (APPROVE 2026-07-21; worktree `agent-a058b5928e67b84cc`, commits `3eac438a` + `3f0a8f4f` (post-review touch-up: stale settle-vip catch wording), 9 files net −29 lines. Reviewer grep: 0 direct `notifyFeed(` calls outside module/tests/scripts; wrapper + settle-vip partial swap read + confirmed. 2 documented deviations APPROVED on merit: settle-vip outer catch guards the GRANT; wrapper log carries `idempotencyKey` so existing spec assertions stay meaningful. 3 silent sites now log per #222. Independent code review: Ready-Yes. Unit 827 + HTTP 7/48 green)                                                                                                                               |
| 053  | Unit-test the Google OAuth server actions                               | P3       | M      | —          | DONE (APPROVE 2026-07-21 after 1 code-review revision; branch `advisor/053-oauth-action-tests`, commits `8a313903` + `0923710c`. Code review PROVED the headline keys-only-log assertion vacuous by mutation (Object.values leak left all 16 green); fix: decoy PII fixtures (`decoy@leak.example`) + key-NAME pins (reviewer-verified `:229/:257/:259`), mutation re-run confirmed 2 RED then restored clean. Refresh assertion tightened to enforce its Bearer-token comment. 16 targeted / 296 full green; still zero production-code changes)                                                                                                                                                               |
| 054  | Extract the Weekly-Challenge slice + backfills out of service.ts        | P3       | M      | 044, 047   | TODO (criteria made drift-proof 2026-08-18 by plan 111 — the Commands-table, Step 5 Verify and Done-criteria line-count targets no longer pin to an absolute service.ts figure (it was 9,302 lines at 16cc85d3, past the stale <=~7,700 target before any work started), so future growth cannot stale them again. The account-lifecycle slice is promoted from plan 106s option framing to the RECOMMENDED first extraction — this challenge slice is NOT cancelled, only sequenced second.)                                                                                                                                                                                                                   |
| 055  | scripts/ triage: inventory → operator-approved delete (269 files)       | P3       | S–M    | —          | DONE (APPROVE 2026-07-21; branch `advisor/055-scripts-triage`, commits `a87be597` (inventory) + `b69e78a8` (deletions), worktree `.worktrees/055-scripts-triage`. Operator approved the 76-file DELETE list (user: "approve" 2026-07-21); reviewer verified the commit = exactly 76 D + 1 M (README), all 16 externally-referenced KEEPs present post-delete, `ls scripts` = 194. Gates green: reference-scan primary (one benign stale comment in `seam_check.py`), `npm run check` secondary. STILL OPEN: 107 ASK files, 9 grouped family questions in scripts/README.md — per-family operator decisions pending. Repoint later: qa-csp ROUTES lists dead /claw; qa-demo-spin/qa-claw-e2e target dead routes) |
| 056  | DESIGN SPIKE: Weekly-Challenge settlement engine                        | P2       | M      | —          | DONE (APPROVE 2026-07-21; worktree `agent-a0c3d9912dea85cad`, commit `f592f325` — new `docs/superpowers/specs/2026-07-20-challenge-settlement-design.md`, 320 lines, all 9 sections + sources + appendix; reviewer verified structure + spot-checked citations (reason enum = 11 values confirmed; `challenge_payout` would be the 12th, all four plan-005 mirror surfaces named). 4 operator open questions in §9 — ANSWER BEFORE building plan 057. STOP-greps clean: reward-box `drawDailyBox` correctly distinguished (the old `settleRewardDraw` was deleted by Task 7; comment-only hits), no existing challenge settlement)                                                                              |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

### Round-6 dependency & sequencing notes

- **047 after 044** (both edit `challenge-validate.ts`); **054 after 044+047**
  (service.ts/validator neighbors — 054 is a move-only refactor and must
  rebase over them, never the reverse).
- **056 can start any time** (doc-only) but its design ASSUMES 044's caps and
  047's payout-field retirement; its output becomes plans 057+ after operator
  sign-off.
- **050 Step 1 needs operator input** (live `doctl` resource names); Steps
  2–4 complete regardless (DONE-partial allowed).
- **055 has a hard human gate** — inventory first, deletions only after
  explicit approval.
- All others independent; recommended first three: **044, 045, 046**
  (security caps, the hottest-path index, the money-display rollback).

### Round-6 findings table (vetted)

Ordered by leverage. Confidence is the advisor's after re-reading the code.

| #   | Finding                                                                                                                                                                                                                                                                      | Category  | Impact                          | Effort | Risk | Confidence              | Plan |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------------------------------- | ------ | ---- | ----------------------- | ---- |
| 1   | `voucher_amount` (`vip-levels-validate.ts:47`) + `reward_credits`/`payout_credits` (`challenge-validate.ts:64,116`) accept any value ≥0; `voucher-ranges.ts:11` caps the SAME column at `MAX_VOUCHER_MYR=10_000` and says an admin write needs the bound.                    | security  | MED-HIGH (latent minting lever) | S      | LOW  | HIGH                    | 044  |
| 2   | `/admin/vip-levels`, `/admin/challenge/stages`, `/admin/challenge/settings` absent from the `adminActionRateLimit` block (`middlewares.ts:519-596`) — every sibling economy mutation shares that budget.                                                                     | security  | MED                             | S      | LOW  | HIGH                    | 044  |
| 3   | `notification_read`'s only index is the composite unique `(notification_id, customer_id)`; the true-unread COUNT filters `customer_id` alone (`store/notifications/route.ts:63`) → full scan, fired by the bell every navigation+focus. Core-table index unverified.         | perf      | MED-HIGH (grows globally)       | S      | LOW  | HIGH                    | 045  |
| 4   | `update-card.ts` commits the Card then mirrors to Product with zero try/catch — mirror throw = Card↔Product divergence + orphaned slab; `create-card.ts:297-306` hand-rolls exactly this rollback; failure path untested.                                                    | bug       | MED (silent admin corruption)   | S–M    | LOW  | MED-HIGH                | 046  |
| 5   | Admin challenge/VIP tab buffers die on tab switch (Radix unmounts `Tabs.Content`, no `forceMount`; seed-once guard at `challenge/page.tsx:120`); + retired `payout_credits`/`payout_card_ids` still accepted end-to-end though the store route declares them dead.           | bug       | MED (operator data loss)        | S–M    | LOW  | HIGH                    | 047  |
| 6   | `card-management.spec.ts` needs `pw-test-card` (never seeded → always skips both lifecycle specs); `rewards.spec.ts` needs `REWARDS_REDEMPTION_ENABLED` (never set in e2e.yml → claim path never runs).                                                                      | tests     | MED-HIGH (dark coverage)        | S–M    | LOW  | HIGH                    | 048  |
| 7   | Modules-tier testMatch re-runs all 43 `*.unit.spec.ts` in the DB-backed `--runInBand` job (`jest.config.js:31,33`); `integration-http` (the 8-shard critical path) lacks the `Cache turbo` step its 3 siblings have.                                                         | tests/dx  | MED                             | S      | LOW  | HIGH                    | 049  |
| 8   | Docs vs reality ×4: `.do/backend.app.yaml` says `polycards-pg/valkey/media`, both runbooks say the rename never ran; runbook §1.3 lists the already-fenced vendor self-reg (`middlewares.ts:136,193`) as open+BLOCKING; CHANGELOG = template era; redesign doc header false. | docs      | MED (operator hazard)           | S      | LOW  | HIGH (infra half MED)   | 050  |
| 9   | Mark-all-read counts the current page only (`NotificationsClient.tsx:24`) and its comment claims the server total is page-scoped — backend documents TRUE total (`route.ts:22`); + `use-sound.ts:59-67` pool effect has no cleanup (bigwin bleeds past unmount).             | bug       | LOW-MED                         | S      | LOW  | HIGH                    | 051  |
| 10  | `notifyFeed` swallow-and-log try/catch copy-pasted across 7 producer sites; topup's variant doesn't even log (contradicts #222's intent).                                                                                                                                    | tech-debt | LOW-MED (recurring)             | S      | LOW  | HIGH                    | 052  |
| 11  | Google OAuth server actions (`auth.ts:212,246`) + callback route: zero coverage; the harness (`auth.test.ts` sdk mocks) already exists.                                                                                                                                      | tests     | MED                             | M      | LOW  | HIGH                    | 053  |
| 12  | `service.ts` 4,425 → 5,096 (+671 in one round — 3.5× the prior max): the whole challenge slice + 3 one-shot backfills landed in the god object; slice is self-contained (own models/validators) = the natural first extraction seam.                                         | tech-debt | MED                             | M      | MED  | HIGH                    | 054  |
| 13  | `scripts/` = 269 files; ~130 reference deleted entities (claw/repacks/pixelslot/pokenic); ~9 more one-offs added this round.                                                                                                                                                 | tech-debt | LOW (nav noise + trap)          | S–M    | LOW  | MED (split needs owner) | 055  |

### Round-6 findings vetted but NOT planned (ask for a plan if wanted)

- **Wallet badge/bar dual-field coupling** (`wallet/page.tsx:32-34` derives
  `gateOpen` from `remaining` but the bar from `used/deposited`): can't fire
  under the current backend contract (`remaining = max(0, deposited-used)` by
  construction). LOW smell; fold into any future wallet-page touch.
- **`ToastPolicy`/`policy` scaffolding** (`src/lib/notifications/copy.ts:21`)
  defined+tested, read by zero runtime code — deliberate PR2 scaffolding (the
  toast-system plan exists in docs/superpowers). Keep, but if PR2 dies, delete.
- **Batch-count guard untested** (`roll-pack-batch.ts:22-27`, self-documented
  in its spec) — S; fold into the next roll-pack touch.
- **Back-nav read-state UI probe is manual-only** (#226 probes aren't wired
  into any suite; API read-state IS covered) — M if wanted as a Playwright spec.
- **`/bank-withdrawal` copy overlap** with the #227 wallet gate panel — both
  honest; consolidation (or pointing Me's "Withdraw" pill at `/wallet`) is a
  product call.
- **Prism slab-frame wiring** (paid asset `public/images/slab-frames/prism.webp`,
  one `FRAME_TIERS` entry away) — direction item DIR-F; wire when a prism tier
  gets a product decision (unlock rung needed).
- **PERF-02 core-notification-table index** — folded into plan 045 as the
  investigate half, listed here so the "verify" nature is on record.

### Round-6 corrected / considered and rejected

- **`/bank-withdrawal` "orphaned route" claim — REFUTED during vetting**: it
  is linked from `src/app/(account)/me/page.tsx:293` (the "Withdraw" pill).
  Live surface, not dead code; only the copy-overlap note above survives.
- **"PayoutTab still titled Week & Payout" — partially stale**: the trigger
  label already says "Week & Reset" and the form has no payout inputs; what
  remains is the internal component name, a stale section comment, and the
  still-accepted payout patch fields (all folded into plan 047).
- **Weekly Challenge vs leaderboard divergence — cleared**: both
  `GET /store/challenge` and weekly `GET /store/leaderboard` call the same
  `challengeWeekTop` aggregation; board and any future payout can't disagree.
- **Notification producers on money paths — cleared** (perf): every
  `notifyFeed` fires after commit, best-effort; no round-trips inside ledger
  transactions.
- **#227 wallet redesign — cleared** (perf): `walletSummary` reuses
  `creditSummary`'s single scan via `precomputed`; the round-4/5 diet held.
- **CI shard divisor — cleared**: `run-http-shards.mjs` forwards `--shard=N/8`
  straight to jest; matrix has 8 entries; the #210 trap is handled.
- **Slab pipeline — cleared** (perf): baked once at save time, stored on CDN;
  no per-request image work. Tier-frame overlay = 6 cached webps + static
  box-shadow.
- **Notifications routes — cleared** (security): owner-scoped via bearer token,
  fail-closed 404, no IDOR; OAuth callback-host allowlist sound; reset-token
  prod logging fixed (#173) and Resend error path leaks nothing; #186 decode
  bounds run header-first before full decode; plan-010 metadata guard holds on
  the profile update paths.
- **Deps — cleared**: root audit 0 high (2 known moderates under next);
  backend `yarn npm audit` no high suggestions; multer dedupe held at 2.2.0;
  new deps (react-easy-crop, @medusajs/auth-google, resend) all justified;
  #182 audio and #197 liquid-glass added ZERO deps (Web Audio + CSS).
- **8 new migrations — cleared**: additive, idempotent, nullable columns with
  COALESCE readers, monotonic timestamps, correct down()s.
- **CODEBUDDY.md / QODER.md / .cursorrules** — byte-identical deliberate
  multi-IDE mirrors of the code-review-graph rule (#217). Not stale.
- **Sentry configs** — real conditional wiring (`enabled: Boolean(dsn)`), not
  placeholder.
- **debit-at-press (#200/#204) + enrichment-failure serving (#201)** — read
  clean: charge/settle identity guards, idempotent `handleSettled`, watchdog
  paths converge; paid rolls served on enrichment failure covered single+batch.
- **#219 route deletions** — complete; zero dangling links/imports/sitemap
  entries.
- **E2E press-debit guard (#204), #227 basis specs, VIP CRUD spec (#208)** —
  verified meaningful assertions (not vacuous), incl. anti-vacuous IDOR
  posture from #221.

### Round-6 Direction (options for the maintainer — not bugs)

1. **DIR-A — Weekly-Challenge settlement engine** → selected as plan 056
   (design spike). The runbook's BLOCKING go-live item; all data
   prerequisites exist.
2. **DIR-B — Cash-out writer + admin payout surface**: every internal
   prerequisite now landed (#224 backfill, open_id provenance, honest wallet
   gate); writer + admin approval = M, PSP/KYC = the L/HIGH long pole. The
   S-size compliance prerequisite: disclose the 100%-playthrough term in
   `TopUpSheet.tsx` at deposit time (still absent). Sequence the spike after
   056's build starts, or parallel — operator call.
3. **DIR-C — Rewards-economy launch spike**: gates, caps (036/044), CI nets,
   and admin editors all exist; remaining work is product parameters + a
   rollout runbook. M. Sequence AFTER the challenge settles cleanly (don't
   launch two reward systems half-settled).
4. **DIR-D — Operator config flips before launch** (checklist, not plans):
   Resend env vars at APP level (subscribers run on the worker); publish the
   Google OAuth consent screen (still Testing mode). Both are silent auth
   dead-ends at go-live. Also: prod challenge stage thresholds are
   demo-sized (runbook §1.2); prod external-basis backfill script run state.
5. **DIR-E — Notification preferences** (create-without-control asymmetry):
   no repo evidence of user demand — investigate-only. The registry makes
   `challenge_stage`/`cashout` templates S each when their features land.
6. **DIR-F — Wire the paid prism frame** (`prism.webp` → one `FRAME_TIERS`
   entry + an unlock rung): S, needs the product decision first.
7. **Stale prior-round item**: "6 hero loops never wired" is FALSE as of the
   delta — `HeroBoard.tsx:50` plays `shop-night`; `factoryVideo()` feeds the
   pack-detail loops. Dropped from the roadmap.

### Round-6 coverage — what was NOT audited

- **No runtime exercise** — every verdict is a static code-read (plan
  verification commands are the executors' runtime proof).
- Live DigitalOcean resource names (the crux of finding 8's infra half —
  plan 050 Step 1 is the operator tie-break).
- Admin SPA browser walk; vendored/framework internals beyond what findings
  required; `backend/apps/vendor` (dormant, fence verified only);
  `scripts/` file bodies (classified by name/pattern — plan 055 opens them);
  i18n depth; `.claude/` hook scripts.
- The ~570 test files outside the delta (scanned, not line-read).

---

## Round 7 — storefront design / UX audit, 2026-07-22

Base commit `a15f88e1`. Four parallel read-only audits (global tokens + app
shell, home/marketing, conversion funnel, account cluster). Scope was
deliberately disjoint from rounds 1-6: design system, shipped UI, and
user-facing copy truth only — no money, security, or backend correctness.

Output is a single ranked findings doc, not per-item plans:

| Doc | Title                                  | Items         | Status             |
| --- | -------------------------------------- | ------------- | ------------------ |
| 057 | Storefront design / UX audit (round 7) | 42 (6 tier-1) | TODO — unscheduled |

Tier 1 (5 active; the sixth, a claimed dead `--font-geist-sans` / body-font
regression, was RETRACTED on review as a false finding): buyback rate stated
three ways, VRF vs commit-reveal contradiction, spin-screen freeze on Server
Action transport rejection, fabricated live status on /contact, referrals
dead-end for handle-less users. Tier 1 plus the boundary/a11y/money-path work
shipped in PR #249; see doc 057's "Review corrections" for the full state.

---

## Round 8 — delta audit (PRs #249–#313), 2026-08-01

Generated by the `improve` skill (standard tier) against commit `a993f34a`
(master). Scope: the delta since round 7's `a15f88e1` — 53 commits / 586
files / +50,544/−13,056 (POLYCARD-BACK epics 1–5, challenge auto-payout #296,
odds work #293/#274/#298/#305/#310, suspension #294, operator QA
batches #305/#311, Meta Pixel #259, dep sweep #275–#288) — plus reconciliation of all
open plans. Method: four parallel read-only audits (backend money+security,
storefront correctness+trust, tests/CI/perf, debt/deps/docs/direction), every
table-bound finding re-opened and confirmed by the advisor against live code.
Same caveat as rounds 2–6: **static code-read, no runtime exercise.**
Non-interactive run: plans written for the top findings by leverage (expanded
past the 3–5 default per the round-4 precedent — this operator executes every
round).

**Headline.** The delta's engineering held up well where it mattered most —
the settlement job is genuinely idempotent with per-winner transactions (it
did NOT repeat the matureDueCommissions lock mistake), the money-input caps
recurred correctly on the new admin routes, xlsx export is injection-safe,
PriceCharting import inherited its caps, and both dependency trees audit
0-high. The two round-8 stories: **(1) process — seven executed, reviewed,
approved round-6 plans never merged**; their commits sit orphaned in the
object DB while this round's audit independently re-found four of them as
live defects (unread-count index, update-card rollback, dark e2e specs, stale
docs). **(2) The challenge payout is invisible to the operator's own ledger**
— the `WP` transaction type exists end-to-end but `settleChallengeWinner`
never writes it, so every real payout since #296 moves balance the
Transactions page cannot show.

### Round-6 merge-state reconciliation (content-verified against master)

| Round-6 plan             | State in master                                                                                               | Disposition                                                    |
| ------------------------ | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| 044, 047                 | MERGED (squashed into PR #247)                                                                                | rows stand                                                     |
| 053                      | MERGED (auth.test.ts carries the decoy-PII fixtures)                                                          | row stands                                                     |
| 045 unread index         | ABSENT (`notification-read.ts` has only the composite unique)                                                 | recover via **plan 059**                                       |
| 046 update-card rollback | ABSENT (no catch in `update-card.ts`)                                                                         | recover via **plan 059**                                       |
| 048 dark e2e seeds       | ABSENT (`pw-test-card` unseeded; specs still skip)                                                            | recover via **plan 059**                                       |
| 049 CI dedupe/cache      | ABSENT (`Cache turbo` ×3; modules tier still re-runs unit specs)                                              | recover via **plan 059**                                       |
| 050 docs truth           | ABSENT (runbook/CHANGELOG stale)                                                                              | SUPERSEDED by **plan 068** (fresh content; do not cherry-pick) |
| 051 unread total / SFX   | ABSENT (no `serverTotal`; client comment contradicts the backend contract)                                    | recover via **plan 059**                                       |
| 052 notifyFeedNonfatal   | ABSENT (zero references)                                                                                      | recover via **plan 059**                                       |
| 055 scripts triage       | Partially re-done by #303/#304; **ASK inventory (107 files, 9 family questions) lost** from scripts/README.md | ASK-table recovery folded into the not-planned list below      |
| 056 settlement spike     | Branch spec never merged; #297 wrote a fresh design and #296 built it                                         | SUPERSEDED — mark DONE-superseded                              |

Root cause: round 6 ended without an `advisor/round6-execution` rollup PR
(rounds 2/4 had one). **Standing recommendation: every execution round ends
with a rollup branch + PR, and DONE means merged.**

Also reconciled: **plan 054** (extract the challenge slice) remains TODO and
its target grew — `service.ts` is now 6,899 lines (5,096 at round 6); the
round-8 debt audit independently recommended exactly 054's slice (challenge
methods + anchor constants, the newest and best-specced code). Refresh 054's
line numbers before executing. **Plan 058** (POLYCARD-BACK): epics 1–5 all
shipped (#270 orders, #271 players, #274 odds sets, #292 ledger, #289
inventory+purchasing); §6 referral redo is **cancelled** (per the
`admin/ledger/route.ts` comment) — mark 058 DONE-except-§6-cancelled.

### Round-8 plans

| Plan | Title                                                                      | Priority | Effort | Depends on    | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ---- | -------------------------------------------------------------------------- | -------- | ------ | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| 059  | Recover the executed-but-never-merged round-6 fixes into master            | P1       | M      | — (run 1st)   | DONE (APPROVE 2026-08-01 after 1 revision; branch `advisor/059-recover-round6`, 14 commits `60edd77f..4b70d5f7`, worktree `agent-acb2996c138d7c490`. 12 orphan SHAs cherry-picked (1 real conflict — the 052 delivery producer had moved to `delivery-orders/notify.ts`; resolved by intent at the new location) + 2 authorized additions (settle-challenge-week notifyFeedNonfatal conversion; revision commit hardening the card-management skip into a hard assertion). Reviewer re-ran done-criteria greps (Cache turbo=4, pw-test-card seeded, notifyFeedNonfatal live, customer_id index present) + read the notify/settle/update-card/guard diffs. Executor gates: storefront check + 385/385, backend typecheck + unit 1060/1061 (1 pre-existing skip). Accepted exemption: `scripts/seed-notification-probe.ts` keeps bare `notifyFeed` (deliberately-throwing dev fixture). Modules tier not re-run (shared DB; covered by CI once merged). Round-6 rows 045/046/048/049/051/052 recoverable→recovered. NOT MERGED — operator decision)                                                                                                                                                                                      |     |
| 060  | Write the WP ledger row when a challenge winner is settled                 | P1       | S      | —             | DONE (APPROVE 2026-08-01; branch `advisor/060-wp-ledger-row`, commit `7ea4f522`, worktree `agent-a6d3a5e723bac3c3a`. Reviewer re-ran challenge-settle 16/16 on the shared DB, read the diff + all 3 new WP cases (replay case genuinely re-enters recordLedgerEntry by clearing the payout gate — anti-vacuous). Accepted documented judgment calls: `stage` = max unlocked stage (no per-winner stage exists in RankPayout), `value: 0` (snapshot carries no FMV, per plan fallback), `vaultDelta: null` (TP precedent). NOT MERGED — operator decision)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 061  | Rate-limit the 3 unthrottled admin mutation routes + coverage guard test   | P1       | S      | —             | DONE (APPROVE 2026-08-02 after 1 revision; branch `advisor/061-admin-ratelimit-sweep`, 3 commits `7fbcdf23`+`d73e9bc5`+`c352dd06`, worktree `agent-af6993bb938346a1c`. Final posture: limiter on exactly 5 economy-mutation routes (purchase-invoices, delivery bulk, pack reorder, packs/_/odds, packs/_/members — reviewer verified the final diff); catalog/media CRUD (cards, cards/_, from-pricecharting, pixel-pokemon, media, packs create, top-hits) EXEMPT with reasons — the revision reversed the executor's wider sweep because #299 bulk import and #305 bulk retier loop per-row client-side and would 429 on the shared 30/10s+200/60s budget, and round 4 recorded the only-money-mutations convention. Coverage-guard spec (4 assertions incl. no-double-match) re-run green by reviewer; red-green proven twice by executor (learned: block-commenting a matcher does NOT trip the text-scan guard — deletion does); the guard even caught the executor's own mid-revision EXEMPT omission. 2 wildcard-overlap routes (delivery-orders/[id], packs/[slug]) EXEMPT pending a scoped follow-up (path-to-regexp `_`spans`/`, empirically verified; RoutesSorter half source-read only). NOT MERGED — operator decision) |     |
| 062  | Challenge/leaderboard display truth (podium tile, All-Time caption, pool)  | P2       | S–M    | —             | DONE (APPROVE 2026-08-01; branch `advisor/062-display-truth`, 4 commits `c50b34d3..84a0b810`, worktree `agent-a4549652dd6b1795d`. Reviewer re-ran 382/382 + typecheck, read all diffs + both new challenge cases. Accepted documented deviations: `src/lib/data/packs.ts` touched (forced by Step 5's own "and the parse site" — the plan's scope list was incomplete, not executor creep); modal renamed "All cards" (correct — it now lists every tier). Executor-disclosed residuals: zero-Rare+ packs still render no pool listing (topPool gate); malformed-but-non-null published_odds now parses to `{tiers:{}}` instead of null (unreachable via admin editor). NOT MERGED — operator decision)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 063  | Pixel off /reset-password; consent withdrawal; OAuth callback fail-closed  | P2       | S      | —             | DONE (APPROVE 2026-08-01 after 1 revision; branch `advisor/063-privacy-auth-hardening`, commits `7771182d`+`e37db7bf`+`e16d7a44`+`49aead30`, worktree `agent-aca295c7512dcd89d`. Revision restored `localhost:3000` (Google-registered next-dev origin) alongside the new `:4000` entries + a positive test; reviewer re-ran 386/386 + read the final allowlist and callback diffs. Accepted deviation: `resolveCallbackOrigin` helper lives in allowed-hosts.ts because a Route Handler module may only export HTTP-method names (build-verified by executor). OPERATOR FOLLOW-UPS: register `http://localhost:4000/auth/google/callback` on the Google OAuth client if :4000-local login is wanted; `scripts/verify-phone-google.mjs` header comments now describe the deleted fallback (assertions still pass). NOT MERGED — operator decision)                                                                                                                                                                                                                                                                                                                                                                                     |
| 064  | Validate the delivery-address phone as E.164                               | P2       | S      | —             | DONE (APPROVE 2026-08-01; branch `advisor/064-address-phone-e164`, commit `095a4f93`, worktree `agent-aeb28c34574e9cf21`. Reviewer re-ran 384/384 + typecheck, read the diff + all 4 new cases (assert E.164 in the SDK body, exact error copy, SDK-not-called on reject). Accepted documented deviations: shared `validateAddressPhone` helper; form `onSubmit` reads phone from FormData (PhoneField exposes its value via hidden input only — the naive swap would have silently dropped it); `col-span-2` layout not visually verified — operator eyeball on next run. Legacy unnormalized values prefill blank; a blank re-save clears to null, which enables the validated profile-phone fallback (strictly better). NOT MERGED — operator decision)                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 065  | Reject purchase-invoice lines with unknown product handles (v2)            | P2       | M      | —             | DONE (APPROVE 2026-08-02; v1 executor STOPPED correctly — cards-only resolution would 400 legitimate PriceCharting-imported products (cost view keys on PRODUCT handles) and ~17 http fixtures posted unbacked handles; plan rewritten to v2 and re-dispatched. v2: worktree `agent-a4ac39aa09f1e7526`, branch `worktree-agent-a4ac39aa09f1e7526` (rename to `advisor/065-invoice-handle-check` at merge if desired), commits `8985a447` (fixtures, proven green against the UNMODIFIED route) + `3c3d747c` (product-handle existence gate + reversal carve-out via listPurchaseInvoiceLines union, backstopped by assertReversalCovered; untracked-branch warn; 2 new cases incl. deleted-product reversal). Reviewer read the full diff + re-ran purchase-invoices.spec 22/22; executor also ran inventory-buckets 3/3, neighbor regression 6/6, unit 24/24, typecheck. Known minor: multi-offender message join untested (single-offender case is). NOT MERGED — operator decision)                                                                                                                                                                                                                                                 |     |
| 066  | Inventory loader: scope the card read; bound the xlsx export               | P2       | S–M    | —             | DONE (APPROVE 2026-08-02; branch `advisor/066-inventory-read-diet`, commit `b117333c`, worktree `agent-a736d283912f81bd2`. Reviewer read the diff + re-ran inventory-export.spec 5/5 on the shared DB; executor ran all four inventory http suites (14 tests) at HEAD + full unit tier. Card read now scoped to `handles` with the empty-array guard; export capped via `INVENTORY_EXPORT_MAX_ROWS` (default 10000, error-not-truncate). Accepted documented deviation: cap read per-request instead of module-top (matches the rewardsRedemptionEnabled / COMMISSION_COOLDOWN_DAYS env pattern — required for the spec to drive both cap states through one booted app); `listCards` array-filter shape verified against real call sites (delivery-view.ts:44, service.ts:1817) since the plan's suggested exemplar turned out to be raw SQL. NOT MERGED — operator decision)                                                                                                                                                                                                                                                                                                                                                         |     |
| 067  | CI/test hygiene: wire CSP gate, cover close-instant, re-pair backend React | P2       | S–M    | 059 (e2e.yml) | DONE (APPROVE 2026-08-02; branch `advisor/067-ci-hygiene`, 4 commits `06f3119c..81617ea7`, BASED ON `advisor/059-recover-round6` @ `4b70d5f7` — merge 059 first. Worktree `agent-a23588250d1a7ee9c`. Reviewer re-ran close-instant 4/4 on the shared DB + read all diffs. Executor proofs: CSP gate run for real on :4010 incl. a bare-server negative control proving the new header assertion fires; react-dom 18.3.1 re-pinned with lock churn scoped to the pair (+scheduler, react-dom's own dep); dependabot semver-major ignore for react-dom; admin build verified via the PINNED tsc (global TS7 shadowing — known machine gotcha). Follow-up candidates recorded: symmetric dependabot ignore for `react` (blocks the mirror half-bump); admin/vendor workspaces have no check-types script (their tsc runs inside build). NOT MERGED — operator decision)                                                                                                                                                                                                                                                                                                                                                                   |     |
| 068  | Docs & naming truth round 5 (runbook, CHANGELOG, ADRs, turnover rename)    | P3       | S–M    | —             | DONE (APPROVE 2026-08-02 after 1 authorized revision; branch `advisor/068-docs-naming-truth`, 7 commits `4ed06102..052ceb8c`, worktree `agent-a92332800d644ea54`. Runbook §1 blockers struck with evidence; CHANGELOG deleted + version 1.0.0-rc; ADR 0003 (turnover basis) + ADR 0004 (suspension, review-by 2026-10-01); odds spec superseded-header; CONTEXT.md marker + VIP-Level definition fix; 2 SUSPENDED banners; `lifetimeExternalSen*`→`lifetimeTurnoverSen*` (reviewer git-grep: zero old-name matches); players i18n cluster relabeled to Turnover after the shared-key trace proved both consumers funding-blind. Reviewer verified the revision diff + ADR files + runbook greps. Executor STOP on en.json was correct per plan and resolved by authorization. Follow-ups flagged, not fixed: `.gitignore:104` stale CHANGELOG comment; runbook §5 known-good line stale; password-reset §1.3 reworded to the code-level gate — operator should confirm live prod env state (memory says verified 2026-07-27). Marks round-6 plan 050 SUPERSEDED. NOT MERGED — operator decision)                                                                                                                                       |     |

Sequencing: **059 first** (it edits `e2e.yml`, which 067 also touches, and it
closes four findings other plans would otherwise duplicate). 060/061 next
(money-record + security, both S). All others independent; 068 marks 050
superseded when it lands.

### Round-8 execution rollup — **MERGED to master 2026-08-02 as PR #314 (squash `0157f257`)**

Post-approval pipeline: rollup branch `advisor/round8-execution` (10 no-ff
merges, zero conflicts) → fresh-context code review ("With fixes": 0 Critical,
3 Important, 6 Minor — all resolved, incl. reordering the update-card rollback
and a clamped OAuth proto) → CI (one prettier fix on delivery.ts; `npm run
check` does NOT run format:check — the known trap) → two CodeRabbit review
rounds (11 threads total, all fixed-or-answered and resolved; its best catch:
the fail-closed OAuth redirect built from `request.nextUrl` recreated the #311
bind-origin bug — now a manual 302 with a relative Location) → merge state
CLEAN → squash-merged. Merged-tree gates: storefront check + 397/397; backend
typecheck + unit 1064/1065; full CI matrix green twice. WATCH: first nightly
must show card-management specs PASSING (not skipped) and the new CSP gate
green; DO auto-deploys master (the notification_read index migration applies
on deploy). Executor worktrees under `.claude/worktrees/` + the
`.worktrees/round8-rollup` husk remain for manual cleanup (locked-file
Windows chore, see worktree-removal memory).

> **2026-08-02 post-merge cleanup + merge-state verification:** the executor
> worktrees, the rollup husk, and all nine `advisor/*` branches are now
> DELETED. Before deletion, every branch tip was verified an ancestor of PR
> #314's head (`git log <pr-314-head>..<branch>` empty for each, and
> `git diff <pr-314-head> 0157f257` empty — the squash preserved the head
> tree), so the per-row "NOT MERGED" annotations below are settled: all ten
> plans ARE merged via `0157f257`. The branch names and commit ranges in the
> rows are historical; those commits are reachable only via GitHub's
> `refs/pull/314/head`. Audit caveat for future rounds: a
> `git merge-tree`-containment check against master reports phantom
> "unmerged" deltas for these branches, because the rollup evolved past the
> branch tips during review (renames, added guards, prettier). Ancestry
> against the merged PR head is the decisive test, not content containment.

Original pre-merge record follows:

All ten plans were executed by dispatched subagents in isolated worktrees and
reviewed by the advisor (scope + full diff + independently re-run gates).
Revisions: 059 (harden the e2e skip guard), 061 (narrow the limiter sweep off
the bulk-loop catalog routes), 063 (restore `localhost:3000` to the OAuth
allowlist); 065 was re-planned (v2) after a correct investigative STOP; 068's
STOP on the shared i18n key was resolved by authorization after the executor's
two-consumer trace.

**Merge order** (everything else is independent off master, zero-conflict
expected): `advisor/059-recover-round6` FIRST, then `advisor/067-ci-hygiene`
(based on 059's tip `4b70d5f7`). 060+068 both touch `service.ts` in disjoint
regions (settle path vs turnover rename) — merge in either order. Recommended:
integrate all ten on a rollup branch (`advisor/round8-execution`), re-run the
merged-tree gates, one PR — the round-6 loss happened precisely because no
rollup PR existed.

**Operator follow-ups from execution** (recorded in the per-plan rows too):
register `http://localhost:4000/auth/google/callback` on the Google OAuth
client if :4000-local Google login is wanted (063); confirm prod
password-reset env state before trusting the reworded runbook §1.3 (068);
watch the first post-merge NIGHTLY — card-management specs must PASS (not
skip), CSP gate green — and the first CI run — 049's modules-tier dedupe +
http turbo cache restore (059/067); consider the symmetric dependabot ignore
for `react` (067 note); scoped follow-up for the two wildcard-overlap admin
routes left EXEMPT from the limiter (061 note); ~10 executor worktrees under
`.claude/worktrees/` can be pruned after merge (see the worktree-removal
gotchas memory before deleting).

### Round-8 findings table (vetted)

Ordered by leverage. Confidence is the advisor's after re-reading the code.
Tags: `introduced` (this delta) / `pre-existing` (in touched files).

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Category     | Impact                                          | Effort | Risk    | Confidence      | Tag          | Plan |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ----------------------------------------------- | ------ | ------- | --------------- | ------------ | ---- |
| 1   | Seven approved round-6 plans (045/046/048/049/050/051/052) never merged; commits orphaned; four re-found as live defects this round.                                                                                                                                                                                                                                                                                                                                       | process      | HIGH (paid work lost)                           | M      | MED     | HIGH            | introduced   | 059  |
| 2   | `settleChallengeWinner` (`service.ts:~6161`) mints winner credits with no `WP` ledger row — the `WP` type exists end-to-end with no writer; `admin/ledger/route.ts:72-75` comment now false ("no challenge-settlement job exists").                                                                                                                                                                                                                                        | bug/money    | HIGH (record of truth)                          | S      | LOW     | HIGH            | introduced   | 060  |
| 3   | `/admin/purchase-invoices`, `/admin/delivery-orders/bulk`, `/admin/packs/reorder` POSTs absent from `adminActionRateLimit` (`middlewares.ts:547-661`) — 4th recurrence of the plan-004/015/044 class; 5 older siblings also unmatched.                                                                                                                                                                                                                                     | security     | MED                                             | S      | LOW     | HIGH            | introduced   | 061  |
| 4   | "Top 3 will receive" tile (`WeeklyChallenge.tsx:171`) lists cards from EVERY rank (`challenge.ts:289-297` has no rank filter; `credits` right below is rank-filtered) — with #296 paying for real, the storefront advertises rank-7 prizes to the podium.                                                                                                                                                                                                                  | bug/trust    | MED-HIGH                                        | S      | LOW     | HIGH            | introduced   | 062  |
| 5   | Delivery-address phone is a plain text field (`AddressesClient.tsx:350`); `addAddress`/`updateAddress` never call `normalizePhone`; a garbage value SUPPRESSES the validated profile-phone fallback (`request-delivery.ts:125` fires only when blank).                                                                                                                                                                                                                     | bug          | MED (fulfilment path)                           | S      | MED     | HIGH            | introduced   | 064  |
| 6   | Purchase-invoice `card_handle` validated as non-empty string only (`validate.ts:107-116`); adjust step silently `continue`s on no target — phantom cost basis + stock_movement, 201 success, no signal. Sibling route hard-fails unresolvable ids.                                                                                                                                                                                                                         | bug          | MED (cost-basis integrity)                      | S      | LOW-MED | HIGH            | introduced   | 065  |
| 7   | Meta Pixel mounts in the root layout (`layout.tsx:89`); on `/reset-password?token=…` a consented visitor's PageView beacon (full URL) races the client-side query scrub (`ResetPasswordClient.tsx:21-24`). Plus: no consent-withdrawal path exists (`setConsent` called only from the banner).                                                                                                                                                                             | security     | MED                                             | S      | LOW     | HIGH (race MED) | introduced   | 063  |
| 8   | Google OAuth callback falls back to `request.url` — the known-bad bind origin #311 fixed — when the forwarded host isn't allowlisted (`callback/route.ts:28-29`); allowlist names `localhost:3000`, a port this repo never serves (`allowed-hosts.ts:12-16`).                                                                                                                                                                                                              | bug/security | MED                                             | S      | LOW     | HIGH            | introduced   | 063  |
| 9   | `loadInventoryRows` reads the ENTIRE card table on every call incl. the single-item path (`inventory-view.ts:100`); xlsx export reuses it unbounded (`export.xlsx/route.ts:130`) while #299 pushes the catalog toward five figures. (List route's unpaged-by-design is settled and stays.)                                                                                                                                                                                 | perf         | MED (grows with catalog)                        | S–M    | MED     | HIGH            | introduced   | 066  |
| 10  | `qa:csp` runs in NO workflow while prod ENFORCES CSP and the delta changed the policy twice; the script passes vacuously when the CSP header is absent (`qa-csp.mjs:50-58`) — the #244 vacuous-gate class, unfixed for CSP.                                                                                                                                                                                                                                                | tests/dx     | MED                                             | S      | LOW     | HIGH            | pre-existing | 067  |
| 11  | `closeInstantWindow` write path (#265) has zero test references (route caps/dedup, owner scoping, idempotence) — only the rate MATH is covered (`buyback-rate.unit.spec.ts`).                                                                                                                                                                                                                                                                                              | tests        | MED (money-adjacent)                            | S      | LOW     | HIGH            | introduced   | 067  |
| 12  | `backend/package.json` declares react `^18.3.1` with react-dom `^19.2.8` (+ split @types) — Dependabot #279 bumped half a coupled pair; admin survives only via local hoisting.                                                                                                                                                                                                                                                                                            | deps         | MED (latent)                                    | S      | MED     | HIGH            | introduced   | 067  |
| 13  | Go-live runbook §1 lists two RESOLVED blockers as BLOCKING (settlement shipped #296; vendor self-reg fenced #173); CHANGELOG still describes the cloner template (frozen 2026-03); #254 turnover basis + #294 suspension have no ADR; odds spec superseded by #298 but marked Approved; CONTEXT.md teaches suspended vocabulary; 2 orphans missing SUSPENDED banners; `lifetimeExternalSenFor` now sums turnover but keeps the "external" name and an admin "Spend" label. | docs/naming  | MED (operator hazard + future money-basis trap) | S–M    | LOW     | HIGH            | introduced   | 068  |
| 14  | All-Time board caption claims "lifetime pulled value" while ranking stays spend-order (operator decision recorded at `LeaderboardClient.tsx:247-250` — the DISPLAY choice is settled; the caption + dead `points`/`overall` wire fields are not).                                                                                                                                                                                                                          | bug/copy     | LOW-MED                                         | S      | LOW     | HIGH            | introduced   | 062  |

### Round-8 vetted but NOT planned (ask for a plan if wanted)

- **Bulk "Remove from pack" silently skips the auto-reweight**
  (`set-pack-members.ts:187-218`): on a `computeSetWeights` error the step
  `console.warn`s, returns ordinary success, and collapses survivors'
  set-2/3 weights to NULL (the degraded path is deliberate and well-commented
  — fail-safe inheritance — but the OPERATOR gets no signal, and #310/#305
  made the path routine). S–M: thread the skip reason into the workflow
  response and render it in the odds/members editor. Pre-existing, made
  routine by the delta.
- **`save-pack-odds` reads existing odds with `take: 1000`**
  (`save-pack-odds.ts:67-70`) while the editor GET pages the full pool — on a
  pack with >1000 card rows the `sameSet` check can never pass and odds saves
  400 forever (fails closed, no corruption). Real functional ceiling, unlikely
  today; fold into the next odds touch.
- **Hourly settlement re-runs the whole-pull-table pool aggregate for weeks
  that settle nobody** (`service.ts:5966-6002` — both early returns exit
  AFTER the aggregate; `prior` never materializes). Bounded (1–2 aggregates
  ×168 ticks/week); reorder the cheap gate first when next in the file. The
  per-winner-gate comment at `:5935-5940` explains why a whole-week marker is
  subtle — read it first.
- **Spin-screen S fixes** (both pre-existing in delta-rewritten files):
  post-charge mapping failure leaves `builtOffers` shorter than `cards`
  (`SlotMachineClient.tsx:536-547` — pad with nulls before storing;
  null-offer path handled downstream), and toggling mute mid-spin re-arms the
  settle watchdog's full countdown (`SlotMachineClient.tsx:688-697` deps
  include `handleSettled` → `play` → `[muted]`; mirror through a ref like
  `RevealStage.tsx:146-149`).
- **scripts/ intake rule + lost ASK inventory**: the 055 triage's 107-file ASK
  table vanished when scripts/README.md was rewritten; recover it from
  `git show a87be597:scripts/README.md` into `scripts/TRIAGE.md`, and adopt
  the intake rule (PR-verification scripts live in the PR branch; only
  `package.json`/workflow-wired scripts land on master — the delta added ~25
  unreferenced one-offs while deleting 47).
- **Mark-all-read label still page-scoped** (round-6 051 residual): the
  current client comment (`NotificationsClient.tsx:120-122`) claims the
  server's `unread_count` is page-scoped; the backend documents TRUE total
  (`store/notifications/route.ts:23-26`). Plan 059 step 5 recovers the fix;
  if 059 skips it, this stands alone as S.
- **Footer/fairness reachability**: deliberate 2026-07-31 removal, consequence
  recorded in-code (`SiteFooter.tsx:20-31`) — surfaced as an explicit operator
  question in plan 068's maintenance notes, not a plan.

### Round-8 corrected / considered and rejected during vetting

- **"All-Time board ranks by a metric it doesn't display" — DOWNGRADED**: the
  spend-order-ranking + pulled-value-display split is an operator decision
  recorded in-code (`LeaderboardClient.tsx:247-250`, 2026-07-23). Only the
  caption and dead fields survive as findings (plan 062). Do not change the
  ORDER BY.
- **"CHANGELOG last touched with template content" — partially corrected**: it
  does carry one real `[Unreleased]` Node-24 entry; the substance (frozen
  since 2026-03, template-era body, ~300 PRs unrecorded) stands (plan 068).
- **"Inventory aggregates receive the full handle array on the single-item
  path" — corrected**: products and all five aggregates ARE handle-scoped;
  only the `listCards({})` read ignores scope (plan 066 narrowed accordingly).
- **Settlement job (#296) — CLEAN** on the concern that mattered: per-winner
  `@InjectTransactionManager` with per-winner advisory locks (never
  accumulated), notifications after commit, idempotency via payout-row check +
  `challenge:` reference + unique-index backstop, anchor-shift overlap guard;
  covered by a 522-line integration spec that CI's modules tier runs.
- **Also clean** (checked, do not re-file): xlsx formula injection
  (write-excel-file emits string cells); PriceCharting bulk import caps + SSRF
  posture (fixed upstream, cursor-only param, host-allowlisted images);
  unmasked feed #311 (first name only — masking rule holds); payout-details
  PII (last4-only audit, admin-only); disable-switch fail-closed incl.
  Google-minted bearers; odds-set roll parity (set resolved server-side from
  the customer group; per-card weights never exposed); odds-math edge guards;
  batch bounds on every new bulk endpoint; ledger route pagination + strict
  filters; #308 CDN rewrite complete (zero `pokenic-media` in src/ or
  public/); suspension #294 link hygiene (zero dangling hrefs; robots/sitemap
  correct; notification copy nulls suspended hrefs with delimiter-correct
  tests); Meta Pixel consent gating itself (null until accepted, no noscript
  beacon, CSP consistent); reel decoy cap #307 (cap and rest-sharp guard are
  the same inequality + boundary test); spin/reveal timer cleanup +
  double-conclude guard; `npm audit` 0 high (root) and yarn audit no
  suggestions (backend, all workspaces); dep bumps #275–#288 manifest/lock
  coherent; `.gitleaksignore` single documented synthetic-fixture waiver;
  admin Pager/format-mirror duplication hypotheses refuted (shared component;
  parity-tested mirror); storefront ledger-reason `z.string()` looseness is a
  documented deliberate design.
- **No prompt-injection content** found in the delta by any audit.

### Round-8 Direction (options for the maintainer — not bugs, coarse estimates)

1. **DIR-A — Build the manual cash-out writer.** The delta built the last
   missing piece: `player_payout_details` (bank destination "for MANUAL
   cashouts", `Migration20260728100001`, atomic setter + audit + 282-line
   spec). Gate (`withdrawable.ts:13-14`), ledger reason (`cashout`, written by
   nothing), P&L reader (`economy.ts:131`), customer label
   (`transactions.ts:17`) all exist. What's missing is ONLY the writer + an
   admin approve→execute surface. M (composition, not design); MED-HIGH risk
   (money) — own PR, own integration spec, and per plan 060's pattern the
   writer must emit its ledger row in the same locked transaction. The mock
   top-up exclusion stands: this is the MANUAL, admin-reviewed payout, not a
   PSP integration.
2. **DIR-B — Disclose the playthrough gate at deposit time.** Still absent
   from `TopUpSheet.tsx` (the only at-payment copy is the demo-checkout note);
   the wallet page discloses only after the money is in. Third consecutive
   round carrying this item; it is one line of copy + a link, and it is the
   compliance prerequisite for DIR-A. S; the conversion dip is the honest cost.
3. **DIR-C — Close the outbound half of the inventory ledger.** #289 defined
   the 7-kind `stock_movement` enum and ships only the `purchase` writer;
   delivery transitions deliberately touch "NEVER stock_movement"
   (`service.ts:6707`). Once shipping volume is real, on-hand and the movement
   log diverge with no COGS counterpart to the carefully-built weighted-average
   cost. M spike: map the 7 kinds to events, decide backfill-or-fresh, confirm
   COGS is actually wanted.
4. **DIR-D — Decide retire-or-restore on the suspended reward economy** by
   ADR 0004's review-by date (2026-10-01, set by plan 068). `PRODUCT.md:18`
   still claims the VIP/referral loop drives retention; the storefront 404s
   it; the backend keeps it fully built and tested. Restore = a revert;
   retire = a large deletion; the current ambiguity taxes every sweep and
   audit. S decision.
5. **DIR-E — Resolve the PRODUCT.md redesign fork.** `PRODUCT.md:44-46` still
   mandates a full mobile-first redesign and instructs DESIGN.md be starved
   until it starts — while the delta shipped substantial incremental design
   work (#257/#258/#295/#249) on surfaces a redesign would replace. Either
   re-date PRODUCT.md to bless the incremental path (and unfreeze DESIGN.md)
   or scope the redesign. S decision, L if the redesign is confirmed.

### Round-8 coverage — what was NOT audited

- **No runtime exercise** — every verdict is a static code-read; plan
  verification commands are the executors' runtime proof.
- Files outside the `a15f88e1..HEAD` delta (rounds 1–7 own that record; their
  rejected-findings lists were trusted, not re-audited).
- Admin SPA browser behavior (static reads only); vendored/framework
  internals beyond what findings required; `backend/apps/vendor` (dormant);
  live DO resource state; i18n depth; `.claude/` hook scripts.
- The ~570 pre-delta test files (scanned for delta interactions only).
- `docs/research/` gitignored reference specs (local-only).

---

## Round 9 — whole-repo code-quality audit, 2026-08-02

Generated by the `improve` skill (standard tier) against commit `0f14da91`
(master). Scope: whole-repo code quality (the user's ask), the unaudited
micro-delta since round 8's base (`a993f34a..HEAD` = the #314 rollup itself,
already advisor-reviewed, plus PRs #320–#323's pool-dialog/QA work and #324's
docs), and **live verification of round 8's WATCH items**. Method: four
parallel read-only audits (backend + admin correctness/debt, storefront
correctness/debt, tests/CI/DX with live `gh` evidence, cross-cutting
debt/deps/docs), every table-bound finding re-opened and confirmed by the
advisor against the source. Unlike rounds 2–8, the tests/CI lane examined
**live CI/nightly runs**, not just code. Non-interactive run: plans written
for all vetted findings per the round-4/8 precedent (this operator executes
every round).

### Round-8 WATCH items — verified with live run evidence

- **CSP gate**: ran GREEN on the first post-#314 nightly (run 30714035144)
  and is genuinely fail-closed (`qa-csp.mjs` fails on non-2xx, missing
  header, unrestricted policy, enforce→report-only downgrade). Closed.
- **Modules-tier dedupe (plan 049)**: LIVE — 24 suites, zero
  `*.unit.spec.ts`; unit tier owns them (99 suites / 1066 tests). Closed.
- **Turbo cache in integration-http**: restore step present in all shards;
  payoff not shown on the first run (lockfile-changing commit = designed
  cache miss). Closed as wired.
- **card-management specs**: the skip→hard-fail conversion WORKED (no longer
  skips) — but the lifecycle spec now genuinely FAILS at `:117`
  (`/slots/pokemon-rookie` missing "cards in this pack") → folded into
  plan 070's triage; the cleanup spec still self-skips → plan 069.
- **react/react-dom re-pair + symmetric dependabot ignore (067 follow-up)**:
  CONFIRMED closed (`backend/package.json` `^18.3.1` pair;
  `.github/dependabot.yml` carries both ignores).
- **Runbook §5 known-good staleness (068 follow-up)**: CONFIRMED fixed.

### Round-9 findings table (vetted — every citation re-read by the advisor)

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Category   | Impact                      | Effort | Risk    | Confidence                                                    | Plan |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------- | ------ | ------- | ------------------------------------------------------------- | ---- |
| 1   | PR #311 added a required signup phone field; the e2e helper never fills it → the flagship customer money-path spec (`customer.spec.ts`) has timed out every nightly since 2026-08-01. Compounding: the signup retry budget (200s) exceeds the 180s test timeout, so the diagnostic throw can never fire.                                                                                                                                                 | tests      | HIGH (money-path dark)      | S      | LOW     | HIGH (live-run + code)                                        | 069  |
| 2   | Nightly E2E red 14 of 15 nights — two chronic failures (`admin.spec.ts:94` pulls-ledger table never visible; `odds-reflection.spec.ts:80` "Save win rates" stays disabled) + the a11y gate now red (real `/leaderboard` 4.17:1 contrast; `/about` hits likely measured mid fade-in). A gate that is always red is off.                                                                                                                                   | tests/dx   | HIGH (no signal)            | M      | LOW     | HIGH                                                          | 070  |
| 3   | Four offset-paginated admin lists lack the unique `id` tiebreaker five siblings carry (`admin/pulls`, `customers/[id]/pulls` on `rolled_at`; transactions, delivery-orders on `created_at`) — batch opens stamp identical `rolled_at`, so ties are the norm and pages can drop/duplicate rows. Plus `?q=` drift: ledger's `coerceQ` feeds ILIKE unescaped (`?q=%` = whole table reads as filtered); purchase-invoices/players silently drop array `?q=`. | bug        | MED (operator data views)   | S–M    | LOW     | HIGH (both `rolled_at` sites; MED the two `created_at` sites) | 071  |
| 4   | `buyback-pull` re-reads the pull "right before pricing" but uses the fresh read only for `instant_closed_at` — `revealed_at` stays stale, so a reveal ping landing in the multi-await gap collapses the instant window and under-credits vs the quote shown. Latent (narrow race), one-line fix.                                                                                                                                                         | bug/money  | MED (quote-credit parity)   | S      | LOW     | HIGH asymmetry / MED reach                                    | 072  |
| 5   | Pack-detail surface: desktop catalog's `?count=N` has no reader (dead stepper both ends — CORRECTS plan 057 #29's "desktop works" claim); sibling-pack switch seeds the live poll with the URL pack's snapshot and a failed refetch strands the WRONG pack's pool/odds permanently; PoolByRarity derives its label 4×; `useDragScroll` misses pointercancel; `qa-pool-modal.mjs` always exits 0 and its zero-Rare skip accepts any empty rail.           | bug        | MED-HIGH (funnel surface)   | M      | LOW-MED | HIGH                                                          | 073  |
| 6   | ~9 storefront mutation handlers await server actions with the busy flag set outside try/finally — a transport failure (offline, 502, post-deploy action-id rotation) strands "Saving…" forever with no message; NotificationsClient additionally keeps optimistic read state with no throw-path rollback. The correct shape exists in the same files.                                                                                                    | bug        | MED (recovery = reload)     | M      | LOW     | HIGH                                                          | 074  |
| 7   | `useChromeInert` writes `body.style.overflow` directly, corrupting the refcounted lock `use-modal-a11y` maintains (its comment documents per-modal capture as the bug it replaced): spin screen + odds sheet interleaving can strand the site unscrollable.                                                                                                                                                                                              | bug        | MED                         | S      | LOW     | HIGH corruption / MED strand                                  | 075  |
| 8   | Admin challenge/VIP inline validators omit the server money ceilings (`MAX_VOUCHER_MYR`, `MAX_THRESHOLD_MYR`) their own headers claim to mirror; 3 stale comments actively misinform (one denies plan 044's shipped cap, one denies plan 060's live WP writer, one cites a deleted tab); one raw money render bypasses `rm()`.                                                                                                                           | bug/debt   | LOW-MED (operator UX)       | S      | LOW     | HIGH                                                          | 076  |
| 9   | `/store/challenge` still emits `rewardCardIds`/`rewardCredits` under a comment claiming a storefront consumer that no longer exists (`rewardCredits` is a max, not a sum — a wrong number if re-adopted); the 7-template notification list lives in 3 hand copies with a parity test that never reaches the backend copy.                                                                                                                                | debt/tests | MED (seam integrity)        | S      | LOW-MED | HIGH                                                          | 077  |
| 10  | `apps/admin` declares 4 deps but imports 7 more (101 sites) resolved only by hoisting — the React-18 constraint the repo defends (#279 incident) is expressed in a manifest Dependabot's guard doesn't watch.                                                                                                                                                                                                                                            | deps       | MED (latent)                | S      | LOW-MED | HIGH                                                          | 078  |
| 11  | Docs/agent-config drift: scripts/ re-accreting (23 unreferenced additions since the 055 triage; intake policy lost), `sync-agent-rules.sh` claims pointer files that don't exist or carry wrong content, `.gitignore` promises a CHANGELOG that doesn't exist, and the tracked CODEBUDDY/QODER/.cursorrules triplet has no generator.                                                                                                                    | docs/dx    | MED (operator/agent hazard) | S–M    | LOW     | HIGH                                                          | 079  |

### Round-9 plans

| Plan | Title                                                                 | Priority | Effort | Depends on                                 | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---- | --------------------------------------------------------------------- | -------- | ------ | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 069  | Restore the flagship customer money-path E2E (signup phone helper)    | P1       | S      | —                                          | DONE (APPROVE 2026-08-02; branch `advisor/069-e2e-signup-phone`, commit `bd995d7f`, worktree `agent-aaacb5079cddbb8c3`. Reviewer re-ran: `--list` 21 tests/11 files, typecheck, lifecycleRan grep clean, scope exactly 2 files. Executor proofs: `0123456789` validated for MY via libphonenumber; page-wide `form input[required]` guard safe (AuthForm is the only form on the pack page). RUNTIME PROOF PENDING: first nightly must show customer.spec.ts PASSING, not timing out. NOT MERGED)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 070  | Nightly E2E signal triage (2 chronic specs + a11y gate)               | P1       | M      | — (coord. 069: same suite, disjoint files) | DONE-with-caveat (APPROVE 2026-08-02; commits `b0ab299a`+`201b3b2a` on worktree branch `worktree-agent-a108cf6e245c85510` (rename to `advisor/070-nightly-triage` at merge if desired), worktree `agent-a108cf6e245c85510`. Steps 1/2/4 complete, Step 3 hit its own STOP correctly. Diagnoses (trace-artifact substitute: gh run view --log-failed — see the artifact gap below): the /pulls spec was testing a route RETIRED in #271 — re-anchored via Support→View 360→Pulls tab, same assertion; odds helper broken by the tier-budget model (unlocked non-Common rows pin to their tier share → preview.error disables Save; no dirty-tracking exists) — now locks every non-target at explicit 0% matching the sibling API test; a11y scan runs reducedMotion at context level; leaderboard header -500→-400 (3.78:1→6.94:1, measured via the script own oklch resolver). Reviewer re-ran --list + read all 4 diffs + verified both /about citations. CAVEATS: (1) a11y gate stays RED — full-coverage scan surfaced 2 REAL previously-invisible /about violations (axe skips opacity-0 nodes; the old gate under-scanned) — design calls, listed under vetted-not-planned; (2) admin-spec fixes + leaderboard fix runtime-proven only by the first nightly (backend cannot boot in a worktree); (3) /leaderboard local OK is vacuous (no rows without a backend) — fix is measurement-grounded. NOT MERGED) |
| 071  | Admin list-query hardening (pagination tiebreakers + honest `?q=`)    | P2       | S–M    | —                                          | DONE (APPROVE 2026-08-03; branch `advisor/071-admin-list-hardening`, commit `93119c3c`, worktree `agent-af795ac4c105b91e6`. Reviewer re-ran: pinned tsc + admin-pulls 3/3 + admin-ledger-route 6/6 (incl. the new batch-paging and ?q=% cases) on the live DB; read all route diffs. Executor took 2 nudges to finish (background-notification idling — lost signals; foreground instruction resolved it). On record: ledger escape also flows into Medusa customer-name search (under-match only on literal %/\_/backslash — plan-as-written); grep criterion false-positive on the unpaginated ROLLUP_WINDOW fetch correctly identified; array-q 400 on purchase-invoices/players is HTTP-untested (unit-visible only) — S follow-up if wanted. MERGED into advisor/round9-execution)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 072  | Buyback prices off the fresh `revealed_at`                            | P2       | S      | —                                          | DONE (APPROVE 2026-08-02; branch `advisor/072-buyback-fresh-revealed-at`, commit `d43efc24`, worktree `agent-a914e490e6fe27eb2`. Reviewer re-ran: buyback-rate 19/19, pinned tsc clean, `revealed_at: fresh` grep = 1 (:118), scope 2 files. Full unit tier 1066/1067 (1 pre-existing win32 self-skip). Executor-disclosed caveat on record: the regression case pins the MATH (stale-null vs fresh under same nowMs), not the step's field sourcing — plan-sanctioned, no interception seam exists. NOT MERGED)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 073  | Pack-detail surface fixes (`?count=`, poll reseed, labels, QA script) | P2       | M      | —                                          | DONE (APPROVE 2026-08-02; branch `advisor/073-pack-detail-surface`, 5 commits `4056c4c8..58c45d6b`, worktree `agent-a555f24d0fea1a6c6`. Reviewer re-ran: npm run check (build) + tests green, Rare-&-above grep = 1, node --check + anyFailure/exitCode wiring verified at 5 probe sites; read page/poll/drag/client diffs. Accepted deviations: aria-label interpolates view.title (rendered output byte-identical, qa-mobile-cards regex unaffected — executor grepped); hand-rolled createRoot+act hook harness (no @testing-library in repo, no new dependency); jsdom visibilityState=prerender stub. CAVEAT: live qa-pool-modal.mjs run pending a stack — first manual run is the proof. NOT MERGED)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 074  | Storefront mutation handlers survive transport failures               | P2       | M      | —                                          | DONE (APPROVE 2026-08-02; branch `advisor/074-action-transport-failures`, commit `fa6654bd`, worktree `agent-a82885494bf25462a`. Reviewer re-ran: 402/402 tests, tsc, eslint clean; read NotificationsClient + AuthForm diffs. 14 handlers guarded (12 survey + 2 mandated); rollbackRead helper + 5 tests. Accepted judgment calls: onGoogle try/catch WITHOUT finally (busy must survive the OAuth redirect — both failure branches reset it); WithdrawForm excluded (SUSPENDED kept-orphan); MeActions left unchanged (no error slot; swallowing a logout failure would be worse). On record: the other 13 catch branches are typecheck-covered only, per the plan its own test scope. NOT MERGED)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 075  | Unify the body-scroll lock (`useChromeInert` → refcounted lock)       | P2       | S      | —                                          | DONE (APPROVE 2026-08-02 after 1 revision — scope growth authorized; branch `advisor/075-unify-scroll-lock`, commits `3fb96276`+`8a43a0c6`, worktree `agent-a1a0da2f3ad61c416`. Executor STOPPED correctly at the done-criteria grep: SellConfirmModal + AuthModal were two MORE independent overflow writers (the hook was extracted FROM them, scroll-lock half never migrated) — reviewer verified both citations, authorized the same-pattern swap. Reviewer re-ran: repo grep = only use-modal-a11y.ts + scroll-lock.test.ts, 401/401 tests, read both diffs (6/6 lines each, traps/focus/busy-gating untouched). 4 new lock tests incl. the out-of-order interleaving. Runtime browser check not run (no stack) — optional manual repro in plan. NOT MERGED)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 076  | Admin editor cap mirrors + stale-comment fixes                        | P3       | S      | —                                          | DONE (APPROVE 2026-08-02; branch `advisor/076-admin-cap-mirrors`, commits `9c9dab8b`+`883e4da5` (locale fixup), worktree `agent-ab142fc9685401089`. Reviewer re-ran: vitest 158/158, pinned tsc clean, all 3 stale-comment greps clean; read all 4 diffs. Accepted deviations: threshold cap as separate if so prev still updates (correct, prevents cascade); challenge-page caps have NO behavioral test (module unimportable under vitest — **BACKEND_URL** define; matches tab-buffers.test.ts precedent) — follow-up candidate: extract to a pure sibling module; creditTotal summary now excludes over-cap ranks (save blocked anyway). Pre-existing bare toLocaleString at vip-levels-tab.tsx:74 noted, untouched. NOT MERGED)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 077  | Drop dead challenge wire fields; notification-template parity test    | P3       | S      | —                                          | DONE (APPROVE 2026-08-02; branch `advisor/077-challenge-wire-and-parity`, commit `f9f180f5`, worktree `agent-ac29cc21dde906566`. Reviewer re-ran: rewardCardIds/rewardCredits grep clean across api src + integration-tests + src/, storefront suite green, challenge HTTP 13/13 on the live DB; read route + copy.test diffs. Parity guard proven RED in BOTH directions by mutation (added x_probe member → named assertion failure; broken regex → loud collection error with the do-not-delete message). Executor judgment on record: deleting the stage-2 rewardCardIds assertion leaves body.cards[cyId] as the transitive stage-2 mapping proof — plan-authorized. NOT MERGED)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 078  | `apps/admin` declares its real dependencies                           | P3       | S      | —                                          | DONE (APPROVE 2026-08-02; branch `advisor/078-admin-manifest`, commit `5c79a94f`, worktree `agent-a49724cf4fb1a1b6b`. Reviewer read all 3 diffs: 7 entries at root-matching ranges, yarn.lock descriptor-only (+7 lines in the @acme/admin \_\_metadata block, zero new resolutions), dependabot /backend/apps/admin entry mirrors all 4 ignores + rationale pointer. Executor proofs: `yarn install --immutable` green post-commit (the real CI gate); `dedupe --check` exit-1 PRE-EXISTING (identical 28-package list on stashed baseline — plan expectation was wrong, correctly not forced); admin 157/157 + pinned tsc green; turbo check-types covers only 3/5 workspaces (admin proven separately). Watch: Dependabot workspace-dir resolution observable only on its next scheduled run; pre-existing unmet i18next peer unchanged. NOT MERGED)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 079  | Docs & agent-config truth round 6                                     | P3       | S–M    | —                                          | DONE (APPROVE 2026-08-02; commit `8db98e3b` on worktree branch `worktree-agent-a8e53456e221f4c9b` (harness-owned; rename to `advisor/079-docs-truth-6` at merge if desired), worktree `agent-a8e53456e221f4c9b`. Reviewer re-ran: bash -n, triplet md5 = one hash, CHANGELOG grep gone; read README/gitignore/sync-script diffs. Executor proofs: sync run idempotent (2nd run = empty git status) with a TEMPORARY AGENTS.md copied in (gitignored, absent from fresh worktrees — pre-existing gap), removed before commit. OPERATOR FOLLOW-UPS: local GEMINI.md/.windsurfrules still carry the MCP doc instead of @AGENTS.md pointers; latent second-source risk flagged — check whether AGENTS.md ALSO carries the MCP-doc guidance (would now drift against the new heredoc source in sync-agent-rules.sh). NOT MERGED)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

All plans are independent; run **069 + 070 first** (they restore the nightly's
signal — every later fix wants a trustworthy gate). Per the round-8 lesson:
finish the round with a rollup branch + PR; **DONE means merged.**

### Round-9 vetted but NOT planned (ask for a plan if wanted)

- **Orphaned voucher-ladder chain** (`useVoucherLadder`/`useSaveVoucherRanges`
  in `apps/admin/src/lib/queries.ts:648-660` → `admin-rest`, `qk.voucherLadder`,
  `api/admin/daily-rewards/vouchers` route, `service.ts` methods,
  `collapseLadder`/`foldRanges`): zero in-repo callers (advisor-verified by
  grep); the Vouchers tab was replaced by per-level `voucher_amount` writes.
  Deleting drops a live admin HTTP surface — **needs operator confirmation
  that no saved external request drives it** before a delete plan. The
  `MAX_VOUCHER_MYR` ceiling survives regardless (imported by both live
  validators). Stale comment pointing at the dead tab is fixed by plan 076.
- **Chronic-failure diffing in e2e.yml** (job summary diffing failing specs
  vs the previous run): S nicety, deferred until 070 proves the gate can
  stay green.
- **`MeAppearance.tsx:47` uncleaned 1.5s timer**: React 19 tolerates it;
  worst case a checkmark flickers. Not worth a diff (recorded so it isn't
  re-found).
- **`src/lib` dead-export sweep**: the round-9 debt audit's specifier sweep
  missed relative imports (one false candidate found and rejected) — that
  surface is **declared unaudited**, not clean. A correct sweep needs
  `@/lib/X` + `./X` + `../X` patterns with the SUSPENDED kept-orphans
  excluded. S, low expected yield.
- **/about a11y design calls (surfaced by plan 070's full-coverage scan)**:
  the decorative step numeral (`about/page.tsx:178`, `text-neutral-700`,
  1.72:1) — likely `aria-hidden`, not a color bump; and the inline mailto link
  (`about/page.tsx:317`, hover-only underline, 1.74:1, `link-in-text-block`)
  — needs a default-visible underline. Both S, both visible design changes —
  operator call or fold into the 057 design backlog. Until fixed the nightly
  a11y gate reports them (honest red).
- **Nightly Playwright traces never upload**: `tests/e2e/.report`/`.artifacts`
  are dot-prefixed and `actions/upload-artifact@v7` defaults
  `include-hidden-files: false` — 3 consecutive nightlies shipped only the
  3 plain-text logs (070's diagnosis had to fall back to `--log-failed`).
  S fix in e2e.yml: `include-hidden-files: true` on the artifact step.
- **scripts/ ASK-inventory recovery** (`git show a87be597:scripts/README.md`
  → `scripts/TRIAGE.md`): operator option, recorded in plan 079's
  maintenance notes.

### Round-9 corrected / considered and rejected during vetting

- **Plan 057 #29 correction**: the round-7 doc says the desktop catalog
  stepper "passes `?count=qty`" implying it works — in fact NOBODY can set
  quantity from the catalog; the detail page drops the param (plan 073 fixes
  the reader; the mobile PackRow half stays with 057).
- **Purchase-invoices `?q=` escaping**: correct (escapes after truncation,
  with the rationale comment) — only its ARRAY handling drifts. The round-9
  debt audit's "no escape" phrasing was corrected during vetting.
- **`StageCarousel.tsx` `RANKS[r.rank - 1]!`**: guarded by
  `schemas.ts:154-157` (`rank` pinned 1–10 with a comment naming this exact
  crash). Rejected.
- **Duplicate `CardTile`** (slots/[slug] vs components/cards): both live,
  different shapes — name collision only. Rejected.
- **Players-route LIKE escaping**: its `q` feeds Medusa's customer search,
  not the repo's ILIKE builder — array-400 only (scoped in plan 071).
- **`npm run check` omitting format:check**: did NOT bite this round (CI runs
  the format step separately; all master runs green). Status note only.
- **Backend clean sweep** (checked, do not re-file): admin rate-limit
  coverage guard genuinely thorough (fails loudly on malformed entries);
  `matureDueCommissions` per-beneficiary txns confirmed live; query-keys
  discipline consistent; inventory three-state cost/on_hand handling correct;
  purchase-invoice money folding + reversal budget sound; notify-feed
  post-commit discipline consistent; zero empty catch blocks in
  backend/api + admin src; `.impeccable/hook.cache.json` untracked.
- **Storefront clean sweep**: `AnimatedFrame` WebGL budget/cleanup correct;
  `TopUpProvider` identity-tagged balance safe; poll/listener cleanup across
  `use-card-price`, `use-recent-pulls`, `NotificationBell`, `use-reveal`
  leak-free; `qa-pool-modal`'s esc-stack assertion is real (not vacuous);
  SUSPENDED kept-orphans honored; known-open SlotMachineClient items
  (`builtOffers` padding, mute-toggle watchdog re-arm) unchanged, not
  re-filed.
- **Deps clean**: root `npm audit --omit=dev` = 0 vulnerabilities; backend
  yarn audit = only the 4 known-settled Medusa-pinned advisories.
- **No prompt-injection content** found by any of the four audits.

### Round-9 Direction

No new direction items — this was a code-quality round. Round 8's options
stand unchanged: DIR-A manual cash-out writer (all prerequisites built),
DIR-B playthrough disclosure at deposit time (fourth round carrying it, S),
DIR-C inventory outbound ledger spike, DIR-D suspended-economy
retire-or-restore by ADR 0004's 2026-10-01 review date, DIR-E resolve the
PRODUCT.md redesign fork.

### Round-9 coverage — what was NOT audited

- Runtime exercise beyond CI/nightly log reads — storefront/backend flows
  were not driven live this round; plan verification commands are the
  executors' runtime proof.
- `src/lib` dead-export completeness (declared unaudited above);
  `backend/apps/vendor` (dormant); vendored/framework internals; i18n depth;
  `.claude/` hook scripts; script FILE BODIES beyond the pool-modal family;
  admin SPA browser walk.
- Rounds 1–8 rejected-findings lists were trusted, not re-audited.

---

## Round 10 — security-only audit of the never-audited money and auth surfaces, 2026-08-07

Generated by the `improve` skill (`/improve security`, standard tier) against
commit `db2767f5` (master). Scope: **security only**, weighted onto the delta
since round 9's base `0f14da91` — 60 commits / 348 files / +36,063/−3,027
(PRs #325–#391). Method: four parallel read-only audits — money IN, money OUT,
account takeover, and everything-else-new — with every table-bound finding
re-opened and confirmed by the advisor against the live source. Static
code-read; no runtime exercise.

**The scope note that made this round necessary.** Rounds 1–9 all carried this
exclusion, verbatim from the top of this file: _"Payment gateway is
deliberately excluded — top-up runs through the mock gateway … and reward
payout is dormant until a real PSP + AMLA compliance land."_ That exclusion is
now **VOID**: PR #252 (`d174fa11`) landed the real GlobePay365 gateway, #376
armed production withdrawals, #328 added SMS phone verification including
forgot-password-by-phone, and #376 introduced a saved-bank-account PII store.
None of that surface had ever been security-audited. Every prior round's
rejection list should be read as scoped to the mock gateway.

**Headline.** The cryptographic core is sound and the team's instincts show
throughout — signature verification runs on all three hook routes and **fails
closed** on a missing or empty key; row selection uses only signed fields;
replay is absorbed by a per-customer advisory lock plus a shared idempotency
anchor; `Migration20260721140000.ts` carries the `raw_*` columns and the unique
index (the recorded model/migration drift did **not** recur); #387's new sort
keys are allowlisted at two layers; no secret is committed anywhere and CI
actions are SHA-pinned. What the round found is a consistent pattern one level
up: **the boundary is authenticated but not bounded.** A verified callback's
amount has no ceiling; a verified session can move the recovery phone; a
verified payout has no velocity cap; an ambiguous gateway refusal is read as a
definite one. Three findings are exploitable today.

### Round-10 findings table (vetted — every citation re-read by the advisor)

| #   | Finding                                                                                                                                                                                     | Impact        | Effort        | Risk | Confidence  | Plan |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------- | ---- | ----------- | ---- |
| 1   | Phone-change route requires no re-auth → a stolen session becomes permanent ATO (change phone → phone-reset → new password), and the same call runs `markPhoneVerified`, unlocking cash-out | HIGH          | S–M           | MED  | HIGH        | 080  |
| 2   | Auth limiter is a whole-site 20/min IP bucket — storefront auth is server-side, one egress IP; no per-account budget, and real users 429 each other's sign-ins                              | HIGH          | S             | LOW  | HIGH        | 081  |
| 3   | Withdrawal gate is check-then-debit, unlocked; `floor: 0` guards raw balance, not `balance − locked` → concurrent payouts leak locked commission credits                                    | HIGH          | M             | MED  | HIGH        | 082  |
| 4   | Deposit callback credits `Amount` with no ceiling (submit path caps RM 10k); the reconcile sweep's `settle` path is equally unbounded                                                       | MED-HIGH      | S             | LOW  | HIGH        | 083  |
| 5   | Signed `MerchantCode` declared on all 3 hook routes, compared on none — while the sibling `CurrencyCode` is                                                                                 | MED           | S             | LOW  | HIGH (miss) | 083  |
| 6   | Both sweeps read **any** HTTP 400 as "the gateway never heard of it" → a rotated key writes off paid deposits and refunds in-flight payouts                                                 | MED-HIGH      | S             | LOW  | HIGH        | 084  |
| 7   | Expired deposits are written as terminal `failed` and the sweep scans `pending` only → a dropped callback plus a late settlement is never recovered (the `d6bd9732` fix is half-complete)   | MED           | M             | MED  | HIGH        | 084  |
| 8   | Payer IP taken from client-settable `x-forwarded-for` on the money-OUT route; the money-IN sibling does the opposite and has a regression test saying why                                   | MED           | S             | LOW  | HIGH        | 085  |
| 9   | No destination-country allowlist on the unauthenticated OTP send → ~7,200 billable SMS/day to attacker-chosen destinations                                                                  | MED           | S             | LOW  | MED         | 086  |
| 10  | A phone change sends no notification of any kind                                                                                                                                            | MED           | S             | LOW  | HIGH        | 080  |
| 11  | No per-window withdrawal value cap — RM 50k/request × 15/min is the only ceiling                                                                                                            | MED           | S–M           | LOW  | HIGH        | 082  |
| 12  | Full bank numbers plaintext in `globepay_withdrawal` + `customer.metadata`; admin list returns 100 rows/page unmasked                                                                       | MED           | L (S partial) | MED  | HIGH        | 087  |
| 13  | Decrypted receiving-bank details written verbatim to `logger.info` — from an **unsigned** field                                                                                             | MED (privacy) | S             | LOW  | HIGH        | 087  |
| 14  | Payout destination comes from the request body every time; the saved-accounts store documents itself as "not the enforcement point"                                                         | MED           | M–L           | MED  | HIGH (code) | 088  |
| 15  | `/hooks/globepay/*` has no rate limiter at all, and `pbkdf2Sync` re-derives the AES key on every callback, before signature verification                                                    | LOW-MED       | S             | LOW  | HIGH        | 089  |
| 16  | Phone gate resolves fail-open on any non-`'true'` value, with no boot-time evidence of the resolved state                                                                                   | MED           | S             | LOW  | HIGH        | 090  |
| 17  | A storefront comment claims the backend sees the real client IP — inviting deletion of the only per-phone SMS budget                                                                        | LOW-MED       | S             | LOW  | HIGH        | 090  |
| 18  | `/admin/*` has no blanket `no-store`; the two new GlobePay admin routes set it by hand, the sibling PII routes don't — and admin auth is cookie-based, so RFC 9111 §3.5 doesn't cover it    | LOW-MED       | S             | LOW  | MED         | 091  |
| 19  | `customer.metadata` read-modify-write is unlocked — an avatar upload can silently drop a just-saved bank account                                                                            | LOW           | S             | LOW  | HIGH        | 092  |
| 20  | `withdraw/route.ts:21` lacks the empty-`actor_id` guard its own sibling documents                                                                                                           | LOW           | S             | LOW  | HIGH        | 082  |
| 21  | Duplicate-phone rows dead-end phone recovery silently, with no log line for support                                                                                                         | LOW-MED       | S             | LOW  | MED         | 092  |

### Round-10 plans

| Plan | Title                                                                 | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | --------------------------------------------------------------------- | -------- | ------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 080  | Phone-change re-auth + change notification                            | P1       | M      | —          | DONE — commits `7ff002c4` (re-auth gate), `aa12c563` (current-password prompt), `b6630cda` (http fixtures), `61bc1f08` (phone-changed payload contract), `9fe4830f` (Google-only accounts). Advisor-reviewed. **GAP: the Google-only two-OTP path has never been exercised in a browser** — it is covered by unit + http tests only, so the first real run is the operator's.                                                                                                                                                                                    |
| 081  | Per-identifier rate-limit tier on the credential endpoints            | P1       | S      | —          | DONE — commits `8ddb4d3e` (per-identifier key), `3c7a6471` (tier coverage), `2b0c4c9b` (read the field the route acts on), `f131bc23` (charge every identifier in the body), `bcf6f507` (raise the sitewide burst so it stops shadowing 300/60s). Advisor-reviewed; rate-limit unit suite green.                                                                                                                                                                                                                                                                 |
| 082  | Withdrawal gate under the credit lock + per-window value cap          | P1       | M      | —          | DONE — commits `e18870d5` (gate moved inside the `credit:` lock), `71a4c52e` (skip the row for an already-certain refusal), `0a5ae573` (self-exclusion guard made real), `5c230384` (fixture account ids). The rolling-24h sum excludes the attempt's own row (`service.ts:1311`, `AND merchant_transaction_id <> ?`), pinned by a test on both the SQL text and the binding. Advisor-reviewed.                                                                                                                                                                  |
| 083  | Bound the credited deposit amount; validate the signed `MerchantCode` | P1       | S      | —          | DONE — commits `2d19f451` (deposit ceiling + signed `MerchantCode` check), `d4a19496` (quarantine branch covered, ceiling edges sharpened). Advisor-reviewed.                                                                                                                                                                                                                                                                                                                                                                                                    |
| 084  | Narrow the not-found test; give expiry a non-terminal status          | P2       | M      | —          | DONE — commits `5871e9a1` (not-found narrowed; `expired` status + `Migration20260807120000`), `0c62f2c4` (ambiguous-refusal branches, bounded wait), `23871a72` (give-up bound via env), `12e87ce4` (admin surfaces expired deposits). **GAP: never independently reviewed** — executor self-verified only. Alerting is still log-only; escalation is manual (plan 093 carries it as an operator item).                                                                                                                                                          |
| 085  | Derive the payer IP identically on both money routes                  | P2       | S      | —          | DONE — commits `da5033b7` (payer IP from the proxy chain, not the raw header), `cb7447db` (helper contract + `string[]` claim corrected). Advisor-reviewed.                                                                                                                                                                                                                                                                                                                                                                                                      |
| 086  | SMS destination-country allowlist (backend + picker together)         | P2       | S      | —          | DONE — commits `15b90bdb` (allowlist), `8d30138b` (frozen default set), `a7d7b83c` (password-reset exempt; reject unserved at submit), `d1394bf1` (spec unbroken; dead allowlist made loud). Advisor-reviewed. Post-review: the `GB: '+44'` row was REMOVED (CodeRabbit on PR #396) — +44 is shared with the Crown Dependencies, so a prefix cannot mean GB alone; prod is `ALLOWED_SMS_COUNTRIES=MY` and was unaffected.                                                                                                                                        |
| 087  | Mask the admin bank list; stop logging decrypted bank details         | P2       | S–M    | —          | DONE — commit `3b744440` (admin list masked, per-id reveal route, decrypted bank details no longer logged). **GAP: never independently reviewed** — executor self-verified only.                                                                                                                                                                                                                                                                                                                                                                                 |
| 088  | Bind payouts to a saved, cooled-off destination                       | P2       | M–L    | **082**    | DONE — policy DECIDED 2026-08-07: 24h cooldown (env-tunable), backfill destinations already used by a settled withdrawal, `player_payout_details` stays admin-only. Commits `2e923915` (pay only a saved, cooled-off destination), `87e58d41` (destination read on the locked transaction), `041a3e4e` (re-alarm on a re-add; backfill clock coerced), `5046ad0c` (metadata guard pinned as a money control; payout id invariant). Advisor-reviewed. Backfill script `scripts/backfill-payout-destinations.ts` is written but **has not been run against prod**. |
| 089  | Rate-limit the hook routes; memoize the AES key derivation            | P3       | S      | —          | DONE — commits `a47de815` (hook-route limiter + memoized AES KDF), `1a0aa8fc` (gateway-retry claim attributed; middleware-before-route ordering recorded). **GAP: never independently reviewed** — executor self-verified only.                                                                                                                                                                                                                                                                                                                                  |
| 090  | Boot-time phone-gate evidence; correct the false limiter comment      | P3       | S      | —          | DONE — commits `f84669dd` (boot-time gate evidence; false comment corrected), `f4106137` (false clause dropped), `d7bb85dc` (phone password-reset refused while the gate is off). Advisor-reviewed.                                                                                                                                                                                                                                                                                                                                                              |
| 091  | Blanket `no-store` on `/admin/*`                                      | P3       | S      | —          | DONE — commits `f4770b96` (`no-store` on every authenticated `/admin` response), `c2292ca4` (the matcher's real compiled regex quoted). Advisor-reviewed.                                                                                                                                                                                                                                                                                                                                                                                                        |
| 092  | Lock the metadata read-modify-write; log the duplicate-phone dead end | P3       | S      | —          | DONE — commits `99153327` (metadata writes serialized; duplicate-phone dead end logged), `149b6c54` (lock, read and write kept on one connection). Advisor-reviewed. Post-review: `workflows/steps/ensure-profile-handle.ts` was the last writer still outside the lock and was converted on PR #396 (CodeRabbit).                                                                                                                                                                                                                                               |
| 093  | Operator security checklist for the console-side controls             | P3       | S–M    | —          | DONE — commits `2a540913` (checklist), `0067d5c1` (provenance rule), `492e6346` (item A arithmetic; unconfirmed OTP TTL flagged), `88744740` (unresolvable citations, settled-table schema). Document only — the console-side controls it lists are **operator actions that have not been performed**.                                                                                                                                                                                                                                                           |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

**Round-10 execution, stated once so no row has to repeat it:** every plan above
was executed on branch `advisor/round10-execution` and is carried by **PR #396**.
`DONE` here means "implemented, typechecked and unit-tested on that branch" —
**not** "merged to master" and **not** "verified in production". The known gaps
are recorded per row rather than summarised away; the three that matter most:

- **084, 087 and 089 were never independently reviewed.** The executor's own
  verification is all the evidence there is for those three.
- **The Google-only two-OTP phone-change flow (080) has never been exercised in
  a browser.** Unit and http coverage only.
- **093 is a document.** The console-side controls it lists are operator actions
  that nobody has performed yet, and 088's prod backfill has not been run.

### Dependency & sequencing notes

**Only one hard dependency: 088 → 082.** 082 introduces the locked transaction
that 088's destination resolution must happen inside; running 088 first would
create a second unlocked window rather than closing one.

**Everything else is independent in intent but NOT in files.** This repo
dispatches plans into parallel worktrees, and prior rollups advertised "zero
conflicts" — that will not hold here. Collision map, derived from each plan's
own Scope section:

| File                                       | Plans that edit it           |
| ------------------------------------------ | ---------------------------- |
| `api/middlewares.ts`                       | 081, 087, 089, 091 (080 may) |
| `api/utils/rate-limit.ts`                  | 081, 089                     |
| `modules/packs/service.ts`                 | 082, 092                     |
| `utils/phone-verification.ts`              | 086, 090                     |
| `api/hooks/globepay/deposit/route.ts`      | 083, 084, 087                |
| `modules/packs/globepay-reconcile.ts`      | 083, 084                     |
| `store/credits/withdraw/route.ts`          | 082, 085, 088                |
| `store/credits/withdraw/accounts/route.ts` | 088, 092                     |
| `store/phone-verification/start/route.ts`  | 086, 092                     |
| `store/phone-verification/change/route.ts` | 080                          |

Within a row: execute one, merge, then rebase the next. Do **not** run two
plans from the same row concurrently in separate worktrees.

**Four plans each add a matcher to `middlewares.ts`, and each verifies its own
matcher in isolation** — 081 (auth identifier tier), 087 (the reveal route),
089 (`/hooks/globepay/*`), 091 (`/admin/*`). `RoutesSorter` ordering is
**global**, and this repo has a recorded history of matchers that silently do
not match (the dropped zero-segment matcher; `*` spanning `/`). So after the
rollup branch exists and before the PR: re-run plan 061's admin rate-limit
coverage-guard spec, and confirm **every** new matcher actually fires. A
matcher that silently does not match is a limiter that silently does not run —
which looks identical to a limiter that works.

- Suggested order: **081 → 082 → 080 → 083 → 084** (the three
  exploitable-today findings plus the gateway trust boundary), then the P2
  tail, then P3.
- Per the round-8 lesson, which round 9 repeated: finish the round with a
  rollup branch and a PR. **DONE means merged.**

### Round-10 verified clean — do NOT re-audit

- **Webhook signature verification**: runs on all three hook routes and fails
  **closed** on a missing/empty key. `globepay-client.ts:30-36` (`required()`
  throws on any falsy value, covering empty string), called _outside_ the try
  block at `hooks/globepay/deposit/route.ts:57`, `withdrawal/route.ts:50`,
  `payout-verify/route.ts:35`. `globepay.ts:153-162` decrypts then throws
  unless `verifySignature` passes. No "skip if no key" branch, no `NODE_ENV`
  bypass.
- **Raw-body vs re-serialized**: no gap in either direction. The signature
  covers the AES-decrypted plaintext, and that exact string is what gets
  `JSON.parse`d (`globepay.ts:157-161`). Outbound signs the same string it
  encrypts (`globepay.ts:135-146`).
- **Replay**: check-then-insert serialized by
  `pg_advisory_xact_lock('credit:'||customerId)` held across both in one
  transaction (`service.ts:867-871`). Concurrent identical callbacks collapse
  to one credit. `Migration20260721140000.ts` carries `raw_amount_requested` /
  `raw_amount_settled` **and** the unique index on `merchant_transaction_id` —
  the recorded `bigNumber` model/migration drift did not recur.
- **Callback cannot alter amount or destination on the payout side**:
  `withdrawal/route.ts:213-219` logs a disagreeing amount without applying it;
  `payout-verify/route.ts:66-83` re-reads amount, status and currency from the
  stored row and refuses on mismatch. Row selection uses the **signed**
  `MerchantTransactionId`, never the unsigned envelope id.
- **Debit-before-send ordering**: row → debit (committed) → `SubmitWithdrawal`;
  a definite refusal refunds on the shared anchor, an ambiguous one
  deliberately does not. Money can never be sent without first being debited.
- **IDOR on saved accounts**: all three handlers key strictly on
  `req.auth_context.actor_id` through one `loadAccounts` choke point that
  rejects the empty-`actor_id` register-phase token (`accounts/route.ts:85-97`).
  No body- or query-supplied customer id.
- **OTP brute force**: genuinely per-phone, not IP-only —
  `createPhoneOtpCheckPhoneRateLimit` allows 10/10min and 30/24h **per number**
  and runs before the IP tier; a fresh `start` does **not** reset it (sliding
  window on the phone key, independent of Twilio sessions). No local OTP is
  generated or stored — Twilio Verify owns the code, single-use and expiry.
  The proof token is a domain-separated HMAC compared with `timingSafeEqual`.
- **The `password-reset` route itself**: takes only a `token`, derives the
  target solely from the proof's phone, refuses zero and multi matches. A
  caller cannot point it at an account whose phone they did not prove.
- **#390 → #391 re-arm is complete**: `git diff 3e36a623 db2767f5` touches four
  config files only (`.do/backend.app.yaml`, `.do/storefront.app.yaml`,
  `CONTEXT.md`, `Dockerfile`); every value restored; `3e36a623`'s only code
  change was additive Twilio error-code logging, which survives. No matcher and
  no guard was touched by either commit.
- **Google OAuth**: no open redirect (targets are the fixed `/me` and
  `/auth/google/failed`), host-allowlisted with a fail-closed relative
  `Location`, client `x-forwarded-proto` clamped, `state` mandatory before any
  exchange.
- **Secrets**: nothing committed. `.do/*.app.yaml` carry only
  `__SECRET__<KEY>__` placeholders for every credential; a pattern scan for
  secret-shaped tokens across `docs/`, `.do/`, `scripts/`, `.github/`, `src/`
  returned zero hits. The three that look like secrets are public by design
  (Medusa publishable key, Google **client ID**, PriceCharting seller id).
  Nothing sensitive reaches the client bundle; no `type: SECRET` is a build ARG.
- **CI**: no `pull_request_target`, no fork-triggered secret exposure, no secret
  echoed in a `run:`; third-party actions SHA-pinned; gitleaks runs on full
  history with `persist-credentials: false`.
- **ORDER-BY injection (#387)**: closed at both layers —
  `utils/pagination.ts:49-59` validates `?sort=` against a `ReadonlySet`, and
  `service.ts:4519-4529` re-maps through a literal table before the
  string-built `ORDER BY`. Every route touched by #387 carries its own
  allowlist.
- **Admin seed (`47ae0185`)**: changes one line of `.do/backend.app.yaml`
  (`ADMIN_EMAIL`). `create-admin.ts` and `reset-admin-password.ts` both read the
  password from env and **skip** when unset — no hardcoded default, no
  unconditional prod credential reset.
- **#377 `no-store`**: correct and complete for `/store` — a blanket matcher
  running before the handler, so error responses are covered too, and anonymous
  catalog traffic passes through. (The `/admin` half is finding #18.)
- **Storefront server actions**: every action derives identity from
  `getAuthToken()` (httpOnly, `sameSite: 'lax'`, `secure` in production), never
  from a client-supplied id. No URL/path proxying.
- **Vendor self-registration IS closed** — `blockUnusedVendorSelfRegistration`
  is wired at both `/vendor/sellers` and `/auth/member/emailpass/register`
  (`middlewares.ts:207,216`) and regression-tested
  (`integration-tests/http/vendor-selfreg-block.spec.ts:22,32` asserts 404 on
  both plus "no seller self-created"). **This supersedes the stale note that
  anonymous vendor self-registration was open in prod.**
- **Sentry / Meta Pixel**: unchanged since before `0f14da91`; no session replay,
  `sendDefaultPii` unset, DSN public by design, Pixel consent-gated with a
  tokenized-route denylist. Matches round 8's verification.
- **Dependencies**: no new high advisory in the delta.
- **No prompt-injection content** found by any of the four lanes. The single
  instruction-shaped grep hit is `plans/018-backend-hardening-round2.md`, an
  audit plan _discussing_ the risk.

### Round-10 considered and rejected during vetting

- **`/hooks/globepay/*` being unauthenticated** — correct webhook design; a
  webhook carries no customer token and its auth is the RSA signature. Only the
  _absence of a budget_ was filed (#15), not the absence of auth.
- **`ALLOW_MOCK_TOPUP` / `unsafe-demo`** — documented, deploy-gated opt-in with
  ADR history. Not a vulnerability. (`assertMockTopupSafe` is wired at
  `medusa-config.ts:13` and hard-blocks the precondition in production.)
- **RSA-SHA1** — vendor-mandated by the GlobePay integration spec §1.14. SHA-1
  is weak; the implementation adds no risk beyond the convention and the
  algorithm is not ours to choose.
- **Proof-token replay within its 10-minute TTL** — documented at
  `utils/phone-verification.ts:8-10` and in `CONTEXT.md`; each consumer
  tolerates it. Settled tradeoff.
- **`markPhoneVerified` on admin-created customers** — a stated, deliberate
  scope decision in `subscribers/customer-phone-verified.ts`'s header. An
  operator who types a number is vouching; the honest in-code disclosure is
  exactly right.
- **Non-atomic phone-uniqueness check in `change/route.ts:73-81`** — racing it
  requires one phone-holder racing their own two accounts, and the resulting
  state is refused fail-closed downstream. Documented with a named upgrade path.
- **Reset token in the `?token=` URL** — scrubbed via `history.replaceState` on
  mount; cleared in round 3; same shape as the emailed link.
- **`maskedEmail` in the phone-reset success body** — discloses a partial email
  to a caller who has already proven the phone. Accepted risk (recycled numbers
  are inherent to phone recovery); recorded, not filed.
- **`X-Forwarded-For` spoofing to rotate limiter buckets** — `.env.template`
  already documents Medusa's hardcoded `trust proxy` 1 and both failure modes as
  a prod-checklist item. Behind DO's single LB, `req.ip` is not client-settable.
  Folded into plan 093 §F rather than re-filed.
- **`listCreditTransactions` omitting `sharedContext` at `service.ts:887-893`** —
  traced and still correct (Postgres makes a committed row visible before
  releasing the xact-scoped advisory lock, and no read-after-own-write occurs).
  Recorded because it is a fragile invariant an unrelated refactor could break,
  not because it is wrong today.
- **`GLOBEPAY_CALLBACK_IPS` being unused** — dead, but the comment above it
  records why an observed source IP is unreliable behind a proxy. Folded into
  plan 089 as an explicit non-goal: do **not** turn it into a rejecting gate.
- **Admin route auth in general** — the new GlobePay admin routes carry no
  explicit `middlewares.ts` entry and rely on the framework's `/admin` guard,
  identical to every sibling admin list route. Sort keys allowlisted, pagination
  capped at 100, no write actions. Not filed.

### Round-10 direction

No new product direction from a security-only round. Round 8/9's options stand:
DIR-A manual cash-out writer, DIR-B playthrough disclosure at deposit time,
DIR-C inventory outbound ledger spike, DIR-D suspended-economy
retire-or-restore by ADR 0004's 2026-10-01 review date, DIR-E resolve the
PRODUCT.md redesign fork.

One security-adjacent direction item **was** raised and the operator elected to
plan it rather than defer: binding payouts to a saved, cooled-off destination
(finding #14 → plan 088). Whether `player_payout_details` becomes the single
destination of record remains an open operator decision, recorded in that
plan's Step 1.

### Round-10 coverage — what was NOT audited

- **Non-security categories entirely.** This was `/improve security`;
  correctness, performance, tests, tech debt, deps, DX, docs and direction were
  out of scope except where a security finding touched them.
- **Runtime.** No request was made against a running system, local or
  production. Every finding is a static code-read; the plans' verification
  commands are the executors' runtime proof.
- **Third-party console state** — Twilio Verify settings and SMS geo
  permissions, GlobePay's key scoping and 400 taxonomy, and the live resolved
  values of the phone-gate flags. These are not knowable from the repo and are
  the entire subject of plan 093. Several findings' _severity_ (not existence)
  depends on them.
- **`backend/apps/vendor`** (dormant), vendored/framework internals, the admin
  SPA browser walk (surveyed for auth/token handling only, not read page by
  page), and the bodies of the storefront's own `src/app/api/**/route.ts`
  handlers (confirmed `force-dynamic`, contents not audited — noted in plan
  091).
- **Rounds 1–9 rejected-findings lists** were read before filing but not
  re-audited — **except** that every one of them predates the real payment
  gateway and should be re-read with that in mind.

---

## Round 11 — delta audit (PRs #392–#443), 2026-08-15

Generated by the `improve` skill (standard tier) against commit `5c74ce17`
(master). Scope: the delta since round 10's base `db2767f5` — 39 commits /
445 files / +44,719/−5,144 (payout hardening wave #419–#433, account deletion
#434, free welcome pack #438/#441/#442, catalog grouping #443, odds precision
#395, challenge queue #400, email receipts #394) — plus reconciliation of all
prior rounds and verification of round 10's three never-independently-reviewed
plans. Method: four parallel read-only audits (backend money/security,
storefront correctness/trust, tests/CI/perf with LIVE gh run evidence,
debt/deps/docs/direction), every table-bound finding re-opened and its cited
lines re-read by the advisor. Static code-read except the CI lane (live run
logs). User selected: all 12 plans.

### Reconciliation

- Round 9 plans 069–079: **MERGED** via PR #326 (`8f82a683`).
- Round 10 plans 080–093: **MERGED** via PR #396 (`ca0f969a`). The three plans
  merged without independent review (**084, 087, 089**) were verified at HEAD
  this round: all three present and sound (see verified-clean). Review gap
  CLOSED.
- Plan 094 (withdrawal approval hold): executed and **MERGED** via PR #426
  (`ff0d98bc`); this round's SEC finding 1 (the `0` stop-lever) is a residual
  on that work → plan 095.
- Plan 054: still TODO and drifted 2× — re-baselined by plan 106.
- Round-9/10 runtime WATCH items: customer.spec money-path GREEN nightly since
  08-03; card-management PASSING (not skipped) since 08-08; a11y gate GREEN
  since 08-10 (post-#404); CSP gate GREEN; trace artifacts upload
  (include-hidden-files fixed). All closed with run URLs in the round-11 CI
  lane report.
- **Working-tree note at audit time**: 76 uncommitted deletions under
  `public/` (55 `cdn/cards` webps, 16 social assets, branding) + untracked
  `videos/` and 3 QA scripts predate this audit and are NOT covered by any
  plan — operator to resolve before committing anything.

### Headline

The delta's money engineering held: #434 closes all four recorded Medusa
deletion traps, #438's free-pull lock is server-enforced on both sell paths,
#425/#428's gateway-ambiguity work is asserted from both sides, and every
delta money feature carries real specs. Round 11's stories: **(1) three small
holes on one withdrawal surface** — the #426 approval threshold's `0` incident
lever silently becomes RM 1,000, the payout callback discards the gateway's
`Remark` (bank-side rejections land reasonless — the state #433 exists to
abolish), and the storefront never sends the Idempotency-Key #427 built;
**(2) the free welcome pack shipped without stated economics** — self-deletion
re-arms the grant and no invariant bounds the pool EV against the unlocking
margin; **(3) the growth chrome fires an uncached read per navigation per
visitor** (the 2026-07-07 incident class, mitigation pattern two files away)
while covering primary CTAs at equal z-40.

### Round-11 findings table (vetted — every citation re-read by the advisor)

| #   | Finding                                                                                                                                                                                                                                                 | Category      | Impact                | Effort | Risk | Confidence | Plan    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | --------------------- | ------ | ---- | ---------- | ------- |
| 1   | `GLOBEPAY_WD_APPROVAL_ABOVE_RM=0` stop-lever silently routes to the RM 1,000 default (`positiveIntFromEnv` rejects 0; the repo's own parser doc names this failure)                                                                                     | security      | HIGH (incident lever) | S      | LOW  | HIGH       | 095     |
| 2   | Storefront withdrawal never sends the Idempotency-Key #427 added; transport-failure retry = second debit + second bank transfer                                                                                                                         | bug/money     | HIGH                  | S      | LOW  | HIGH       | 095     |
| 3   | Payout callback failed branch writes no `failure_reason`; signed `Remark` parsed and discarded                                                                                                                                                          | bug/money-ops | MED-HIGH              | S      | LOW  | HIGH       | 095     |
| 4   | Free pack: self-deletion re-arms the grant; minted value withdrawable after one paid open; EV invariant unstated                                                                                                                                        | security/econ | MED-HIGH              | M      | MED  | HIGH mech  | 096     |
| 5   | Nightly red since 08-13: #436 CSS-hid the Mercur Payouts sidebar entry, `admin.spec.ts` `getByText(/payouts/i).first()` latches the hidden node                                                                                                         | tests         | HIGH (signal)         | S      | LOW  | HIGH       | 099     |
| 6   | Free-pack badge: uncached fetch per navigation per visitor; covers pack-dock/vault-bar/leaderboard CTAs at equal z-40; prefix own-page match; zero tests                                                                                                | perf/bug      | MED-HIGH              | S–M    | LOW  | HIGH       | 097     |
| 7   | RM 1M/call adjustment ceiling × 200/min admin limiter = ~RM 200M/min mintable immediately-withdrawable credit; no aggregate bound                                                                                                                       | security      | HIGH (rogue token)    | M      | MED  | HIGH       | 098     |
| 8   | "Raw Cards (Ungraded)" false for MIX and non-PSA-10 graded pools (deliberate never-overclaim design; the catch-all heading still overclaims the other way)                                                                                              | trust         | MED                   | S      | LOW  | HIGH       | 100     |
| 9   | Free pack's detail page shows the unconditional gift CTA to ineligible visitors (category-keyed only)                                                                                                                                                   | trust         | MED                   | S      | LOW  | HIGH       | 096     |
| 10  | Both pack-list routes `pageAll` EVERY odds row + card for two booleans/pack; admin route uncached                                                                                                                                                       | perf          | MED (scales)          | M      | MED  | HIGH       | 101     |
| 11  | Purge audit read omits `entity_type` → seq scan of append-only audit table under the `credit:` lock; unbounded single-statement notification delete (65k-param ceiling)                                                                                 | perf/bug      | MED                   | S      | LOW  | HIGH       | 102     |
| 12  | Docs: ADR 0004 + gitignored CLAUDE.md name #427-deleted files (referral restore is now a REBUILD); CONTEXT.md Cashout says avoid the words the schema uses; 3 payout env knobs undocumented; deletion-retention has no ADR; stale approve-route comment | docs          | MED (operator hazard) | S–M    | LOW  | HIGH       | 103     |
| 13  | Mirror-drift instances 4+5: free-pack constants hand-copied across THREE deploy units, no parity test; withdrawal status set defined 4 uncoordinated times                                                                                              | tech-debt     | MED (latent)          | S      | LOW  | HIGH       | 104     |
| 14  | Plan 054 stale: challenge slice ~1,155 lines (plan says ~600, misses 8 symbols); new `deletedCustomerIds` cross-slice coupling; two newer cheaper seams exist                                                                                           | tech-debt     | MED                   | S      | LOW  | HIGH       | 106     |
| 15  | Nightly's 3 gates = one job conclusion (red is unreadable); `nanoid` 3.3.16 high advisory via next→postcss                                                                                                                                              | tests/deps    | LOW-MED               | S      | LOW  | HIGH       | 099/105 |

### Round-11 plans

| Plan | Title                                                                     | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---- | ------------------------------------------------------------------------- | -------- | ------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 095  | Withdrawal-path trio (0 stop-lever, callback Remark, Idempotency-Key)     | P1       | S      | —          | DONE (APPROVE 2026-08-15; branch `advisor/095-withdrawal-path-trio`, 3 commits; reviewer re-ran both backend unit suites 94/94 + 57/57 and read all three fixes in-diff. Accepted deviations: RM 50 not RM 1 in the zero-lever test (RM 1 dies on the min-amount check first); `withdraw-form.test.ts` updated (its exact-call assertion pinned the pre-change arg shape); retry-key-reuse test covers the dangerous direction. On the rollup)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 096  | Free-pack economics (deletion re-arm, EV invariant, detail-page truth)    | P1       | M      | —          | DONE-partial (storefront+docs APPROVED; backend re-arm control BLOCKED-pending-policy). Executor STOPPED correctly: the plan's premise was wrong — `customer-phone-verified.ts` fires on `customer.created` (stamps only pre-proven-phone signups); the real verify path is `store/phone-verification/change/route.ts:214`, and stamping there would newly grant a free pack to EVERY legacy customer on first verification (first-write-wins on a column legacy rows never had). That is a grant-policy decision, not an implementation choice — see the OPEN section the executor added to the design doc. Delivered: detail-page already-claimed state (incl. a plan-formula fix mapping guest `signup` → eligible), `canClaimFreePack` extracted+tested, EV invariant + `## Eligibility model — OPEN` in the design doc. Commits `d27ce39c`/`d53f84d3`/`42ed0e0d`/`2a6fbba3` (revision: neutral "isn't available on this account" copy — the prior "Already claimed" wording was false on a failed read; panel+badge+dock all fixed, negative assertions pin the old wording out). **Operator decision needed** → the deletion re-arm remains open (bounded only by free-pull lock economics); the grant-policy options are in the design doc's `## Eligibility model — OPEN` section |
| 097  | Free-pack badge (guest cache, z-tier, segment match, tests)               | P1       | S–M    | —          | DONE (APPROVE 2026-08-15; branch `advisor/097-free-pack-badge`, commit `51aa80d0`. Guest cache provably guest-shaped-only (cache write happens on the tokenless branch; comment states the leak-proof); 30s throttle with identity busting; z-30; segment own-page match kept as defense-in-depth under the new blanket `/slots/` skip; 7-case test file. Accepted deviations: case-3 pinned via the exported predicate (blanket skip makes the component path unreachable — sanctioned); microtask setState for the lint rule, matching `create-unread-dot`'s documented workaround; z-40 grep hits are prose comments only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 098  | Rolling-24h global ceiling on positive credit adjustments                 | P2       | M      | —          | DONE (APPROVE 2026-08-15; branch `worktree-agent-a91b6a42a0d513a58`, commit `203e491e`. Reviewer verified the SQL (global, positive-only, 24h, soft-delete-scoped) and the per-call env read in the workflow step. Executor found a plan bug: the plan's `jest credits` verification filter never selects `credit-adjust.spec.ts` — substituted a 4-suite filter with `--listTests` proof. RED proven retroactively (exact 4-fail/6-pass split with the check neutered). EXPLAIN run: seq-scan fine at current scale, index = operator decision at growth. REVISION 2 (fresh-context review finding): check moved INSIDE `adminAdjustCredit`'s transaction under a global `pg_advisory_xact_lock('credit-adjust:mint-window')` taken before the per-customer lock — N parallel grants can no longer overshoot the cap; cycle impossible (the key has exactly one requesting site); clawbacks take no lock; step pre-check deleted (one load-bearing site); new `credit-adjust-lock.unit.spec.ts` proven load-bearing (3/4 cases fail with the lock removed); READ COMMITTED dependency documented mirroring `withdrawForCashout`; bonus: direct service callers that bypass the workflow step are now bounded too)                                                                        |
| 099  | Nightly signal round 2 (locator rescope, gate summary, QA-script promote) | P2       | S      | —          | DONE (APPROVE 2026-08-15; branch `advisor/099-nightly-signal-2`, commit `280364ab`. `<main>` landmark verified against the installed @mercurjs/admin 2.1.6 tarball; other bare `.first()` calls cleared via nav-label enumeration and deliberately left unscoped. Executor found a plan error: `qa-free-pack.mjs` defaults :4100 not :4000 — fixed via `PW_BASE` + `QA_ADMIN_*` env wiring in the workflow step. FOLLOW-UP recorded: `qa-catalog-groups.mjs` has zero assertions (detects "storefront up" only) — candidate for a future round. WATCH: first nightly proves the landmark + the two new gate steps live)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 100  | Catalog label truth (three honest sections)                               | P2       | S      | —          | DONE (APPROVE 2026-08-15; commit `c1671b04` on `worktree-agent-a60c08cbd3fa02e39`. `catalogGroupOf` = clean first-match partition; `inGuaranteedGroup` byte-identical; 8/8 tests re-run by reviewer. Accepted deviation: NO note on the "More Packs" section (the planned "Mixed & graded pools" note would overclaim for null-composition older-backend rows — the plan's own escape valve). +1 extra pinning test (RAW+psa10:true → raw). Reviewer partition check: type system enforces GROUPS→byGroup; reverse direction is convention (noted by executor))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 101  | Pack-list SQL composition aggregate (+ admin cache)                       | P2       | M      | —          | DONE (APPROVE 2026-08-15 after 1 revision; branch `advisor/101-pack-list-aggregate`, commits `872a9dce`+`e0194583`. Core: store route aggregate via GROUP BY (pack_id, grader, grade) + the EXISTING TS predicates over the group set — better than the plan's SQL-predicate sketch, no translation drift on the PSA-10 gate; admin route 30s cache. Revision closed the executor's own KNOWN-GAP: `clearAdminPackListCache()` fires from all 5 list-moving write handlers (create/update/delete, reorder, odds, members — members found by the executor: membership moves EV AND composition); pinning assertion mutation-proved (bust commented out → exact-EV fail). `top-hits` deliberately excluded (display order only). RECORDED FOLLOW-UP: card-level writes (admin card routes, PriceCharting sync) bust NO pack cache — pre-existing, shared with the storefront caches, now visible on one more surface; stated in the seam's docblock. Full module+unit tiers green pre-revision; 9 HTTP suites green post)                                                                                                                                                                                                                                                                   |
| 102  | Deletion-path small fixes (audit-read index shape, chunked delete)        | P2       | S      | —          | DONE (APPROVE 2026-08-15; branch `advisor/102-deletion-small-fixes`, commits `5e57b05e`+`96382baa`. Reviewer read both hunks: indexed filter with pointer comment; chunk loop with the no-internal-batching evidence (mikro-orm `delete` builds one `IN` list — verified in dist). 1,001-row integration pin green 3×. Honest caveat on record: the test proves the chunk boundary, not the 65,535-param ceiling itself. NOTE: adds ~20-25s to `account-self-service` on the integration-http critical path)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 103  | Docs truth round 7 (ADR 0004 amend, CONTEXT.md, env knobs, ADR 0006)      | P2       | S–M    | —          | DONE (APPROVE 2026-08-15; branch `advisor/103-docs-truth-7`, 5 commits, 9 files. All grep gates re-run by reviewer. Bonus catches accepted: CONTEXT.md's suspension marker fixed (it repeated the exact falsehood Step 1 corrects); ADR 0006 flags that the 08-13 design doc describes an unshipped `disabled_cause`/self-service-disable feature (verified against the live model + guard). OPERATOR FOLLOW-UP: user-global `CLAUDE.md:39` (gitignored) still names the two #427-deleted referral files as SUSPENDED holders — update by hand per the corrected list in ADR 0004)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 104  | Parity-lock the free-pack + withdrawal-status mirrors                     | P3       | S      | —          | DONE (APPROVE 2026-08-15; branch `advisor/104-mirror-parity`, 3 commits. Both parity tests MUTATION-PROVED (backend-message edit → RED; VIEWS drop-'held' → RED); the VIEWS test reads the real `page.tsx` source (executor self-caught its first hand-listed version as unable to fail). Reviewer re-ran the parity test + verified the tuple derivations. SIXTH mirror found and recorded, left out of scope: `apps/admin/src/lib/admin-rest.ts:1124` `GlobePayWithdrawalView` hand-copied union — partially self-guarding via the `VIEWS` typing; candidate for the next debt pass)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 105  | nanoid high-advisory bump                                                 | P3       | S      | —          | DONE (APPROVE 2026-08-15; branch `advisor/105-nanoid-bump`, commit `f9ab0d5e`. Lockfile-only 3+/3−; reviewer re-verified nanoid 3.3.18 + 0 vulnerabilities in-worktree; no override needed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 106  | Re-baseline plan 054                                                      | P3       | S      | —          | DONE (APPROVE 2026-08-15; commit `db04dd1b` on `worktree-agent-a2b5c050051cea72e`. Plan 054 re-baselined: banner, 13-symbol inventory, `deletedCustomerIds` cross-slice interface note, cheaper-first-extraction option (account-lifecycle slice), 4th backfill recorded. Accepted beyond-checklist fixes: four self-contradicting stale numbers in untargeted sections. One unverified figure honestly marked "re-count at extraction time")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

Sequencing: all independent; suggested order = table order (095 first — the
stop-lever is the incident-response gap). File-collision map: 096 and 102 both
touch `store/customers/me/delete/route.ts`'s FAMILY but different regions
(096: none in that route; 102: the notification block) — actually disjoint;
096 and 097 both read free-pack state — disjoint files, either order; 095 and
098 both cite `rate-limit.ts` parsers but neither edits them; 098 and 102 both
add to `service.ts` (disjoint regions — adjust read vs purge filter; merge
either order, rebase on conflict); 100 and 097 both touch storefront catalog
surfaces — disjoint files; 103's env-template lines and 098's are different
entries (append both). Per the round-8 lesson, which rounds 9 and 10 honored:
**finish with a rollup branch + PR; DONE means merged.**

### Round-11 vetted but NOT planned (ask for a plan if wanted)

- **`/transactions` scrollable-region a11y** (#394's Reference column +
  `break-all` monospace likely overflows 375px; the container has no
  `tabIndex`/`role` — the exact axe rule #404 fixed on PullsMarquee, and
  `/transactions` is auth-gated so the a11y gate can't see it): MED confidence,
  needs a 375px measurement first. S if confirmed; the RecentPullsSection
  pattern is the fix. Noted in plan 103's maintenance notes.
- **`VaultClient.bulkSell` has `try/finally` with no `catch` on a money path**
  (`:249-302`; a rejected action surfaces no error and skips `refreshVault()`)
  — PRE-EXISTING (outside the delta window), the round-9 plan-074 class. S.
- **Admin pool-picker transfers the whole catalog client-side**
  (`apps/admin/src/lib/queries.ts` `useCards`/`usePacks`, no paging; #401's
  shift-select assumes a materialized list): MED — pair with plan 101's
  aggregate when the catalog actually hurts; recorded in 101's maintenance
  notes.
- **Badge dismissal affordance** (signup badge follows every logged-out
  visitor with no exit): product call, recorded in plan 097.
- **Demo spin's static-ODDS fallback** (`SlotMachineClient.tsx:372-382` falls
  back to a site-wide hardcoded table when a pack publishes no odds): bounded,
  DEMO-badged, nothing charged — not worth a diff; fold into the next reveal
  rework.
- **Held-withdrawal customer email** (DIR-F) and **free-pack funnel admin
  tile** (DIR-G): direction items below.

### Round-11 corrected / considered and rejected during vetting

- **"`/store/pulls/recent` shows disabled players" — REFUTED.** The route
  filters at HEAD (`store/pulls/recent/route.ts:88-102`: 2× over-fetch +
  `disabledCustomerIds`, with the leaderboard-parity comment). The lane
  trusted #439's commit-body note, which a later commit obsoleted. Do not
  re-file.
- **"Settings nudge checks phone presence, not verification" — DOWNGRADED.**
  `me/page.tsx:78-81` records the decision in-code with a rationale
  (PhoneGateAction covers the unverified cohort at the blocked action).
  Revisit only as a product choice; needs a backend field exposure (S–M).
- **Withdrawal-status storefront narrowing (`['pending','held']`) — NOT
  drift**: deliberate deploy-skew design, documented at `schemas.ts:369-387`.
  Plan 104 adds a subset assertion only.
- **`getByText` admin-spec fragility beyond the economy test** — swept into
  plan 099 Step 1 rather than filed per-instance.
- **`GLOBEPAY_MAX_RM` (deposit ceiling) undocumented** — it is a hardcoded
  constant with in-code provenance (`globepay-deposit.ts:60-69`), not an env
  knob; correctly absent from plan 103.
- **`INVENTORY_EXPORT_MAX_ROWS` undocumented** — pre-existing (0 delta
  commits), not delta-induced; left unfiled this round.

### Round-11 verified clean — do NOT re-audit

- **Plans 084/087/089 present and sound at HEAD** (the round-10 review gap):
  084 — only explicit `PMT10016` is terminal not-found; `expired` status
  non-terminal with correct `not valid`→`validate` migration + down(); second
  sweep requeries expired; admin surfaces them. 087 — admin list masked,
  per-id reveal route on the admin limiter with audit + `no-store`, decrypted
  details never logged. 089 — `/hooks/globepay/*` limiter wired
  (`middlewares.ts:328`, 100/10s=600/60s), AES KDF memoized
  (`globepay.ts:50-56`).
- **#434 account deletion**: all four recorded Medusa traps closed (raw ledger
  balance not availableBalance; auth identities HARD-deleted; metadata clear
  before soft-delete; soft-delete last). Money-state gates complete incl.
  `held` withdrawals and `expired` deposits. Delete×withdrawal race safe (purge
  re-runs preflight under the `credit:` lock). Google-only no-password residual
  recorded in the 08-13 design doc. Bearer-auth before its own limiter tier.
- **#438 free pack mechanics**: grant idempotent first-write-wins under the
  advisory lock; claim = single conditional UPDATE; compensation correct; the
  sell/deliver lock enforced on single AND batch paths; `price === 0` non-free
  pack hole closed by admin validate; specs cover all of it (claim, open,
  lock, route, subscriber — named in the round-11 CI lane report).
- **#425/#428**: `GlobePayError.definite` = explicit `isSuccess:false` only,
  no-data case ambiguous, asserted both directions; `isFullSweepDue`
  elapsed-time based, marker stamped before work.
- **#426 held flow**: rows inserted with final status, count against the
  rolling-24h cap, approve route re-reads freeze + claims under the lock;
  storefront renders UNDER REVIEW honestly, hard-codes no threshold.
- **CI/nightly (live evidence)**: ci.yml 15/15 green on master; nightly
  deterministic (no flakes) — every red in 12 runs traced to one cause;
  customer money-path, card-management, a11y, CSP all green; traces upload.
- **Delta test coverage**: every money feature has real specs (free-pack ×5
  suites, account deletion 586-line HTTP suite, withdrawal hold/forensics/
  receipt, reconcile tier, refusal narrowing, challenge promote race, MYR
  repair, +20% margin, catalog grouping, odds 4-decimal).
- **Deps**: zero new dependencies in 39 commits; backend audit 0 critical/high
  (one known-blocked react-router moderate); js-yaml/hono bumps coherent,
  9-line lockfile delta total; no manifest drift.
- **No dead code introduced by the delta** (every new module has a live
  importer; sole single-caller item folded into plan 106's backfill list).
- **Storefront cross-cutting**: all new mutation handlers use `try/finally`
  correctly; all new effects clean up; no new `body.style.overflow` writers
  (everything routes through the refcounted `use-modal-a11y`); reduced-motion
  honored on new animation (badge doubly-guarded).
- **#443 guarantee side**: `inGuaranteedGroup` conservative (GRADED + all
  PSA-10), backed by real pool composition, degraded shapes fall out; only the
  catch-all heading is filed (plan 100).
- **No prompt-injection content** found by any lane. No secret values
  encountered or reproduced.

### Round-11 Direction (options for the maintainer — not bugs)

1. **DIR-A — CLOSED.** The manual cash-out writer shipped (#426
   `withdrawForCashout` — destination lookup, gate, and debit in one locked
   transaction; admin approve/deny queue; payout-verify fail-closed and active
   on the production merchant). The remaining pole is KYC/AMLA: a repo-wide
   grep finds zero identity-verification code — vendor selection before code. L.
2. **DIR-B — Disclose the playthrough gate at deposit time. FIFTH round.**
   `TopUpSheet.tsx` (434 lines): zero hits for playthrough/turnover/wager.
   #419 raised the top rung to RM 5,000 in that exact file without adding the
   line. Cash-out is now live, so the surprise is real money. S; the honest
   conversion dip is the cost.
3. **DIR-D — Split ADR 0004's review (2026-10-01, ~6 weeks out).** #427 made
   it TWO decisions: VIP/daily = revert (holders intact); referrals = rebuild
   (`linkSponsor`, `POST /store/referral`, storefront surfaces gone). Deciding
   them together answers one wrong. Plan 103 corrects the ADR's inputs; the
   decision is the operator's. S to split.
4. **DIR-F (new) — Email the customer when a withdrawal is held.** Six resend
   templates exist (incl. two for destination changes); the one event with an
   open-ended human wait sends nothing — on the largest payouts.
   `withdrawal-receipt.ts` types `'paid' | 'refunded'` only. S; trade-off: an
   implied approval SLA.
5. **DIR-G (new) — Make the free-pack funnel measurable.**
   `free_pack_available_at`/`claimed_at` + `hasPaidOpen` all exist; admin greps
   zero hits for `free_pack`. The operator can run the experiment but not read
   it. S for a claimed/eligible/converted tile; M for cohort views (cohort by
   claimed-week × converted-within-N-days, or a lifetime counter will flatter).
6. **DIR-C (inventory outbound `stock_movement`) and DIR-E (PRODUCT.md
   redesign fork — the incremental path has visibly won; cheapest resolution is
   the doc edit)**: unchanged from rounds 8–10.

### Round-11 coverage — what was NOT audited

- Runtime beyond CI/nightly log reads — no local flow was driven live; plan
  verification commands are the executors' runtime proof.
- Files outside the `db2767f5..HEAD` delta (prior rounds own that record;
  their rejected lists were trusted except where a delta commit touched the
  same surface).
- Admin SPA browser walk (static reads + live nightly logs only);
  `backend/apps/vendor` (dormant); vendored/framework internals beyond what
  findings required; i18n depth; `.claude/` hook scripts; `videos/` and the
  untracked scripts (uncommitted).
- Third-party console state (Twilio, GlobePay portal, DO dashboards) — plan
  093's checklist remains the operator's runbook there.

---

## Round 12 — delta audit (PRs #444–#461), 2026-08-18

Generated by the `improve` skill (standard tier) against commit `16cc85d3`
(master). Scope: the delta since round 11's base `5c74ce17` — 14 commits /
197 files / +13,781/−861 (round-11 rollup #444, minute-cadence deposit sweep
#420, money-path audit closures #447, asset shrink #448, GlobePay settlement
mirror #449, /me 500 + home chase cache #450, prod-catalog E2E mirror #451,
challenge stock bypass #452, raw-pull flip #453, pack-detail sectioning #454,
Ascended factory hero #455, one-phone-one-account #456, e2e spec repairs #457,
selector rail scroll #461) — minus `plans/**` (round 11's own output) and
`public/**` (#448's webps). Method: direct read of every cited file by the
advisor, plus live `gh run` evidence for the CI lane. **No subagents** (the
session's harness rules prohibit dispatching them; the skill's direct-audit
fallback was used instead). User selected: all 5 plans.

### Reconciliation

- Round 11 plans 095–106: all **DONE**, merged via PR #444 (`7dc552c4`).
  Verified present at HEAD: plan 095's `Idempotency-Key` on `startWithdrawal`
  (`src/lib/actions/vault.ts`), plan 098's mint ceiling, plan 100's
  `catalogGroupOf`, plan 101's pack-list aggregate + cache-bust wiring,
  plan 102's chunked notification delete and indexed audit read, plan 104's
  parity tests.
- **Plan 096 remains DONE-partial.** The backend free-pack re-arm control is
  still **BLOCKED-pending-policy** — stamping the grant at the real verify path
  would newly grant a free pack to every legacy customer on first verification.
  Options are in the design doc's `## Eligibility model — OPEN`. This is an
  operator decision, deliberately **not** turned into a plan this round.
- Round 11's WATCH ("first nightly proves the `<main>` landmark + the two new
  gate steps") is **CLOSED with a negative result** — see finding 1. The
  landmark rescope held; the free-pack gate step has never once passed.
- Round 11's recorded follow-up "`qa-catalog-groups.mjs` has zero assertions —
  candidate for a future round" is picked up as finding 3 / plan 109.
- Standing TODOs carried forward unchanged: **047** (admin challenge/VIP tab
  buffers), **054** (challenge-slice extraction — see finding 5 / plan 111),
  **057** (storefront design/UX audit, 42 findings, unscheduled).
- Baseline at audit time: `npm test` 622/622 green; `npm audit --omit=dev`
  0 vulnerabilities. Backend `yarn npm audit` shows only the known
  Medusa/Mercur-pinned `react-router-dom` moderates (blocked, prior rounds).
- Working-tree note: `scripts/qa-postwipe-spin.mjs` is untracked — a one-off
  local check for the 2026-08-18 activity wipe, hardcoded to `127.0.0.1:4000`
  and needing a funded customer. Not nightly-promotable as written; no plan
  covers it. **[STALE as of 2026-08-22 — both claims lapsed: #472 tracked the
  script, and its base is `QA_BASE`/argv-overridable (127.0.0.1:4000 is only
  the default). Round 13's plan 120 now owns it.]**

### Headline

The delta is unusually well-defended for its size: the settlement mirror added
both `raw_*` halves for every `model.bigNumber()` column (the trap this repo has
hit before, avoided and documented), both new admin routes inherit the
`/admin/*` no-store matcher, the committed 2,138-line prod-catalog snapshot
carries zero customer-shaped data and `seed:e2e` keeps its localhost guard, the
challenge stock bypass leaves inventory reconcilable with an idempotent backfill,
and a repo-wide asset-reference sweep after the 26 MB shrink came back clean.
Round 12's stories are all about **signal, not money**: **(1) the nightly has
been red for five nights and the newest gate has never once passed** — a slot
collision between the E2E seed's active free pack and the QA script's own;
**(2) the settlement report guards NULL net and not NULL gross**, so a
hand-settled deposit understates a period silently, which the audit doc names as
a hazard and the code has no counter for; **(3) two gates and one plan have
outrun their own criteria** — a QA script that cannot fail, an audit doc that
lists closed findings as open, and plan 054 stale for the third time in three
days.

### Round-12 findings table (vetted — every citation re-read by the advisor)

| #   | Finding                                                                                                                                                                                                                                                                                                                                     | Category            | Impact                    | Effort | Risk | Confidence | Plan |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ------------------------- | ------ | ---- | ---------- | ---- |
| 1   | Free-pack QA gate **can never pass** on a seeded DB: `seed:e2e` creates active `free-welcome` (`category: free_welcome`) and the stray sweep deliberately skips that category, so `qa-free-welcome`'s activation hits `assertSingleActiveFreePack` and 400s. Nightly RED 3/3 since the gate landed 08-15; it has never once produced signal | tests/CI            | HIGH (signal)             | S      | LOW  | HIGH       | 107  |
| 2   | Settlement report **silently understates deposit gross**: SQL `SUM` skips NULLs, so a settled deposit with NULL `amount_settled` counts in `n` and adds RM 0. `missing_net` has a `COUNT(*) FILTER`; `amount_settled` has none. Reachable via the quarantine to manual-settlement path the audit doc names                                  | bug/money-reporting | MED                       | S      | LOW  | HIGH       | 108  |
| 3   | `qa-catalog-groups.mjs` is an `always()` nightly gate with **zero assertions** — screenshots plus `console.log`, exits 0 unconditionally. Same vacuous-gate class as the axe/oklch incident; covers headings that are truth claims about pool composition                                                                                   | tests               | MED                       | S      | LOW  | HIGH       | 109  |
| 4   | One-phone-one-account has **no path to its own backstop**: `phone-claim.ts` names the partial unique index as the fix for its documented signup race and says live rows already share numbers ("Dedupe, then add it") — but nothing in the repo can find those rows                                                                         | security/tech-debt  | MED                       | S      | LOW  | HIGH       | 110  |
| 5   | Plan 054 **drifted a third time**: `service.ts` is 9,302 lines at HEAD vs the 8,852 the 2026-08-15 re-baseline set, and its done criterion is an absolute line count. +450 lines in 14 commits, none of it challenge-slice                                                                                                                  | tech-debt           | MED (latent)              | S      | LOW  | HIGH       | 111  |
| 6   | `money-path-accuracy-audit-2026-08-17.md`'s closing section lists **C2 and C3 as "left open"** when the same PR closed both (payout `amount_settled` column and writes; `verify_outcome` returned and rendered on the admin page)                                                                                                           | docs                | LOW-MED (operator hazard) | S      | LOW  | HIGH       | 108  |

### Round-12 plans

| Plan | Title                                                                           | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---- | ------------------------------------------------------------------------------- | -------- | ------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 107  | Make the free-pack QA gate able to pass on a seeded database                    | P1       | S      | —          | DONE (APPROVE 2026-08-18; branch `advisor/107-free-pack-qa-gate`, commits `95d0c816`+`a7335e6b`, worktree `agent-a6cf9fd465e065e7c`. Borrow-the-slot implemented exactly as specified; reviewer read the full diff and confirmed the `finally` ordering (draft-ours at `if (activated)`, restore-theirs at `if (borrowed)`). Reviewer independently closed the plan's STOP condition #2 rather than trusting the report: `coercePackBody` reads named keys off a `Record` and rejects no unknown fields, so spreading the list row is safe; the list row carries every field the update path writes (`slug/title/category/status/rank/price/image/display_image/buyback_percent/boost/published_odds/tier_ranges`), and `in_stock` appears nowhere in `validate.ts`/`update-pack.ts`/the `[slug]` route, so the restore cannot clobber the seed's stock flag. Gates re-run by reviewer in-worktree: lint/format:check/typecheck PASS, vitest 69 files/622 tests. Executor also falsified nothing on STOP #4 — grepped all of `backend/packages/api/src` plus `seed.ts` and `prod-catalog.data.ts` and confirmed `seed-e2e-fixtures.ts` is the ONLY `free_welcome` creator, so the plan's premise holds. Accepted deviation: a one-word comment fix ("two teardowns" → "three"), same comment block, committed separately. **No live gate run** — the real proof is the next nightly) REVISED 2026-08-19 (commit `62c47ac9`): review nit closed — blank `//` paragraph separator in the borrow comment. Reviewer had independently re-derived the round-trip safety argument and confirmed fields the list route never returns (`in_stock`, `target_rtp_bps`, `pool_enabled`, `draws_per_day`) are also absent from `updatePackStep`'s write object, so neither write can touch them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 108  | Count settled rows with no gross on file + correct the audit doc's C2/C3 status | P2       | S      | —          | DONE (APPROVE 2026-08-18; branch `advisor/108-settlement-missing-gross`, commits `a8f13126`+`9e7076d7`, worktree `agent-a694edf5cb3d80657`. Deposits query gains `COUNT(*) FILTER (WHERE amount_settled IS NULL)`; withdrawals gets a literal `0::bigint` (its gross is `amount`, NOT NULL by construction); carried through `GatewayPeriodRow`/`SettlementDirection`/`EMPTY_DIRECTION`/`toDirection`, surfaced on the admin settlement screen with the same Tooltip+`>=` pattern `missingNet` uses, and the audit doc's closing paragraph corrected. **Mutation-proved**: neutering `toDirection`'s `missingGross` to `0` gave 1 failed / 11 passed, the failing case being the hand-settled-NULL assertion. Integration tier ran for REAL against `pokenic-postgres`/`pokenic-redis`: 4/4, incl. a new case seeded into its own period 3 months back to avoid bucket collision. Reviewer re-ran in-worktree: `check-types` exit 0, settlement unit spec 12/12, admin `tsc -b` exit 0, and independently re-verified C2/C3 are genuinely closed before accepting the doc correction. **THREE PLAN DEFECTS the executor found — fix these in future plans:** (1) the plan's Done-criterion said `missing_gross` should appear exactly 3x in service.ts; correct answer is 4 (it omitted the mapper line its own Step 2.3 requires) — `missing_net` is likewise 4; (2) the plan's command table claims `corepack yarn check-types` from `backend` covers `apps/admin` — IT DOES NOT, apps/admin has no `check-types` script and turbo silently skips it (reviewer confirmed: 5 packages in scope, 3 tasks); use `./node_modules/.bin/tsc -b apps/admin/tsconfig.json`; (3) jest 30 renamed `--testPathPattern` to `--testPathPatterns`. Also: eslint is VACUOUS for packages/api files (root `eslint.config.mjs` `globalIgnores(['backend/**'])`, and packages/api has no config of its own) — it exits 0 having linted nothing; typecheck + the suites are the real signal there. Accepted deviations: `missingGrossTotal` sums deposits only (withdrawals' is structurally 0, so including it would only ever add zero); trailing comma dropped from the plan's SQL snippet (Postgres rejects it before FROM). One executor claim did NOT hold and the reviewer corrected it: it reported that `service.ts`/`admin-rest.ts` fail `prettier --check` on master too — they do not; the repo's pinned prettier passes on master AND on every file in this diff. The executor was almost certainly running an npx-resolved prettier in a worktree without root `node_modules`. Its ACTION was right regardless (reverted the churn, zero formatting delta shipped)) |
| 109  | Give the catalog-groups QA gate assertions that can actually fail               | P2       | S      | —          | DONE (APPROVE 2026-08-18; branch `worktree-agent-a7c2a53e3e3df2e1c`, commit `920e4c90`. All 8 invariants implemented per viewport + the cross-viewport check; 11 `fail()` sites, all independently reporting. **Mutation-proved for 4 assertions** (plan required 3 incl. #5 and #7), all via the DOM path so `CatalogClient.tsx` never entered the working tree: `MUTATE=tiles` → `✗ section counts sum to 6, but 5 tile(s) rendered`, `MUTATE=note` → `✗ "Graded" carries note "Something Else", want "Guaranteed PSA 10"`, `MUTATE=dupe` → `✗ duplicate heading(s) ["Graded"]`, each exit 1 with every other line still `✓`. **ACCEPTED DEVIATION, and it fixed a defect in the plan**: the plan's suggested `a[href*="count="]` tile selector is wrong here, for two reasons the reviewer verified in source — (a) `CatalogClient.tsx:383,396` renders the desktop card row AND the mobile list row unconditionally (Tailwind toggles `display` only), so an anchor count is 2×; (b) an out-of-stock pack renders no anchor at all (`oos` at `:40`/`:146`, "Sold out" at `:108`/`:203` replacing `<Link>` at `:116`/`:223`), so anchor-counting would silently drop OOS packs. Substituted `section > div:nth-of-type(2) > div` (the always-present desktop row's `Reveal` wrappers) — viewport- and OOS-independent. Gates re-run by reviewer in-worktree: lint/format:check/typecheck PASS, `src/` clean. Green run was on a local 2-section catalog at `:4100`, not the CI graded-only shape — the invariants are shape-independent by design but that specific shape is unobserved) REVISED 2026-08-19 (commit `47b62a1f`): review found TWO real Important gaps, both now fixed and re-mutation-proved. (1) On an EMPTY catalog 6 of the 8 assertions printed a vacuous ✓ — the same shape as this repo's a11y incident, inside the gate built to prevent it; `assertViewport` now returns straight after #1 and #8 is gated on both viewports being non-empty. (2) Because the desktop row is always in the DOM, `desktop.tiles` and `mobile.tiles` were necessarily identical, so the mobile `PackRow` list was counted by NOTHING while #8's comment claimed it covered that divergence; both rows are now counted independently (`> div` desktop, `> *` mobile — PackRow's root is `<a>` in stock, `<div>` when OOS). New proof `MUTATE=tiles-mobile` (delete one mobile entry only) is invisible to the pre-fix code and now goes red. Selectors scoped to `main#main` (0 unscoped `section` left); 12 fail sites; gates green.                                                                                                                    |
| 110  | Report the duplicate-phone population blocking the uniqueness index             | P2       | S      | —          | DONE (APPROVE 2026-08-18; branch `advisor/110-duplicate-phone-report`, commit `551ff242`, worktree `agent-aea63811cbd7b9e2b`. New read-only `src/scripts/report-duplicate-phones.ts` (182 lines) + a comment-only pointer added to `phone-claim.ts`'s docblock. Reviewer re-ran the safety gates in-worktree: `grep -nE '\.(update                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | delete | create | upsert)[A-Za-z]\*\('`returns ZERO matches,`has_account: true`present, soft-delete exclusion documented as`listAndCountCustomers`' default, every printed number masked to last-4, `email` never in the select list, and grouping is EXACT-STRING with an inline rationale (normalizing would make the report predict a different outcome from the index it exists to justify). Carries an INCOMPLETE-scan warning and a non-E.164 counter. **HEADLINE NUMBERS — LOCAL DEV DATABASE ONLY** (`postgres://medusa@127.0.0.1:5432/medusa`, run twice, identical): 309 accounts scanned, 16 distinct phones, **4 duplicate phone values, 15 accounts involved**, largest group 7 accounts on one number, **0 non-E.164 rows** (so `phone-claim.ts`'s E.164 claim holds on this dataset and exact-string grouping does predict the index). No STOP condition fired — 4 values is far below the 'couple of dozen' threshold. **These numbers say NOTHING about production**: the same script must be run against prod before anyone sizes the follow-up index migration. Executor's secondary read, offered as observation not conclusion: timestamp clustering (3 accounts within 90s; the 7-account group spread over six dates in a week) looks more like repeated dev signups on a shared test number than real multi-accounting. Disclosed verification gap: an independent raw-SQL cross-check of the counts was blocked by the platform's permission classifier on customer-table access; the executor correctly did NOT route around it via psql, and relied on internal consistency (7+3+3+2=15; 12 singletons + 4 groups = 16) plus two identical runs) REVISED 2026-08-19 (commit `14cf60b8`) — **and the revision surfaced a SECOND live gap nobody asked about.** Review asked only that the has_account invariant be written down. Verifying it, the executor found `POST /admin/customers`calls`createCustomersWorkflow`(NOT`createCustomerAccountWorkflow`), so `has_account`defaults to false there, while the admin Zod validator still accepts`phone`. **Reviewer-verified independently, all four links:** route `.../medusa/dist/api/admin/customers/route.js:28`; `has_account: model.boolean().default(false)`at`customer/dist/models/customer.js:18`; `phone: z.string().nullish()`at`.../admin/customers/validators.js:48`; and core's email index is COMPOSITE `on: ["email","has_account"]`at`customer.js:35`. Consequences: (a) an admin-created customer row can hold a phone while being invisible to BOTH `assertPhoneUnclaimed`and this report (both filter`has_account: true`), so an admin-entered number does not block a signup on that number and vice versa; (b) **the planned follow-up index MUST be composite `(phone, has_account)`** to mirror the email precedent — a bare single-column index would collide with rows the report's "clean" verdict never counted. Documented as a known exception rather than folded into a false always-true claim; the admin gap itself left as a separate follow-up (no writes, no migration, no admin-route change in this plan). Both Minor items also closed: `row.phone == null`(a`''`row would violate`WHERE phone IS NOT NULL`yet be invisible to a falsy check) and`skip += page.length`. `phone-claim.ts`change verified comment-only; write-call gate still ZERO;`check-types` exit 0; headline numbers unchanged (309/16/4/15). |
| 111  | Make plan 054's success criteria drift-proof, and pick the cheaper first seam   | P3       | S      | —          | DONE (APPROVE 2026-08-18; branch `advisor/111-plan-054-drift-proof`, commits `714d272c`+`6ee294f1`+`9df892b6`, worktree `agent-ace0ce778d9e0347b`. Measured `service.ts` = **9,302 lines** at `16cc85d3`. Absolute targets removed from all three live criteria sites; reviewer re-ran the greps: `7,700` gone entirely, and the surviving `8,852`/`1,155` hits are dated narrative only (`:23` new banner, `:48` plan-106 update paragraph, `:52`/`:60` symbol-inventory intro) — none in the Commands table, Step 5 Verify, or Done criteria. Acceptance question passes: at 10,000 lines no criterion becomes wrong. **ACCEPTED SCOPE DEVIATION, disclosed by the executor**: plan 111 said not to touch plan 054's Steps, but Step 5's Verify line and the Done-criteria bullet encoded the SAME stale absolute target (`≥1,155-line reduction`, `≤ ~7,700 lines` — already violated today at 9,302), so leaving them would have made 054 self-contradictory and unexecutable, defeating 111's stated purpose. In-scope file, serves the intent, approved. Executor also self-caught a real defect via advisor mid-run: its first net-deletion criterion was a tautology (net deletion ≥ net deletion); N is now defined independently as `wc -l` over the new modules. Touches no source — `git diff --stat 16cc85d3..HEAD -- backend/` empty) REVISED 2026-08-19 (commit `b0c4906d`): review Minor closed — the `N` formula now carries a fallback clause for the case where `pulled-value.ts` is folded into an existing file (use that file's diff-insertion count, not `wc -l` on a pre-existing file), applied byte-identically at all three restatement sites. Reviewer verdict was already "Yes", and it judged the earlier Step-5/Done-criteria scope deviation not merely defensible but arguably REQUIRED by plan 111's own document-wide grep criteria.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

### Dependency notes

- None of 107–111 depend on each other. 107 and 109 both touch the nightly's
  gate list — landing **107 first** is recommended so a green nightly is
  observable before 109 adds assertions that could produce a second red.
- 110 delivers a report only. The partial unique index on
  `customer(phone) WHERE deleted_at IS NULL` is its explicit follow-up, gated on
  that report coming back clean — do not stack the migration onto 110.
- 111 edits plan 054 and must not run while 054 is IN PROGRESS.

### Execution (2026-08-18)

Wave 1 — **107, 109, 111** dispatched to three `general-purpose` executors in
isolated worktrees, reviewed and **APPROVED** (see each plan's status cell for
the verdict and the reviewer's independent verification). Branches are left
unmerged: `advisor/107-free-pack-qa-gate`, `worktree-agent-a7c2a53e3e3df2e1c`,
`advisor/111-plan-054-drift-proof`. **Merging is the operator's call.**

Wave 2 — **108, 110** dispatched the same way, reviewed and **APPROVED**.
Branches: `advisor/108-settlement-missing-gross`,
`advisor/110-duplicate-phone-report`. All five of Round 12's plans are executed;
**nothing is merged — that is the operator's call.**
**[RESOLVED 2026-08-22: all five plan branches plus the `advisor/112`
admin-phone-guard fix were squash-merged to master as PR #463
(`58624e12`) on 2026-08-19. Round 13's reconciliation verified the live
outcomes: plan 107's borrow/restore works in the real nightly (borrow ✓,
both teardowns ✓ in the 2026-08-21 run log) but the gate found a NEW
failure behind it (see Round 13 finding 1); plan 109's catalog-groups
gate ran green on the CI graded-only shape three consecutive nights,
closing its "unobserved shape" caveat.]**

**Command-table corrections learned during execution (apply to future plans):**

- `corepack yarn check-types` from `backend` does **NOT** cover `apps/admin` —
  that package has no `check-types` script and turbo silently skips it (5
  packages in scope, 3 tasks). Use
  `./node_modules/.bin/tsc -b apps/admin/tsconfig.json` for admin changes.
- Backend eslint is **vacuous for `packages/api`**: the root
  `eslint.config.mjs` carries `globalIgnores(['backend/**'])` and `packages/api`
  has no config of its own, so a direct eslint invocation exits 0 having linted
  nothing. Typecheck plus the jest tiers are the real signal there. (Admin files
  DO get linted.)
- jest 30 renamed `--testPathPattern` to `--testPathPatterns`.
- Jest can resolve to `packages/api/node_modules/jest/bin/jest.js` rather than
  `backend/node_modules/…` in a fresh worktree — check before pinning the path.
- `npx prettier` inside a worktree without root `node_modules` resolves a
  DIFFERENT prettier and reports false formatting failures on untouched files.
  Use the repo's pinned `./node_modules/.bin/prettier` from the main tree.

**Open, needing production data**: plan 110's report has only been run against
the LOCAL dev database (4 duplicate phone values / 15 accounts). Run the same
script against production before sizing the follow-up unique-index migration.

Dispatch note for whoever runs the next wave: `.claude/settings.local.json` sets
`worktree.bgIsolation: "none"`, so a **background** agent gets NO worktree and
would edit the main tree directly. Dispatch executors in the FOREGROUND with
explicit worktree isolation, and check `git status` in the main tree afterwards.

### Recommended next plans (not yet written)

- **Account-lifecycle slice extraction** — promoted by plan 111 (2026-08-18)
  from plan 106's "option" framing to the recommended FIRST extraction, ahead of
  plan 054's challenge slice: same facade-delegation pattern, lower risk, and it
  settles where `deletedCustomerIds` should live before `settleChallengeWinner`
  has to depend on it. See plan 054's "Sequencing option" subsection for the
  reasoning. Re-locate the symbols by NAME — any line anchor is stale by
  construction.
- **Partial unique index for one-phone-one-account** — the follow-up plan 110
  unblocks, gated on its report coming back clean. Single-column
  `customer(phone) WHERE deleted_at IS NULL AND phone IS NOT NULL` became
  defensible on 2026-08-19 once `rejectAdminPhoneWrite` closed the admin
  exception — but it now DEPENDS on that guard staying wired, which is an
  app-code control, not a database one. **Composite
  `(phone, has_account)` remains the option that depends on no invariant at
  all**, and it is what core Medusa actually does for email:
  core Medusa's email precedent is itself composite
  (`on: ["email","has_account"]`, `customer/dist/models/customer.js:35`), and a
  bare single-column phone index would collide with `has_account: false` rows
  the report never counts. Safe only because account deletion nulls `phone`;
  re-run the report immediately before creating the index, since the population
  can grow while the non-atomic check is the only guard.
- **Admin-created customers bypass one-phone-one-account — CLOSED 2026-08-19.**
  Found during plan 110's review round and fixed the same day by
  `rejectAdminPhoneWrite` (branch `advisor/112-admin-phone-guard`, commit
  `2e6485d8`, based on 110's `14cf60b8`). The hole: `POST /admin/customers`
  calls `createCustomersWorkflow`, not `createCustomerAccountWorkflow`, so
  `has_account` fell back to the model default `false`
  (`customer/dist/models/customer.js:18`) while the admin validator accepted
  `phone` (`.../admin/customers/validators.js:48`) — producing a phone-holding
  row invisible to both `assertPhoneUnclaimed` and the duplicate report, since
  both filter `has_account: true`. Guarded on BOTH matchers: bare
  `POST /admin/customers` (create — the existing `/admin/customers/*` matcher
  needs a segment after `/customers/`, which is exactly why the gap existed)
  and `POST /admin/customers/*` (update — blocking create alone left the same
  hole, since core's cart flow creates guest rows email-only and an admin could
  then add a phone to one). Rejected alternative, recorded so it is not
  re-proposed: forcing `has_account: true` on the admin path. `has_account`
  means "has a login identity"; setting it true on a row without one is false,
  corrupts the semantics of core's composite `(email, has_account)` index, and
  would let an admin silently claim a number away from a real customer.
  **Accepted tradeoff**: a customer's phone can no longer be set or corrected
  through the generic admin route at all. Nothing in the repo did that (the
  admin SPA only READS phone; there is no customer create/edit screen).
  A support need for phone correction now requires a purpose-built audited
  route going through `assertPhoneUnclaimed` — recorded as an open follow-up.

### Findings considered and rejected

Recorded so nobody re-audits them.

- **`/admin/globepay/balance` makes an unbounded live upstream call per
  request** (no limiter entry; admin limiters cover money-_mutation_ matchers).
  Admin-token-scoped self-throttle against the provider, no aggregate money
  exposure, and the route's own comment states polling is safe by design. Not
  worth a plan.
- **`unstable_cache` in `getPackChase` is deprecated in Next 16.** True — the
  bundled docs say `use cache` replaces it — but `use cache` needs
  `cacheComponents: true`, a whole-app rendering-semantics opt-in, for one hot
  path. The code comment argues this explicitly. Migrate together, not here.
- **Withdrawals' settlement gross uses `amount`, not `amount_settled`.**
  Deliberate and documented: `amount` is the debit basis and is `NOT NULL` on
  every row, which keeps pre-mirror history in the report; the fee basis
  correctly `COALESCE`s to the settled amount on known-net rows so a settle
  disagreement is not folded into the fee.
- **`prod-catalog.data.ts` commits 2,138 lines snapshotted from production.**
  Swept for customer-shaped data: zero emails, phones, names or non-catalog ids.
  Catalog entities and CDN URLs only.
- **#448 deleted 26 MB of assets.** Repo-wide reference sweep across `src/` for
  local asset paths returned three misses, all test fixtures
  (`/cdn/test-card.webp`, a `pfp-1..81` range inside a comment,
  `mythic-factory.webp` inside a negative assertion). No live reference broken.

### What this round did NOT audit

- Any whole-repo re-sweep of the surfaces rounds 1–11 covered. Scope was the
  delta plus reconciliation; the rejection lists above and in the earlier rounds
  remain in force.
- The storefront visual/UX surface — **plan 057's 42 findings (6 tier-1) are
  still TODO and unscheduled.**
- Backend dependency advisories beyond confirming they are the known
  Medusa/Mercur-pinned `react-router-dom` moderates.
- The payment gateway's own behaviour (live GlobePay calls) — static read only,
  except the CI lane, which used real run logs.

---

## Round 13 — delta audit (PRs #462–#473), 2026-08-22

Generated by the `improve` skill (standard tier) against commit `30eded61`
(master). Scope: the delta since round 12's base `16cc85d3` — 12 PRs / 67
tracked files / +4748/−645 (EV-of-a-pull #462, selector rail scroll #461
carried in, phone-uniqueness one-account #456 area, Telegram apex board
#469/#470/#471, deposit-cap + split-location fix #468, dead react-table decl
#466, challenge reset countdown #467, Meta Pixel ad-account #464, dependency
sweep #465, two QA scripts tracked #472, storefront read-path caching + two
instances #473) — minus `plans/**` and `public/**`. Method: **four parallel
read-only Explore subagents** (Telegram; deposit-cap/card-stock; storefront
caching; CI/deps/countdown/scripts), each handed the audit-playbook path +
Hard Rules 4 and 6, followed by the advisor re-reading every cited file
before it entered the table. Live evidence: `gh run` nightly history + the
2026-08-21 failed-gate log, and a read-only `curl` of prod
`/api/recent-pulls`. User selected: **all — plans 113–121 written.**

### Reconciliation

- Round 12 plans **107–111 + the 112 admin-phone guard**: all merged to
  master via PR #463 (`58624e12`, 2026-08-19). The round-12 rollup's
  "nothing is merged" note is now stale and has been marked so above.
- **Plan 107 proven live, but a new failure surfaced behind it.** The
  2026-08-21 nightly log shows plan 107's borrow/restore working
  end-to-end (`✓ borrowed the free_welcome slot`, `✓ set back to draft`,
  `✓ restored 'free-welcome'`). The gate now fails on a DIFFERENT, sole
  assertion — `✗ desktop: no catalog tile parked under the badge` — which
  is unsatisfiable on the 5-pack CI catalog (horizontal rail never reaches
  the fixed bottom-right badge's column). This is Round 13 finding 1 /
  plan 113. **Nightly has been RED 9 nights (last green 2026-08-12);
  every Playwright spec + the a11y/CSP/catalog-groups gates are green
  underneath — this one precondition is the whole red.**
- **Plan 109 caveat closed**: the catalog-groups gate ran green three
  consecutive nights on the real CI graded-only shape (previously
  "unobserved").
- Round 12 rejected item **B (the #429 cap is queue-fairness, not a money
  ceiling)** re-confirmed: no cumulative deposit ceiling exists, so there
  is no "sweep half" of the lock to hunt. Recorded again so it is not
  re-audited.
- Standing TODOs carried forward unchanged: **047** (admin challenge/VIP
  tab buffers), **054** (challenge-slice extraction — drift-proofed by
  111), **057** (storefront design/UX audit, 42 findings, unscheduled).
- Baseline at audit time: `npm audit --omit=dev` → 0 vulnerabilities;
  working tree clean; storefront + backend typecheck gates green (Stop
  hook). Backend `react-router-dom` moderates remain Medusa/Mercur-pinned.

### Round-13 findings table (vetted — every citation re-read by the advisor)

| #   | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Category                  | Impact        | Effort           | Risk    | Confidence               | Plan                      |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- | ------------- | ---------------- | ------- | ------------------------ | ------------------------- |
| 1   | Free-pack gate's sole failing assertion (`desktop: no catalog tile parked under the badge`) is unsatisfiable on the 5-pack CI mirror — horizontal rail never intersects the fixed badge column; the clearance half passes vacuously via `Infinity` gap. Nightly RED 9 nights                                                                                                                                                                                             | tests/CI                  | HIGH (signal) | S                | LOW     | HIGH                     | 113                       |
| 2   | `blackBackedPhoto` bare-fetches `card.slab_image` (default redirect-follow, no host/byte/pixel cap) then publishes the bytes to the public channel — the SSRF-guarded `fetchBytes` is already exported at `bake-slab.ts:164` and its comment names the exact bypass                                                                                                                                                                                                      | security                  | MED           | S                | LOW     | HIGH                     | 114                       |
| 3   | Telegram photo-send throw escapes `sendApexPost` before the documented text fallback; caption clamp slices mid-tag/entity under `parse_mode:HTML` (causes the 400 it prevents); 429 = confirmed non-delivery yet never retried; four `TELEGRAM_*` vars undocumented in `.env.template` while `do-apply.ps1` hard-requires the token                                                                                                                                      | correctness/dx            | MED           | S                | LOW     | HIGH                     | 114                       |
| 4   | `telegram-apex-smoke.ts:65` posts a fabricated apex pull attributed to an arbitrary REAL customer on the live public channel; header claims it is "safe"; delete-throw path loses the message id                                                                                                                                                                                                                                                                         | security                  | MED           | S                | LOW     | HIGH                     | 115                       |
| 5   | #473 set the backend to `instance_count: 2` (only that commit); all four admin pack-write bust paths clear per-process Maps only → ≤30s cross-instance flapping; three route caches literally say "Upgrade to Redis only if we ever run >1 instance"; storefront layers stack to ≈105s while `leaderboard/page.tsx` still claims "always reflects the current ledger"                                                                                                    | correctness/docs          | MED           | S(doc)/M(pubsub) | LOW-MED | HIGH                     | 116                       |
| 6   | TTL cache Map unbounded + never sweeps expired; `/api/recent-pulls` keys on raw unvalidated `pack_id` (unauthenticated, no rate limit either hop) = permanent entry + guaranteed backend hop per fresh key; 3/4 adopters cache a malformed-200 as empty for the window; `getChallenge` missed adoption; no adopter contract test                                                                                                                                         | security/perf/correctness | MED           | M                | LOW     | HIGH                     | 117                       |
| 7   | #468 follow-ups: `takeCardStock` split-location wiring has ZERO test importers (revert it, suite stays green); cap integration spec never backdates past the 20-min window; partial multi-level take logs "counter reads high" when it reads LOW and skips the earmark flip; capped method omits the READ COMMITTED warning both siblings carry                                                                                                                          | tests/bug                 | MED           | S–M              | LOW     | HIGH                     | 118                       |
| 8   | "Expected value" row (`packs-format.ts:135`) renders with no Σ≈100 check and silently drops a published tier that has no priced card — a player-facing money claim valid only when the distribution happens to be coherent; admin's own `pub_ev` naming distinguishes promised vs paid                                                                                                                                                                                   | bug/product               | MED           | S                | LOW-MED | HIGH (math) / MED (freq) | 119                       |
| 9   | Four QA scripts wired to nothing; `qa-motion13` prints `motionNodes` but never asserts it + SKIP branch exits 0 on a 3-surface run (its own comment admits the vacuity); `qa-prod-smoke` never asserts #473's home route-cache deliverable (self-documented silent-failure mode); `qa-postwipe-spin` asserts a spinner while claiming "DB is the assertion surface" and a non-local `QA_BASE` spends real credit; false `mkdirSync` comment propagating across 3 scripts | tests/dx                  | MED           | S–M              | LOW-MED | HIGH                     | 120                       |
| 10  | `ResetCountdown` pins itself for the week when a client clock ahead of the server triggers a rollover refresh that returns the SAME `resetAt` — healthy countdown over last week's standings, the exact state it exists to prevent                                                                                                                                                                                                                                       | bug                       | LOW-MED       | S                | MED     | MED                      | 121                       |
| 11  | `rejectAdminPhoneWrite`'s `/admin/customers/*` matcher also catches `/:id/addresses` (validator accepts `phone`) — a shipping-contact phone blocked by an account-uniqueness guard; latent (no admin address screen), undocumented                                                                                                                                                                                                                                       | security-adjacent         | LOW           | S                | LOW     | HIGH                     | — (doc note, not planned) |

### Round-13 plans

| Plan | Title                                                                                            | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---- | ------------------------------------------------------------------------------------------------ | -------- | ------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 113  | Free-pack gate desktop badge precondition                                                        | P1       | S      | —          | **DONE** — APPROVE 2026-08-22, commit `b0f18a32`. Executor KEPT the `End` keypress the plan's literal "replace this block" wording would have deleted; correct, because assertion (b) scrolls UP hunting an overlap and only finds one from the bottom, so deleting it would have failed BOTH legs including the mobile one the plan calls fine. **Reviewer live-verified the mechanism** against the local prod build: `catalog-root` computed `paddingBottom` = 224px mobile (`pb-56`) / 176px desktop (`lg:pb-44`), and the refit math (railRight=1034, rightOffset=16, badgeW=112 → fitted=1122) DOES park a tile under the badge — the exact CI blocker, resolved. Full gate could not finish locally for an unrelated environmental reason; see "Live verification" below.                                                                                                                                                               |
| 114  | Telegram board hardening (guarded fetch, fallback, clamp, env, 429)                              | P1       | S      | —          | **DONE** — APPROVE 2026-08-22, commits `1a1ecfcb`+`a7b01280`. `fetchBytes`/`isAllowedImageUrl` lifted to `api/utils/image-fetch.ts`, re-exported from `bake-slab.ts` so every existing importer compiles untouched; `blackBackedPhoto` routed through it with `limitInputPixels`. Reviewer confirmed both photo paths wrapped in `attempt()` while the final `sendMessage` stays bare (its throw must reach `postApexPull`'s catch). Clamp uses an EMPIRICALLY MEASURED scaffold (317 → `SCAFFOLD_MAX` 360, `PER_FIELD` 131; 5×132+360=1020 ≤ 1024) plus a drift-guard spec — no guessed expansion factor. 429 retry bounded to one attempt ≤5s. Executor caught its own clamp test as vacuous (plain `x`/`y` never cuts mid-entity), replaced with `&`/`"` repeats, red/green-proved.                                                                                                                                                         |
| 115  | Telegram smoke-script safety (named test customer, id-before-delete)                             | P2       | S      | —          | **DONE** — APPROVE 2026-08-22, commit `dedd1705`. `TELEGRAM_SMOKE_CUSTOMER_ID` now required; the script refuses rather than posting a fabricated apex pull under an arbitrary REAL customer's display name. Message id logged before any delete attempt; `deleteApexPost` wrapped in a `.catch()` that RESOLVES to `{ok:false,description}` so the `finally` row-cleanup still runs (reviewer verified). "Safe to run against the live channel" claim corrected. Expected first operator run: refuses until the env var names a durable test account.                                                                                                                                                                                                                                                                                                                                                                                          |
| 116  | Cross-instance cache truth (comment/doc + staleness ladder; no mechanism change)                 | P2       | S      | —          | **DONE** — APPROVE 2026-08-22 after 1 revision, commits `9b22da71`+`0dfea604`. Operator chose option (A). Executor REPORTED rather than silently expanding when Step 2's grep came back dirty: the plan's Current State had missed three more per-process caches carrying the same tripped sentence (`store/challenge/route.ts`, `utils/profile-cache.ts`, `modules/packs/pricing.ts`). Reviewer verified all three, formally extended scope, revision closed them. `grep -rn "if we ever run" backend/packages/api/src/` now returns **0**; all six surviving `>1 instance` hits are the decision record. Comment-only confirmed by filtering added lines.                                                                                                                                                                                                                                                                                    |
| 117  | TTL-cache bounds + slug gate + shape-throw + getChallenge adoption + tests                       | P2       | M      | —          | **DONE** — APPROVE 2026-08-22, commit `40528e23`. Cache size-bounded (256, oldest-insertion); key gate is catalog-bounded, not shape-only (executor found the storefront `Pack` exposes the backend slug as `.id` via `toPack`, not `.slug`). **Plan premise was wrong on one file and the executor caught it**: the backend `recentCache` already had a `size > 64 → clear()` from PR #440, predating the baseline, so a stacked 256 cap would be dead code — it REPLACED the coarse clear with oldest-insertion eviction (also avoids thunder-herding every hot key). New `cache-contract.test.ts` asserts CALL COUNTS and separates legit-empty-IS-cached from malformed-NOT-cached; one case red/green-proved.                                                                                                                                                                                                                             |
| 118  | #468 follow-ups (stock-take wiring test, cap window case, partial-take diagnostics, iso comment) | P2       | S–M    | —          | **DONE** — APPROVE 2026-08-22, commit `766295d1`. `takeCardStock`'s split-location wiring pinned by a container-fake spec driving the real `cardInventoryLevels → planCardStockTake → adjustInventory` chain, **mutation-proved** (reverting to the pre-#468 body fails exactly the split case). Cap window-boundary case added and **mutation-proved**. `updateGlobePayDeposits` CAN write `created_at` via the `as never` hatch already used in `globepay-reconcile.spec.ts` — the STOP condition never fired, proven not assumed. `CardStockTakeError` stops the "counter reads high" log from lying when it reads LOW; earmark semantics unchanged. Integration run for real: cap 5/5, challenge-settle 17/17.                                                                                                                                                                                                                             |
| 119  | EV display validation (suppress row when odds don't sum / cover the pool)                        | P3       | S      | —          | **DONE** — APPROVE 2026-08-22, commit `c1ff85ec`. Guard tracks published vs contributing mass, `SUM_TOLERANCE` 0.5. A STOP condition fired — an existing test pinned the bug (`{Rare: 50}` expecting non-null `'RM 50.00'`) — and the executor reported it with the fixture before changing it, as the plan directs. **Reviewer independently verified the repair preserves the test's purpose**: retargeted to `{Rare: 100}`/`'RM 100.00'`, so if unpriced `—` cards were wrongly averaged in, the result would be 50 and it would still fail. Admin warning render-only; `allValid` is a pure extraction, Save untouched. Verified with `tsc -b apps/admin` (turbo skips it).                                                                                                                                                                                                                                                                |
| 120  | QA-gate wiring + de-vacuate motion13 + prod-smoke cache assertion + postwipe truth               | P3       | S–M    | 113        | **DONE** — APPROVE 2026-08-22 after 2 revisions, commits `01fdfca5`, `15180171`, + header fix. **Revision 1 caught a regression that would have created the next never-green gate**: the plan's own `nodes > 0` assertion made the newly-wired nightly exit 1 on 4 of 6 surfaces. Reviewer probed `[style*="opacity"]` at 100/300/600/1200/2500/3700ms — `/` and `/slots` read 0 at EVERY point, so the heuristic was invalid, not mistimed, and unrescuable by earlier sampling; dropped, keeping `stuckHidden()` as the real vacuity guard plus the SKIP→FAIL fix. Gate now **6/6 clean, exit 0, live-verified**. `qa-reset-countdown` deliberately NOT wired (CI seeds no challenge → would be red nightly); `qa-prod-smoke` is `workflow_dispatch`-only with SHAs pinned to the repo's style; executor caught a real `inputs.base` empty-string bug in its own workflow. Postwipe balance assertion written from source, runtime-unproven. |
| 121  | ResetCountdown bounded retry ladder                                                              | P3       | S      | —          | **DONE** — APPROVE 2026-08-22, commits `ce0c49df`+`e5acfac3`. Bounded ladder: ≤5 refreshes, 20s apart, per deadline. The three preserved properties (tick-down, late-mount, backwards-clock-is-skew) passed with ZERO test edits, as required. **The executor mutation-tested its own new tests and found two vacuous** — both ladder-stop cases remounted to a FUTURE deadline, so the outer `now >= resetAt` gate alone explained the pass and they survived removing the per-deadline keying; it added an 8th case using a new deadline that is ALSO already past, which genuinely depends on that keying.                                                                                                                                                                                                                                                                                                                                  |
| 122  | qa-motion13 demo-spin crash safety (try/finally + FAIL-not-throw)                                | P2       | S      | —          | **DONE** — APPROVE 2026-08-22, commit `203bf1cd`. Closes the follow-up plan 120 left out of scope. Body now wrapped in `try { … } finally { await browser.close(); }` and the demo-spin block in its own `try`/`catch`, matching the shape `qa-free-pack.mjs` and `qa-reset-countdown.mjs` already use; `process.exit()` replaced by `process.exitCode` so the summary is never truncated. **Both paths live-proved**: happy path `6/6 surfaces clean` exit 0 (reviewer re-ran independently), and a forced-failure run (spin-button regex mutated to something unmatchable) printed `FAIL demo-spin — locator.waitFor: Timeout 30000ms exceeded.`, summary `5/6`, exit 1, no stack trace — the mutation reverted and `git diff` confirmed clean. Reviewer verified with `git diff -w` that the diff contains ZERO assertion or logic changes; the one non-indentation edit is hoisting `const results = []` above the `try` (a scope necessity, since the summary reads it after the `finally` — same precedent as `qa-reset-countdown.mjs` hoisting `first`/`second`). Exactly one `results.push(false)` on the throw path, which the `5/6` arithmetic proves. **Extended after Sourcery's automated review on PR #475 raised a real residual gap** (commit `654bee30`): the outer `finally` closed the browser but a throw from `visit()` / screenshot / evaluate still propagated past the summary, so the gate could STILL crash with a raw stack. Added an outer `catch` — one `FAIL script aborted — <msg>` line, one `results.push(false)`, no rethrow — so control always reaches the summary and `process.exitCode`. Pure 11-line addition; the inner demo-spin catch is byte-identical. Proved live with no source mutation by pointing the script's `argv[2]` base at a dead port: `FAIL script aborted — page.goto: net::ERR_CONNECTION_REFUSED`, summary printed, exit 1, and `grep -c '    at '` = **0** stack-trace lines (reviewer ran this independently). Happy path still 6/6, exit 0. |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

### Round-13 execution & live verification (2026-08-22)

All nine plans were executed **in dependency order** by dispatched
`general-purpose` executors onto a single batch branch,
`advisor/round13-batch`, each reviewed by the advisor before the next was
dispatched. Two plans took revision rounds (116, 120); both revisions were
triggered by the executor surfacing a defect in the PLAN rather than by
executor error, which is the intended failure direction.

**Gate results on the branch** (all run by the reviewer, not trusted from
executor reports):

| Gate                                     | Result                                        |
| ---------------------------------------- | --------------------------------------------- |
| Storefront `npm run typecheck`           | exit 0                                        |
| Storefront `npm test` (vitest)           | **665/665**, 74 files (was 645 on master)     |
| Storefront `npm run format:check`        | exit 0                                        |
| Storefront `npm run build`               | success (24 routes)                           |
| Backend `corepack yarn check-types`      | exit 0                                        |
| Admin `tsc -b apps/admin/tsconfig.json`  | exit 0 (turbo skips this — must be run alone) |
| Backend `corepack yarn test:unit`        | **1843/1844**, 1 pre-existing skip            |
| Backend integration (cap, challenge)     | 5/5 and 17/17                                 |
| `npm run qa:catalog-groups` (live :4000) | 15/15 ✓                                       |
| `npm run qa:reset-countdown` (live)      | ✓ ticking, exit 0                             |
| `node scripts/qa-motion13.mjs` (live)    | **6/6 surfaces clean, exit 0**                |

**`npm run lint` exits 1 locally and that is NOT a branch defect** — both
errors are `require()` calls in
`.agents/skills/security-audit/validate-findings.cjs`, which is UNTRACKED
and excluded via `.git/info/exclude`. CI's `actions/checkout` never
materialises it, so the CI lint gate is green. `npx eslint` over every file
this branch touched exits 0. Do not "fix" this file.

**The free-pack gate cannot complete on this machine, for a reason that has
nothing to do with plan 113.** It dies at the first `page.goto('/slots',
{waitUntil:'networkidle'})` AFTER the customer logs in and accepts cookies.
Diagnosed to exhaustion: with consent accepted the Meta Pixel loads
`https://connect.facebook.net/en_US/fbevents.js`, and in Playwright's
Chromium here that request never settles, so `networkidle` can never fire
(the anonymous leg passes because it clicks **Reject** and the pixel never
loads; `curl` reaches the same URL in 150ms, so it is a browser-level
quirk, not connectivity). The goto is PRE-EXISTING code that plan 113 does
not touch, `src/components/MetaPixel.tsx` is untouched by this branch, and
the same step passed in the 2026-08-21 CI nightly — CI does not have this
quirk. Plan 113's actual new mechanisms were therefore verified directly in
a real browser with only that one request blocked; the numbers are in 113's
status cell above. **The authoritative proof is the next nightly.**

**WATCH — the next nightly run** is the real verdict for two things at once:
that the free-pack gate finally goes green on the 5-pack CI catalog (plan
113), and that the newly-added motion gate holds there (plan 120). If
free-pack still fails, the failure message now prints `railRight`,
`rightOffset`, `badgeW` and `fitted`, so that single run hands over the
numbers instead of costing another round.

### Round-13 dependency & sequencing notes

- **113 first** — restores a green nightly so any later merge's gate result
  is attributable. 120 adds a new nightly gate (motion13) and depends on
  113 landing so the first motion result isn't lost in an already-red run.
- 114 and 115 are both Telegram but touch DIFFERENT files (shared module vs
  the smoke script) — either order, no conflict.
- 116 and 117 both touch the cache cluster: 116 is comment-only in
  `ttl-cache.ts`'s header + route comments; 117 owns `ttl-cache.ts`'s CODE
  and the adopter files. Run 116 first, or coordinate the `ttl-cache.ts`
  header edit. Neither blocks the other functionally.
- 118 is self-contained (backend tests + diagnostics). 119/121 are
  independent storefront fixes.
- **Dispatch note (unchanged from round 12):** `.claude/settings.local.json`
  sets `worktree.bgIsolation: "none"`, so a BACKGROUND executor gets no
  worktree and edits the main tree directly. Dispatch executors in the
  FOREGROUND with explicit worktree isolation; check `git status` in the
  main tree afterwards. Command-table traps learned in round 12 still
  apply: `corepack yarn check-types` does NOT cover `apps/admin` (use
  `./node_modules/.bin/tsc -b apps/admin/tsconfig.json`); backend eslint is
  vacuous for `packages/api`; jest 30 uses `--testPathPatterns` (plural);
  use the repo's pinned `./node_modules/.bin/prettier`, not npx.

### Round-13 findings considered and rejected / verified clean

Recorded so nobody re-audits them.

- **Deposit-cap race (#429) is genuinely closed** — `pg_advisory_xact_lock`
  is xact-scoped, the windowed count and the insert both ride one
  `@InjectTransactionManager` transaction, the key is a full 64-bit
  `hashtextextended` (no truncation), a lock-acquisition failure aborts
  before any gateway call (customer paid nothing), and callback-vs-sweep
  idempotency is anchored on `topupIdempotencyReference` + a conditional
  status flip on both writers. Only the TEST coverage and one missing
  isolation comment are findings (plan 118), not the fix.
- **`planCardStockTake` arithmetic** — Σ(plan) === qty on every branch; the
  negative-remainder is the documented operator "units owed" signal, not a
  clamp bug. The PURE planner is well pinned; only its wiring is not (118).
- **No cached storefront loader reads auth state** — all four call public
  `/store/*` paths with a publishable-key-only SDK; the home shell is
  client-resolved (`layout.tsx` renders auth-dependent chrome as `'use
client'`), so no logged-in variant can bake into the cached `/` render.
- **Telegram caption injection** — `escapeHtml` covers all four entities
  Telegram's HTML parser accepts and every interpolation passes through it;
  the one unescaped `tier` is provably a `RARITY_ORDER` member (gate
  rejects anything else). Bot token never logged (only `err.message` /
  `description` reach the logger; OpenTelemetry commented out). Committed
  spec uses a `__SECRET__` placeholder, not a literal token.
- **Deps sweep (#465)** — zero root manifest/lock drift; `motion` 12→13 is
  the only major and all 8 import sites use only the compatible surface;
  `lucide-react` was already 1.x (a minor bump, not 0.x→1.x); the removed
  `@tanstack/react-table` decl was genuinely dead (zero imports).
- **`ci.yml` delta** — one line, a `dorny/paths-filter` SHA pin; no new
  step, nothing vacuous.
- **`do-apply.ps1` placeholder guard** intact incl. the new
  `TELEGRAM_BOT_TOKEN` key; **settlement `missingGross` (#463/plan 108)**
  surfaced for both directions with the admin type + page consistent;
  **`money-path-accuracy-audit` doc** has no remaining internal
  contradiction (C2/C3 CLOSED).
- **`prod-catalog.data.ts`** (round 12) still carries zero customer-shaped
  data — not re-swept, prior verdict trusted.

### Round-13 direction (options for the maintainer — not bugs)

Nothing new worth inventing this round; the standing options remain the
real ones, each already grounded:

- **Account-lifecycle slice extraction** from `service.ts` — plan 111's
  recommended FIRST seam (ahead of the 054 challenge slice); re-locate
  symbols by name.
- **Partial unique index for one-phone-one-account** — the follow-up plan
  110 unblocks, **gated on running `report-duplicate-phones.ts` against
  PRODUCTION first** (it has only ever run on the local dev DB: 4 dup
  values / 15 accounts). Composite `(phone, has_account)` is the
  invariant-free option (mirrors core's email index).
- **Free-pack re-arm policy** — still BLOCKED-pending-operator-decision
  (stamping the grant at the real verify path would newly grant every
  legacy customer a free pack). Deliberately not a plan.
- **Plan 057's 42 storefront design/UX findings** — still TODO and
  unscheduled.

### Round-13 coverage — what was NOT audited

- Any whole-repo re-sweep of surfaces rounds 1–12 covered. Scope was the
  `16cc85d3..30eded61` delta plus reconciliation; earlier rejection lists
  remain in force.
- No live local runtime drive — the audit read code + `gh` CI logs + one
  read-only prod `curl`. Each plan's verification commands are the
  executor's runtime proof.
- The storefront visual/UX surface (plan 057), backend dependency
  advisories beyond the known Medusa/Mercur pins, the admin SPA browser
  walk, `backend/apps/vendor` (dormant), and live third-party console state
  (Telegram, GlobePay, Twilio, DO dashboards).
- Finding 11 (`rejectAdminPhoneWrite` address sub-route over-reach) was
  NOT turned into a plan — it is latent (no admin address screen exists)
  and the fix is a one-line docblock note or a matcher tightening; recorded
  here as the maintainer's call. If addresses ever become admin-writable,
  promote it.

---

## Round 14 — deep audit (PRs #474–#496 + whole-repo sweeps), 2026-08-26

Generated by the `improve` skill (**deep** tier) against commit `affaab51`
(master). Scope: the delta since round 13's base `30eded61` — 16 PRs / 368
tracked files / **+27,528 / −13,923** — plus whole-repo sweeps of the
categories rounds 1–13 left thin. The delta's headline surfaces had never been
audited by any round: the **#490 weekly commission engine + `/task` +
`/referral` + their admin consoles**, the **#491 wallet-charged delivery fee and
mandatory insurance**, the **#478/#479 operator write-scripts**, and the
**#496 Medusa 2.19 / Mercur 2.3.1 platform upgrade** (which is HEAD itself).

Method: **eight parallel read-only Explore subagents**, one per playbook
category (commission-engine correctness; delivery/operator-script security;
platform-upgrade fallout; test coverage; performance; tech debt; deps + CI/DX;
docs + direction), each handed the audit-playbook path, the recon facts, the
prior rejection lists as suppression, and Hard Rules 4 and 6 verbatim. Every
surviving citation was then re-opened and confirmed by the advisor before it
entered the table below — several subagent findings were corrected or dropped
at that stage.

### Reconciliation

- **Plans 123 and 124 were missing from this index entirely.** Both shipped:
  123 → `dac14a59` (#478, audited operator account-deletion path), 124 →
  `2061fc51` (#479, operator script to release a duplicated phone). Rows added
  below. The index had been stale since 2026-08-22.
- Standing TODOs carried forward unchanged: **047**, **054** (drift-proofed by
  111), **057** (42 storefront UX findings, unscheduled), **043**
  (operator-applied).
- **#496's "cleared every Dependabot alert" claim was verified, not trusted**:
  the open-alerts API returns empty; 116 repo-local migration class names
  intersected against 223 installed Medusa/Mercur names gives **zero
  collisions**; mikro-orm is a single 6.6.14 family with no `resolutions` entry
  left; react-router deduped to 7.18.2. The round-13 blockers are genuinely
  resolved.
- **A standing repo fact changed.** Medusa 2.19's `routes-sorter.js:94-102` now
  handles a zero-segment `"/"` matcher explicitly ("ensures it is not dropped
  from the tree"). The bare-root-matcher drop that `middlewares.ts` documents at
  length is a **pre-2.19 framework bug**. The `'/*'` form is still correct and
  is retained deliberately; only the rationale was wrong (plan 128).
- **Branch protection turned out weaker than every prior round assumed** — see
  finding 1. The recorded belief was "a skipped job counts as passing"; the
  reality was that the backend jobs were not required checks **at all**.

### Round-14 findings table (vetted — every citation re-read by the advisor)

Tier 1 — highest leverage:

| #   | Finding                                                                                                                                                                                                                        | Category       | Impact   | Effort | Risk | Confidence | Plan            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | -------- | ------ | ---- | ---------- | --------------- |
| 1   | Backend CI jobs were **not required checks**. Required set was `quality`/`gitleaks`/`dependency-review` only, so a backend-only PR with all 8 `integration-http` shards RED was mergeable. 13 rounds of backend gates were advisory | ci             | HIGH     | S      | MED  | HIGH       | operator (done) |
| 2   | Delivery zone anti-spoof control dead in production — no address form collected `province`, collapsing the composite check to a 12-city regex. East address outside those cities + West postcode = RM15 instead of RM35, repeatable | security/money | HIGH     | M      | MED  | HIGH       | 126             |
| 3   | An unspent free rip vanishes from `/task` at the Monday reset; the entitlement row survives and is redeemable only by URL, and the unique claim index means it can never be re-earned                                             | bug            | MED-HIGH | S      | LOW  | HIGH       | 125             |
| 4   | Weekly commission payout bypasses `mutateCreditAtomic`, so a referrer whose AUTO freeze the payout repays is credited and **stays frozen**                                                                                        | bug/money      | MED      | S      | LOW  | HIGH       | 125             |
| 5   | Turbo cache keyed on `github.sha` — nine ~900 MB entries (~8.09 GB of a measured 12.46 GB) held the repo over GitHub's 10 GB cap, evicting the `bnm2` node_modules cache. The mechanism behind the long-documented cold-install mystery | ci/perf        | MED      | S      | LOW  | HIGH       | 127             |
| 6   | `RF` never reached the admin ledger console — payouts render as the literal `ledger.typeRf`, no filter tab, and the comment still claimed referral payouts were "cancelled"                                                        | bug/docs       | MED      | S      | LOW  | HIGH       | 125             |
| 7   | Delivery money math hand-copied storefront↔backend (rates + `EAST_PLACE_RE` character-for-character), joined only by a "keep in sync" comment. Sixth instance of this pattern                                                      | debt/money     | MED      | S      | LOW  | HIGH       | 126             |

Tier 2 — high value, cheap:

| #   | Finding                                                                                                                                                                                              | Category   | Impact   | Effort | Risk | Confidence | Plan      |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ------ | ---- | ---------- | --------- |
| 8   | Mercur collision-repair migration (`Migration20260826000000`) is untested on the only DB class it exists for — every CI database is ephemeral/fresh, where it no-ops. Its two `offer` ALTERs are also silently order-dependent | migration  | MED-HIGH | M      | LOW  | HIGH/MED   | TODO      |
| 9   | `CONTEXT.md` taught a `vip_rebate` reason and a settlement-line `kind` that do not exist, in the section ADR 0007 designates canonical for the new money engine                                        | docs       | MED      | S      | LOW  | HIGH       | 128       |
| 10  | Kept-orphan registry named `PrizeReveal.tsx`, which **#490 deleted** — so ADR 0004's "revert, not rewrite" bargain is already partially impossible                                                     | docs       | MED      | S      | LOW  | HIGH       | 128 + 129 |
| 11  | Neither referral cron job has any spec; `pay-referral-week`'s per-run isolation `try/catch` would survive deletion green                                                                               | tests      | MED      | S      | LOW  | HIGH       | TODO      |
| 12  | `src/lib/actions/tasks.ts` untested; the attribution seam is mocked-but-unasserted; `/task`, `/referral`, `/invite` gated by nothing at any layer; four new QA scripts wired to nothing                | tests      | MED      | S–M    | MED  | HIGH       | TODO      |
| 13  | Edit-time typecheck hook silently passes for `@acme/odds-math`, `@acme/pokemon`, `apps/vendor` — the odds/money package type-checks green against a program that excludes it                           | dx         | MED      | S      | LOW  | HIGH       | 129       |
| 14  | Four GlobePay URL variables the money paths hard-require were absent from the tracked env template — dead top-up and dead payout channel, neither error naming a variable                              | dx         | MED      | S      | LOW  | HIGH       | 127       |
| 15  | `/privacy` omitted the mandatory phone + SMS processor and the public Telegram name broadcast (admin-disable only, no customer opt-out)                                                                | docs/legal | MED      | S      | LOW  | HIGH       | 128       |
| 16  | `CLAUDE.md` calls `/task` a placeholder (it is nav slot 1) and omits `/referral` + `/privacy`                                                                                                          | docs       | MED      | S      | LOW  | HIGH       | 129       |
| 17  | `AGENTS.md` still mandates pixel-perfect 1:1 emulation of a removed reference site, against `PRODUCT.md`'s redesign — copied into four generated agent files; also contradicts `CLAUDE.md` on MCP precedence | docs/dx    | MED      | S      | LOW  | HIGH/MED   | 129       |
| 18  | Settlement status unions hand-copied into admin; unlike `globepay-withdrawal.ts`, the #490 models never exported the tuple that makes a parity test possible                                           | debt       | MED      | S      | LOW  | HIGH       | TODO      |
| 19  | The three new destructive-migration `down()` guards have no spec, though the pattern exists in `migrations/__tests__/`                                                                                 | tests      | MED      | S      | LOW  | HIGH       | TODO      |
| 20  | Go-live runbook's checklist had no step for `task_definition` / `referral_settings` — a documented wipe ships an empty primary tab and an invisible-but-live commission rate                           | docs/ops   | MED      | S–M    | LOW  | HIGH/MED   | 128       |

Tier 3 (real, lower leverage, not yet planned): `/referral` aggregate scans
lifetime `pack_open` (window in `SUM(CASE)` not `ON`) · task hub runs two
lifetime scans unconditionally, twice per claim · Customer-360 ships 1,000
downline rows to render `.length` · settlement review drawer unpaginated
(`take: 100_000`) · task banners missing `sizes` · `ingest-pc-image` bypasses
the #477-capped fetcher · `.mercur/index.d.ts` stale (5 dangling route imports,
`skipLibCheck` hides them) · task vocabulary across 7 hand-copied sites ·
`PRODUCT.md` two-tier/series/marketplace · `DESIGN.md` nav contract · CI
timeouts ~5× above measured · TS 6 / TS 5.9.3 + ESLint 9 / ESLint 10 split ·
`check-types` hard-codes a hoist path · `toLocalInput` duplicated in admin ·
referral tier validation duplicated · the design spec has no valid H1.

### Round-14 plans

| Plan | Title                                                                              | Priority | Effort | Depends on | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---- | ---------------------------------------------------------------------------------- | -------- | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 123  | Operator-audited account-deletion path                                             | P1       | M      | —          | **DONE** — shipped `dac14a59` (#478). Row added retroactively in round 14; the index had never recorded it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 124  | Operator script to release a duplicated phone                                      | P1       | S–M    | 123        | **DONE** — shipped `2061fc51` (#479). Row added retroactively.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 125  | Commission-engine follow-ups (free rip, auto-unfreeze, `RF` console)               | P1       | M      | —          | **DONE** — APPROVE 2026-08-26, PR #498. Both new tests mutation-proved by the executor **and independently re-proved by the reviewer against a live database**: reverting `pendingSpins` to the period-scoped array fails exactly the rollover case (1 failed / 11 passed); emptying the job's unfreeze loop fails exactly scenario (C) at `expect(state.frozen).toBe(false)` _after_ the credit assertions pass. Executor improved on the plan: proved the admin `tsc -b --force` check was live by injecting a TS2322, since this repo's admin tsconfig is a refs stub where `tsc -p` falsely passes.                        |
| 126  | Delivery zone State field + fee-math parity lock                                   | P1       | M      | —          | **DONE** — APPROVE 2026-08-26 after 1 revision, PR #499. **The executor found a defect in the plan's scope list**: `OrdersClient.tsx:153` is a THIRD address-writing form, wrongly excluded on the premise that it only reads `province` (lines 193/581 are display reads). Reviewer verified and formally extended scope. A fourth breakage surfaced only under a full suite run — `address-book.test.ts:111` blanks `province` on a MY fixture; moved to `countryCode: 'SG'`, where the null-mapping assertion is still reachable. Backend `delivery.ts` unchanged; storefront `delivery-fee.ts` changed by exactly one word. |
| 127  | Turbo cache key, GlobePay env truth, format gate, tsconfig excludes                | P2       | M      | —          | **DONE** — APPROVE 2026-08-26, **MERGED `f5c0463d` (#497)**. Executor corrected the plan twice: the `git show -w` done-criterion was invalid (prettier normalises quotes, so it proved semantic inertness by `JSON.parse` deep-equality and `yaml.safe_load` equality instead), and the **existing** `ci.yml` comment claiming `bnm2` "has no stored entry at all" was itself false — four entries existed and were being _evicted_. Step 3 (typecheck hooks) descoped mid-run: `.claude/` is gitignored, so those files do not exist in a worktree → moved to plan 129. Format widening narrowed to a code-extension glob.      |
| 128  | Docs truth round 8 (tracked files)                                                 | P2       | M      | —          | **DONE** — APPROVE 2026-08-26, PR #500. **Step 1 correctly REFUSED as a STOP condition**: the design spec's head fragment is NOT duplicated — the Phase-B passage is a different, vaguer sentence omitting the `task_claim` unique index and the free-rip/`rollOne` decision, so deleting it would have destroyed the only record of that decision. The file still has no valid H1; the fix is a relocation, left as an operator content call. Executor also showed the plan's `grep -rl SUSPENDED src/` recipe is wrong **twice**. `git diff -w` on all four comment-only files: zero non-comment lines.                       |
| 129  | OPERATOR-APPLIED — gitignored files (`.claude/hooks/`, `CLAUDE.md`, `AGENTS.md`, `PRODUCT.md`) | P2       | S–M    | 128        | **TODO (operator)** — not worktree-dispatchable; `.gitignore:105-119` ignores all of it. Precedent: plan 043.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

### Round-14 findings considered and rejected

Recorded so nobody re-audits them.

- **The "sixth mirror" (`GlobePayWithdrawalView`, `admin-rest.ts:1124`) — CLOSE
  IT.** Round 11 left it recorded as "partially self-guarding… candidate for the
  next debt pass". It is in fact **transitively locked**: `page.tsx:44` declares
  `const VIEWS: GlobePayWithdrawalView[]`, and
  `globepay/withdrawals/__tests__/list.unit.spec.ts:146-148` asserts set
  equality against `[...WITHDRAWAL_STATUSES, 'all']` by reading the real
  `page.tsx` source. A backend status addition turns that test RED, forcing the
  union to widen. The only unguarded direction is a stale _extra_ member the
  `VIEWS` array never uses — inert. Retired as a debt candidate.
- **`payWeeklySettlement`'s single transaction** — `service.ts:1448-1450`
  carries an explicit `ponytail:` comment naming the ceiling and its upgrade
  path. A recorded deliberate tradeoff, not a defect. What is _not_ settled is
  that its trigger has no measurement.
- **`qa-delivery-fee.mjs:17` hardcoded password** — an ephemeral account the
  script creates itself, matching a convention across ~20 QA scripts. Not a
  credential.
- **`/privacy`'s referral-invite cookie claim** — correct.
  `src/lib/referral-cookie.ts` was rebuilt in #490.
- **Admin `tsconfig` references stub** — real, but does not bite: CI type-checks
  admin via `tsc -b` inside its `build` script, and the hooks target
  `tsconfig.app.json`. No script runs `tsc -p` against the stub.
- **`brace-expansion` GHSA-rgw5-rvv9-x895 (high)** — reachable only through
  eslint/ts-morph dev tooling, not runtime or distribution. Worth noting that
  zero open Dependabot alerts did **not** mean zero high advisories.
- **Storefront delivery-status enum copy** (`schemas.ts:628-637`) — a documented
  transitional old∪new set; both consumers are exhaustive `Record`s derived from
  the schema, so drift drops a row loudly rather than mislabelling one silently.
- **Referral removal ⇄ rebuild stranded fragments** — checked both directions,
  none. Everything ADR 0007 lists as removed is gone; everything on its "kept
  deliberately" list has live importers.
- **`refuseCrossOriginAdminWrite` failing open on unset `ADMIN_CORS`** —
  documented decision with correct reasoning in the file header; the real
  control is the global `sameSite: 'lax'` cookie option.
- **Delivery money-path integrity** — verified clean: the fee derives only from
  the server-side address snapshot, ownership is enforced on request/cancel/edit,
  the charge and order write share one transaction with a floor guard that rolls
  back, and double-submit is blocked by a conditional `WHERE status = from` flip.
- **`closeReferralWeek`** — clean. One grouped aggregate plus three batched `IN`
  reads, riding the partial index; the hourly schedule short-circuits on the
  `week_start` unique index _before_ the aggregate, so the scan runs once per
  week, not 168 times.

### Round-14 direction (options for the maintainer — not bugs)

Standing options are unchanged and not re-proposed: account-lifecycle slice
extraction, the partial one-phone index (still gated on running the duplicate
report against PRODUCTION), free-pack re-arm policy (blocked pending an operator
decision), and plan 057's 42 storefront UX findings. New, each grounded in the
shipped engine:

- **The weekly commission pays out silently.** `jobs/pay-referral-week.ts` has
  zero matches for `notify`, `feed` or `email`, while every sibling money job
  (`globepay-reconcile`, `globepay-withdrawal-reconcile`,
  `settle-challenge-week`) calls `notifyFeed`. `PRODUCT.md` says this loop exists
  to drive retention; it currently deposits money and tells nobody, and
  `/referral` shows only the current week. `weekly_settlement_line` already
  stores every paid line. Coarse S–M.
- **Every shared invite link previews as the generic homepage card.**
  `/invite/[handle]` resolves the handle server-side then discards it in a
  redirect; the app has one static sitewide OG image. This is the top of the
  funnel for the engine that just shipped, pasted into chat apps by design.
  Coarse S.
- **The human approve gate has no fraud signal.** The design spec puts anti-fraud
  automation out of scope _beyond the human approve gate_ — and that gate renders
  raw `customer_id` / `basis_cents` / `rate_bp` with no downline expansion or
  outlier flag, while `player_payout_details` + `referral_attribution` make "N of
  one downline share a payout destination" a join away. Ship advisory-only, never
  auto-block. Coarse M, design/spike first.

### Round-14 coverage — what was NOT audited

- **No runtime anywhere during the audit** — no build, no test run, no live
  database, no browser. Every audit verdict is static reading at `affaab51` plus
  read-only `gh api`. (The _plans_ were then executed and verified for real; that
  is a separate phase.)
- No re-sweep of surfaces rounds 1–12 covered; earlier rejection lists remain in
  force, **except** that every pre-round-10 rejection stays scoped to the old
  mock payment gateway.
- `backend/apps/vendor` (dormant), the admin SPA browser walk, plan 057's
  visual/UX surface, and live third-party console state (Telegram, GlobePay,
  Twilio, DO).
- The other 19 files in `docs/superpowers/specs/` were not checked for
  shipped-vs-designed drift. Finding 9 proves that drift class is live in this
  repo, so they are an open surface.
- `backend/yarn.lock`'s 5,179-line delta was not read line by line — the
  _outcomes_ that matter were verified against installed packages instead.
- CodeQL runs on this repo but is default setup with no workflow file, so its
  language and query configuration could not be read.

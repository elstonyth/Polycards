# Waived gitleaks false positives (fingerprint format:
# <commit>:<file>:<rule>:<line> — see ci.yml gitleaks job).

# Synthetic integration-test admin password ('bulk-delivery-test-pw-1'),
# same fixture pattern as every sibling http spec; seeds an ephemeral
# test-DB admin only. Not a credential.
#
# Paired with an inline `// gitleaks:allow` on that line. The fingerprint below
# pins ONE commit sha, and this repo squash-merges — master re-authors the line
# under a new sha, which this entry would no longer match. The inline comment is
# what waives it going forward (sha-independent); the entry stays because the
# branch commit itself is still scanned in history.
8dccef9feb8a4f95484fef1f63f42db364e6fe26:backend/packages/api/integration-tests/http/delivery-orders-bulk.spec.ts:generic-api-key:9

# Synthetic integration-test admin password ('published-odds-test-pw-1') —
# same fixture pattern and same waiver shape as the entry above: inline
# `// gitleaks:allow` carries forward across squash re-authoring, this
# fingerprint waives the already-pushed branch commit in history.
4f23518f2396708bb97a1f4b011722092c683214:backend/packages/api/integration-tests/http/pack-published-odds.spec.ts:generic-api-key:7

# The prod PUBLISHABLE key (pk_…, NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY) quoted in
# a plan doc's verification curl — public by design, embedded in every
# storefront page served to browsers; not a credential. The doc's tip copy is
# redacted to a placeholder, so squash-merged master never contains the key;
# this fingerprint waives the already-pushed branch commit in history.
b7623b23350b471dfa7db8769af0d8128abfd613:docs/superpowers/plans/2026-08-15-public-free-pack-badge.md:curl-auth-header:690

# Integration-test Idempotency-Key literal ('delivery-fee-topup-i2') on the
# topup helper call — a per-test dedupe tag, not a credential. Same waiver
# shape as the entries above: inline `// gitleaks:allow` carries forward
# across squash re-authoring, this fingerprint waives the already-pushed
# branch commit in history.
a7cdf2332bc83510f88d95f57623032857a2c7bc:backend/packages/api/integration-tests/http/delivery-orders.spec.ts:generic-api-key:595
